# Roadmap — Status og prioritering

**Senest opdateret: 21. august 2026 (seksoghalvtredsindstyvende kørsel)** (🔶 **`G8` er SKRUMPET, ikke lukket — og gennemgangen fandt en fejl i regel 3.** Backloggen er 30 → 31: `G8` bliver stående i Tier 6 med sin udløser uændret, og fundet ved siden af er skrevet ned som `G148` frem for taget med. **Rækkens præmis er stadig sand** — `mode_params.tournaments` har aldrig været skrevet i produktion — men en udløser siger, hvornår en række kan **besvares**, ikke hvornår den kan **skrumpes**: at koden er forkert kan læses i repoet, kun det, rigtige data ville vise, kræver ventetid. **Fejlen:** efterfyldningens regel 3 ("en runde, der er gået i gang, vokser aldrig") regnede rundens start pr. TURNERING, fordi `backfillCompetitionMatches()` kaldes med én `season_id` ad gangen. Men en runde er en **kalenderuge** (`round_key()`) og hører ikke til én turnering: for en konkurrence over Superligaen OG Premier League kunne den ene ligas fredagskamp have sat runden i gang, mens efterfyldningen så en urørt runde i den anden og lagde en ny kamp ind under deltagerne. Filen sagde sætningen selv ét niveau nede (*"hele sæsonens kampe og ikke kun konkurrencens egne"*); det var den samme sætning ét niveau højere oppe, der manglede. Rundens start regnes nu over alle konkurrencens turneringer, mens **kandidaterne** stadig kommer fra den sæson, kaldet gælder — og det ekstra opslag udgår helt, når ingen konkurrence har mere end én turnering, altså i dag hver eneste kørsel. **Sømmen er bundet:** ny `multi_turnering.test.js` giver den RIGTIGE skrivers række direkte til den RIGTIGE læser — begge ender var dækket i forvejen, men hver med sin håndskrevne fixture, og en omdøbning af `league_id` ville lade begge tests bestå (efterprøvet: to påstande falder). **Resten af stien holdt:** `competition_status` udleder sæsonerne af konkurrencens egne kampe, `round_key` er turnerings-uafhængig, og læse-stierne går gennem `competition_matches` og rører aldrig `league_id`. **`G148`:** "Start ved næste runde" gemmes ikke i rækken, så efterfyldningen lægger den valgte-fra runde tilbage ved næste sync, hvis den endnu ikke er låst — en rækkeform-ændring og dermed en beslutning. 1592 tests (9 nye), lint på loftet, grønt build. Intet skal køres i Supabase. Se [`DECISIONS.md`](./DECISIONS.md), [`CHANGELOG.md`](./CHANGELOG.md) og [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (femoghalvtredsindstyvende kørsel):** (✅ **Tier 4 og Tier 5 er kørt tomme: `G147` og `G146`.** Backloggen er 32 → 30, og Tier 1, 2, 3, 4 og 5 er nu alle tomme — tilbage står kun det, der venter på en udløser (Tier 6), og det udadvendte (Tier 7). **`G147`:** onboarding-guiden afgjorde, om en turnering har kampe tilbage at tippe, ved at hente ÉN RÆKKE PR. USPILLET KAMP — ~1.900 ved en sæsonstart med fem turneringer, altså over PostgRESTs tavse loft, og udfaldet var en turnering præsenteret som utipbar i netop den skærm, hvor en fejl koster mest. Kuren er ét afgrænset opslag (`limit=1`) pr. turnering. **`G146`:** Championship-fanens to vælgere hentede tusindvis af rækker for at fylde en dropdown med et par snese værdier; ny migrering **`#74 championship_selectors.sql`** giver to `distinct`-views. 🔴 **`#74` skal køres i Supabase FØR mergen** — samme enkeltrettede afhængighed som `#62`. **Læren:** `G106`s regel handler om antallet af KALD, ikke af rækker — er fan-out'en bundet af en liste, vi allerede har, er en løkke billigere end en migrering; er der intet at binde til, er et view svaret. To rækker, der lignede hinanden, fik derfor hver sin kur.)

**Forrige (fireoghalvtredsindstyvende kørsel):** (✅ **`G145` er LEVERET: de globale lister læses sidevis, og reglen er `sbAll`s.** Backloggen er 31 → 32 — rækken er lukket, og to ting, den blotlagde, er skrevet ned frem for taget med (`G146`, `G147`). **Fejlen:** Supabase klipper hvert svar ved projektets `db-max-rows` (1000 som standard) og **siger det ikke** — svaret er 200 med en kortere liste, så `db.select` kan ikke se forskel på "det var alt" og "her er de første 1000". `G139` rettede Hjems fire TAL; de otte upaginerede opslag i `data/standings.js` blev stående. Skaden ville aldrig have været en skærm, der gik i stykker, men en, der så rigtig ud: en runde, der mangler i Championship-vælgeren, et ratingtal, der mangler ved et navn, en spiller under nr. 1000, der ikke kan finde sig selv — og værst `loadRatingHistory` uden bruger-id, som sorterer `round_key.asc` og derfor ville tabe netop de SENESTE runder, altså dem formkurven er lavet af. **Svaret fandtes allerede i repoet, og det er `G140`s lære en gang til:** `sbAll()` i `api/_shared.js` er skrevet mod den samme fælde på jobbenes side (`G51`). Nye `data/paged.js` er dens tvilling i klienten og arver dens to ufravigelige regler ordret — `order=` er påkrævet, og **der stoppes ved en TOM side og aldrig ved en kort**, fordi et projekt med et lavere `db-max-rows` ville gøre hver eneste FULDE side til en "kort" side og dermed bygge den tavse afkortning ind i selve kuren. **Rettelsens første udgave antog netop det loft og var forkert;** den blev rettet af `sbAll`s eget hoved. Prisen er ét ekstra kald pr. opslag — den tomme side, der beviser, at der ikke var mere — og den er billigere end alternativet, en transport uden om `db.select` for at læse `Content-Range`. **Vagten er en attrap, der kan lyve som serveren:** testenes fake-PostgREST klipper nu ved 1000 rækker uden at sige det, fixturen er 2400 ratede brugere og 2400 spillede kampe, og påstanden er `G139`s — Hjem og ranglisten skal give det samme tal, nu også for en spiller på plads 1498. Dertil en strukturel vagt, der læser filens kildetekst og kræver et bevidst `limit=` i hvert `db.select`, der står tilbage. 1573 tests (22 nye), lint på loftet, grønt build, byte-identiske skærmbilleder med modprøve. Intet skal køres i Supabase. Se [`DECISIONS.md`](./DECISIONS.md), [`CHANGELOG.md`](./CHANGELOG.md) og [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (treoghalvtredsindstyvende kørsel):** (✅ **Tier 5 er kørt tom: `G140` og `G139` — og indbakken er ryddet.** Backloggen er 32 → 31: Tier 1, 2, 3 og 5 er tomme, og Tier 4 bærer den ene række, rydningen gav (`G145`). **`G140`:** tre "vis mere"-flader var `<p onClick>` — hverken tastatur, skærmlæser eller skærmbilled-harnessen (som kun kan trykke på `button`) kunne nå dem. Svaret fandtes allerede: `G28` rettede præcis dette på login-skærmen og lagde knappen i `Auth.jsx`s EGEN fil, hvor ingen anden skærm kunne se den. Den hedder nu `TekstLink` i `ui/components.jsx`, og alle fire steder bruger den. **`G139`:** Hjem hentede hele den globale ratingtabel for at vise fire tal. Prisen var to lofter og ikke ét — `A34`s egress OG PostgREST' tavse grænse på 1000 rækker, hvor et for lavt antal og en placering regnet på en afkortet liste ville komme uden en fejl nogen steder (`G106`s fælde). Kortet slår nu sin egen række op og lader databasen TÆLLE resten; formkurven hentes kun for én bruger, og `BoardScreen` kun for konkurrencens deltagere. **Rækkens egen kur var forkert på to punkter, og det er lærepengen:** delt placering afgøres på den AFRUNDEDE rating (`gte.<min + 0.5>`, ikke `gt.<rå>`), og lukkede konti — som filtreres fra listerne i klienten — skal trækkes fra igen. **Vagten er en ækvivalenstest mod ranglisten**, fordi den farlige fejlmåde er et tal, der er *næsten* rigtigt; seks mutationer, alle fanget. **Og samme påstand kørt gennem hele appen: alle fire skærmbilleder er byte-identiske**, `hjem.png` med rating-kortet inklusive — efterprøvet med en modprøve, hvor et tomt opslag flyttede filen. 1551 tests (14 nye), lint på loftet, grønt build. Intet skal køres i Supabase. **`G139` blotlagde en klasse, og den er skrevet ned frem for løst:** otte upaginerede `db.select` i `data/standings.js` læser lister, der vokser. De ordnede taber bunden (en spiller under nr. 1000 kan ikke finde sig selv); `loadRoundsAvailable`, `loadMonthsAvailable` og `loadRatingMap` uden liste taber VILKÅRLIGE rækker, og `loadRatingHistory` uden bruger-id taber de NYESTE runder, fordi den sorterer stigende. Ingen af dem er målt — det kræver produktion (`A32`), og forespørgslen står i `G145`. Se [`DECISIONS.md`](./DECISIONS.md), [`CHANGELOG.md`](./CHANGELOG.md) og [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (tooghalvtredsindstyvende kørsel):** (🔴 **Tier 2 er kørt tom: `G141` rettet, `G142` afgjort.** Backloggen er 34 → 32, og Tier 1–4 er alle tomme. **`G141`:** analytics-tavlens `viewable` regnede dagskortene efter karusellens regel (`created_at < round_key + 7 dage`) — den flade, v3 fjernede. Nævneren målte derfor fra v3-dagen noget andet, end den sagde, under BÅDE visnings- og afvisningsraten. Hver række måles nu på sin egen flades regel: et v3-dagskort på sit vindue (`created_at` → det tidligste af 48 timer og næste `day_key` for samme bruger), rundekortene og v2's dagskort på karusellen som før. Æra-skellet er `news_value is not null`. Udtrykket er **ordret** `_vindue` fra `sql/story_engine_v3_measure.sql`, så tavlen og aflæsningen ikke kan svare hver sit. **`G142` er afgjort frem for bygget:** de døde kort bliver stående som analysedata og holdes ude af nævneren — vinduet er en *model* af `loadDayCard` og ikke en observation (et afvist nyere kort lader et ældre komme frem), så en dagsløkke, der sprang dagen over, ville fjerne kort, brugeren kunne have set; og rækkerne er selve grundlaget for aflæsningen og for `day_card_coverage`. Samme svar som `#48` gav v2's 197 efterfyldte kort. **Tavlens egen forklaring løj med** — måle-ordbogen, sektionens brødtekst og tomtallet *"alle nåede karusellen"* er rettet i samme ombæring. Ny SQL-test `analytics_story_viewable.sql` (femogfyrre i CI), som kræver af sig selv, at fixturen viser fejlen mod skemadumpets gamle RPC først; fem mutationer efterprøvet, alle fanget. 1536 tests, lint på loftet, grønt build. ✅ **`#17 analytics_dashboard.sql` er gen-kørt i produktion 21. august 2026** — kun funktionen, ingen rækker, ingen frontend-ændring at merge sammen med. Se [`DECISIONS.md`](./DECISIONS.md), [`CHANGELOG.md`](./CHANGELOG.md) og [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (enoghalvtredsindstyvende kørsel):** (🔴 **`G144` er BYGGET: stimekortet kan endelig sige, at stimen brød.** Halvdelen af `STREAK_STATUS` var uskrevet — 💤-teksten *"Din stime stoppede ved N"* kunne kun rammes ved et brud på SAMME kampdag som sidste hit, fordi `_sd_streak` valgte `where hit and ended_day = p_day`. Det almindelige brud, en ny kampdag hvor det går galt, var netop det, den ikke kunne se. **Rettelsen er én gren mere** (`or naeste_dag = p_day`); `alive` behøvede ingen ændring, fordi et brudt løb pr. definition har en senere kamp. **To valg er MÅLT frem for antaget:** `max(next_day)` koster 6 % på dagsmotoren mod `array_agg(… desc)[1]`'s 17 % og giver det samme (`match_day` er monoton i `kickoff_at`); og `distinct on (user_id)` ændrer ikke ét kort i dag — mutationen slipper igennem — men holder den form, `_sd_scored`s join på brugeren alene forudsætter. **Det længste løb vinder.** **Reglen havde aldrig kørt, hverken i produktionen eller i en test**, og har nu blok 22 med begge grene, fan-out og det dobbelte løb. To ting i opstillingen kostede hver sin røde kørsel og står som kommentarer: bruddet må ikke ligge på rundens sidste kampdag, og brugeren skal stå alene i sin konkurrence — ellers vinder `DUEL` over stimen, hvilket er `G143`s dominans-fund i praksis. ✅ **`sql/story_engine_v3.sql` (#47) er gen-kørt i Supabase 21. august 2026** — kun funktionen, ingen rækker, ingen frontend-ændring at merge sammen med. Prisen er ~6 % på dagsmotoren. Backloggen er 35 → 34. Spec: §13.11. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (halvtredsindstyvende kørsel):** (✅ **`G143` er BESVARET: stimen er ikke død kode, den er systematisk domineret.** Rækken spurgte, om `STREAK_STATUS` var død kode eller bare uden anledning. **Reglen virker** — motoren er kørt mod en fixture med en levende stime, og kortet blev skrevet til både hovedpersonen og en fan-out-modtager. Det er reglens **første kørsel nogensinde**: testens egen blok 16 sagde allerede, at fixturen aldrig fyrede den, og produktionen havde nul sejre. **Hvorfor den aldrig har vundet:** `_sd_mag` er nøglet på `(competition_id, user_id)`, og stimen har `competition_id = null`, så joinet rammer ingen række — den får KUN stime-bonussen som størrelsesbidrag (48–60), mens `DAY_TOP`, `CONTRARIAN` og `DUEL` får den samme bonus PLUS flytning og over-snit oven i en højere grundvægt. Målt: `DAY_TOP` 72 mod stimens 60, samme bruger, samme dag. Med de to som 69 % af sejrene er nul sejre i 100 bruger-dage overbestemt. **Ordet "udløst" var upræcist:** aflæsningen grupperer på `winner_rule`, og en taber efterlader intet spor. **Én ting ER en fejl:** 💤-grenen *"Din stime stoppede ved N"* kan kun nås ved et brud på SAMME kampdag som sidste hit, selv om kommentaren lover "brudt i dag" — `G144`, med en påstand (blok 22c), der skal vendes om, når den lukkes. Aflæsningsfilen har fået en blok 3, så næste kørsel også siger, hvor tit stimen havde anledningen. Fire mutationer prøvet, alle fanget. Backloggen er 35 → 35, Tier 1 er tom igen. Ingen migrering. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (niogfyrretyvende kørsel):** (✅ **`A33` er BESVARET — variationen er der, men to regler bærer oplevelsen.** Ejeren kørte aflæsningen samme dag, den blev bestilt: 100 bruger-dage, 25 brugere, 4 kampdage, 168 visninger. **Nævneren var det halve arbejde.** Rå gav den `DAY_RESULT` som vinder på 43 af 100 — mistænkeligt tæt på v2's 44 %, som var hele rækkens anledning — men **35 af de 43 er tips-påmindelser**, hvor brugeren ikke havde ét scoret tip og motoren derfor intet havde at vælge imellem. På den reelle valgmængde (65) er `DAY_RESULT` **12,3 %**, og syv af otte dagsregler har udløst. v3 gjorde præcis det, den satte sig for. **Men spejlbilledet holder:** af 168 visninger er `DAY_TOP` + `CONTRARIAN` **86,9 %** — de to højeste grundvægte efter `MILESTONE`, og to af de tre, der fan-outer. Ankerproblemet er ikke fjernet, det er flyttet opad; ført videre som `A58`, fordi det er en produktvurdering og ikke en måling. **`A35` kunne IKKE besvares: fire kampdage af de ti.** Udløserens to halvdele løber ikke i samme takt — de to uger var gået, men kampdage kommer ~2–3 om ugen. Aflæsningen målte til gengæld tærsklens svaghed: spændet 38→55 flytter andelen 16 point, fordi 75 af 100 kort ligger uden for enhver tærskels rækkevidde. Og andelen ser kun rigtig ud på grund af nævneren: 50,0 % af alle bruger-dage, men **76,9 %** af valgmængden, altså over rækkens eget "over 70 % ⇒ for lav". **Morgenens scorerum-aflæsning holdt punkt for punkt** — `COLLECTIVE_MISS` på præcis 44 i alle fire forekomster, `DAY_RESULT` aldrig over 28, `SO_CLOSE` på gulvet 38. **To fund, ingen bad om:** halvdelen af alle v3-dagskort havde et vindue på nul minutter og kunne aldrig nås (`G142`), og `STREAK_STATUS` har aldrig udløst (`G143`). Backloggen er 31 → 35, og Tier 1 og Tier 2 er fyldt igen af leverancen selv. Ingen kode, ingen SQL. Aflæsningen: [`reviews/story-engine-v3-aflaesning-2026-08-21.md`](./reviews/story-engine-v3-aflaesning-2026-08-21.md). Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (otteogfyrretyvende kørsel):** (🔶 **`A33` og `A35` er ÉT spørgsmål — scorerummet er regnet ud, og aflæsningen er bestilt.** Backloggen er 31 → 31: begge rækker bliver stående, til opslaget er kørt. **Fundet, der ikke krævede data:** en kandidat scorer `grundvægt + størrelse + nærhed`, nærheden er 20 for hovedpersonen, og **tærsklen 45 ligger i hullet mellem `DAY_RESULT`s loft (8 + 12 + 20 = 40) og de fire tungeste reglers gulv (48, 50, 52, 54)**. Andelen af kampdage med ulæst-markering ER derfor andelen, hvor en anden regel end dagens facit vandt — hvilket er `A33`s spørgsmål. **Følgen: tærsklen er for hovedpersonen næsten ikke en tærskel.** Fem af otte regler afgøres af deres grundvægt alene; kun `SO_CLOSE` (kræver 7 størrelsespoint) og `COLLECTIVE_MISS` (kræver 1) afgøres af tallet. Den binder derimod i FAN-OUT-laget, hvor fem af ni (regel × nærhed)-kombinationer afgøres af 45. **Det ændrer, hvad beslutningen kan handle om:** ligger andelen uden for målet på 40–60 %, er svaret sjældent "flyt tærsklen fem point" — 41–47 flytter for hovedpersonen ingenting — men enten at flytte den forbi et gulv eller at flytte GRUNDVÆGTEN for den regel, der fylder for meget. **Den anden halvdel kræver rækker og er bestilt som ét paste** (`sql/story_engine_v3_measure.sql`, read-only, kun temporære tabeller — samme form som `sql/checks/`, og `A32` står ved magt). Filen svarer i én tabel: om udløseren holder, `A35`s andel globalt og pr. aktiv bruger med handlingsgrænserne regnet ud, andelen ved **otte** tærskler, fordelingen af `news_value` skåret ved reglernes gulve, og `A33`s variation målt tre steder. **Vis-bar er regnet for v3** (`loadDayCard` + 48 timer) og ikke lånt fra analytics-tavlens karrusel-begreb fra v2 — at tavlen stadig bruger v2-reglen for `period = 'day'` er noteret i indbakken frem for rettet her. Efterprøvet mod produktionsskemaet på PostgreSQL 16.13 med et syntetisk datasæt, hvis facit var regnet i hånden. Ingen migrering, ingen frontend-ændring. Regnestykket: [`reviews/story-engine-v3-scorerum-2026-08-21.md`](./reviews/story-engine-v3-scorerum-2026-08-21.md). Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (syvogfyrretyvende kørsel):** (✅ **`A44` er AFGJORT — navnene bliver, og fuld-stillingen åbner, hvor du selv står.** Backloggen er 31 → 31: `A44` er slettet, og indbakkens ene linje er blevet `G139`. Spørgsmålet var, om den globale rating skal vise fulde visningsnavne til brugere, man ikke deler noget med. **Ejerens svar er nej til maskering:** navnene er selvvalgte, case-insensitivt unikke pseudonymer, privatlivspolitikken siger det allerede højt (*"Brugernavnet er synligt for alle andre brugere"*), og en titel, der hedder Månedens Champion, skal kunne navngive sin vinder — en tavle, hvor de fleste rækker er tal, er ikke en afgrænsning af tavlen, men en afskaffelse af dét, den findes for. **Aflæsningen fandt til gengæld to ting, rækken ikke selv vidste.** Produktet har en **ufravigelig regel i modsat retning** — *en historie må kun nævne personer, modtageren deler konkurrence med* (juli 2026), stærk nok til at forkaste `rating_history.rnk` som rivalkilde i karriereprofilen — og de globale tavler er den BEVIDSTE undtagelse; spændingen står nu skrevet i beslutningsloggen frem for at være uskreven. Og fladerne er **fire og ikke to**: Rating-fanen (hele listen, upagineret), Championshippets tre kort, hvert navn som tryk-flade til karriereprofilen (`career_profile()`s K1), og Hjem, som henter hele tavlen for at vise ét tal — den sidste blev `G139` i Tier 5. **Det, der blev bygget, er ikke privatliv, men navigation:** kortet viser top 5, så er du nr. 25, er modalen den eneste vej til din egen række, og den startede altid på side 1. Modalen åbner nu på **din egen side**, med genvejene "Top 20" og "Min placering" øverst — deaktiveret frem for skjult, så rækken ikke skifter højde, mens man bladrer. 🔴 **Siden regnes af rækkens INDEKS og ikke af `rank`:** `assignRanks` giver ægte lige spillere den samme placering, så en delt placering hen over sidegrænsen ville — regnet på `rank` — sende brugeren til den forrige side, hvor hendes egen række ikke står. Reglen bor i `pageOfUser()` med sin egen test. **Hvad beslutningen IKKE dækker:** navnelisten er fortsat hentbar for enhver med en konto — kun en server-side-afgrænsning kunne ændre det, og det er præcis den vej, `A43` fravalgte. Ingen SQL, ingen migrering, 13 nye tests (1534 i alt). **Kortet er urørt:** det viser fortsat top 5 og kun top 5 — ingen egen række hægtes på, og der står ingen "du er nr. 25"-linje. Kortet svarer på *hvem fører*, modalen på *hvor står jeg*, og ejeren bekræftede grænsen samme dag; den står nu i `DOCUMENTATION.md` §7, så den ikke kan drive. ✅ **Efterprøvet i en rigtig browser** med harnessen kørt på 45 spillere (midlertidigt, ikke committet — de fire skærmbilleder skydes fra de seks): modalen åbnede på side 2 med egen række markeret og "Min placering" slukket, "Top 20" gav side 1 med den modsatte tilstand, og kortet viste fem rækker uden nr. 25. `A42`s udløser er **ikke** sprunget — dette var en efterprøvning og ikke en fejl, der kun kunne ses i en browser. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (seksogfyrretyvende kørsel):** (✅ **`G138` er BYGGET: Admin → Drift kender nu det job, ingen har planlagt.** Backloggen er 32 → 31, og **Tier 2 er tomt** — rækken blev skrevet og lukket samme dag. Kortet for `story-engine` stod som et *uventet* job og gættede på to forklaringer, der begge er forkerte: rækken skrives af **matches-triggeren** (`sql/rating_trigger_optimization.sql`) og ikke af cron-job.org, hvilket også er grunden til, at dens resumé hverken har `host` eller `authVia`. **Rettelsen er en kategori og ikke en tekst:** `src/lib/ops.js` har fået `TRIGGER_JOBS` ved siden af `BASE_JOBS` og de turnerings-udledte jobs, og `story-engine` står nu i den **forventede** liste. Det er hele forskellen — et forventet job vises også, når det aldrig har skrevet en række, og netop dét var `A38`s tilstand, hvor v3's dagsmotor aldrig havde skrevet noget i produktion, uden at stilheden kunne aflæses noget sted. **Tavshed måles fortsat ikke, men af den rigtige grund:** `stilhedMs` er `null`, fordi triggeren fyrer på en hændelse og ikke efter et ur — en uge uden resultater og en trigger, der er holdt op med at fyre, ser ens ud. Fejlserie, fejlrate og varighed måles uændret, mens dommen mod cron-job.orgs 30-sekunders vindue (`G114`) er fjernet: den grænse tilhører en kalder, jobbet ikke har. **`unexpected` er igen kun det, ordet siger** — en række, ingen kan gøre rede for. **Læren:** *en tekst, der gætter, gætter ud fra den kategori, den er havnet i, så det er kategorien, der skal rettes.* Fire nye tests; `docs/CRON.md`, `DOCUMENTATION.md` §17 og `features/story-engine-v3.md` §13 er rettet med. **Intet skal køres i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (femogfyrretyvende kørsel):** (✅ **`A46` er LUKKET: alle ni cron-jobs står på `prediction-champ.vercel.app`.** Backloggen er 32 → 32 — `A46` er slettet, og indbakkens ene linje er blevet `G138` — og `docs/CRON.md` har ikke længere en pladsholder i kaldkolonnen. Ejeren læste de ni jobkort i Admin → Drift samme dag som bestillingen blev skrevet, og svaret var entydigt ni gange. Alle ni bar `authVia: header`, altså cron-jobbet selv og ikke et manuelt "Hent nu" — så værdien er den, jobbet ER sat op med. **Det, aflæsningen egentlig fortalte, er en tilstand og ikke ni adresser:** domæneflytningen (`I10`) har ikke rørt cron-laget. **Det udløser ingen handling** — `/api/` er med vilje undtaget fra redirectet, og undtagelsen er bevist (bevis 3b), så jobbene rammer funktionen uanset hvilken adresse de er sat op med; prisen er en ikke-kanonisk origin, ikke et brud, og ni redigeringer i en tredjeparts brugerflade for at fjerne et kosmetisk vilkår er ni chancer for at skrive en URL forkert. Ført ind i `DOMAENE.md`s register, hvor rækken stod med `?`. **De fire sidste blev ikke udfyldt ved at slutte fra de fem første:** halvvejs gennem aflæsningen stod fem ens værdier, og de sidste fire blev læst hver for sig alligevel — hvert cron-job har sin egen URL, flytningen tages ét job ad gangen, og fem ens værdier er derfor et mønster og ikke en regel. Registerets eget forord er den bindende regel: *et register, der er forkert, er værre end intet, fordi man tror på det.* **Kolonnen er nu en påstand og ikke en pladsholder**, hvilket er en skærpelse: `<app>` kunne ikke blive forkert, `prediction-champ.vercel.app` kan. **Fundet undervejs og triageret til `G138` (Tier 2) frem for rettet:** Admin → Drift kalder `story-engine` et *uventet* job og gætter på to forklaringer, der begge er forkerte — rækken skrives af matches-triggeren og ikke af et cron-job, hvilket også er grunden til, at dens resumé hverken har `host` eller `authVia`. Fejlen er en antagelse i `mergeJobHealth()`: alt uden for `expectedJobs()` får `unexpected: true`, og teksten er formet efter `G44`s forældede `sync-matches`-rækker. Intet skal køres i Supabase. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (fireogfyrretyvende kørsel):** (✅ **`A46`: værtsnavnet er nu en RÆKKE på jobkortet og ikke en linje i et JSON-dump.** Backloggen er 32 → 32, og det er rækkens egen pointe: `A46`s sidste skridt er at skrive ni værtsnavne ind i `CRON.md`s kaldkolonne, og de ni værdier findes kun i produktion. `A32` står ved magt — den aflæsning er ejerens — så rækken lukkes ikke; den er flyttet **Tier 6 → Tier 1**, fordi udløseren ER sprunget: alle ni jobs har kørt siden `setHost()` blev udrullet 13. august, og det langsomste skema er hver 12. time. **Det, der kunne leveres, var bestillingen — og halvdelen af den manglede.** 13. august-indslaget sluttede med, at Admin → Drift viste værtsnavnet *"uden en eneste UI-ændring, fordi jobkortets «Seneste resumé» i forvejen dumper detaljen som JSON"*. Det var sandt og alligevel det halve: aflæsningen er **ni** jobs, og et rå JSON-resumé bag en `<details>` koster en udfoldning og en gennemlæsning hver gang — bestillingen var stadig "åbn ni ting og skriv ni værdier af", bare inde i appen. `host` er derfor løftet ud som signalrækken **"Kaldt på"** nederst på hvert jobkort, ved siden af varighederne. Rækken vises kun, når værtsnavnet findes — samme regel som fejlraten (`G115`) og varigheden (`G114`): en kørsel fra før `setHost()` har ingen adresse, og en umålt værdi må ikke kunne forveksles med en. **Rækken bliver stående, når `<app>` er udfyldt:** `/api/` er med vilje undtaget fra domæne-redirectet, så et job på den gamle adresse svarer 200 og tier, og flytningen tages ét job ad gangen. **Læren, der er værd at tage med:** `A11`-mønsteret (flyt spørgsmålet ind i appens egne data) er nu brugt fire gange, og skridtet, de tre første sprang over, er, at **at gemme svaret ikke er det samme som at kunne læse det** — er svaret per-job, hører det til som et felt på kortet og ikke som en linje i et dump. Intet skal køres i Supabase. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (treogfyrretyvende kørsel):** (✅ **Tier 2, 4 og 5 kørt tomme: fire gæld-rækker leveret, og oprettelsen af en konkurrence blev ÉN skrivning.** Backloggen er 36 → 33; Tier 2–5 er alle tomme, og Tier 1 bærer én bestilling. `G136`: `schema-export.yml`s `krop()` klipper nu også `\restrict`/`\unrestrict`-linjerne af før kropssammenligningen, så pg_dump 17's tilfældige token ikke længere kan åbne en falsk skema-drift-PR — efterprøvet mod det rigtige dump med et simuleret token-skifte. `G133`: ny migrering `#73 create_competition.sql` — en `security definer`-RPC efter `create_group()`s mønster, der skriver konkurrencen, opretterens deltagerrække og kampene i samme transaktion; klienten kalder den i alle fem modes, `created_by` sendes ikke længere med, og `sql/tests/create_competition.sql` fremkalder begge vinduer i CI med den gamle vejs halve konkurrence som negativ kontrol. `G131`: analytics-testens ⓘ-liste UDLEDES nu af JSX'en og kræves lig måle-ordbogen begge veje — den håndskrevne liste manglede allerede to id'er (`story_viewable`, `invite_funnel`). `G137`: rundemodalens `groupIntoRounds(ms)` fik sit manglende navne-argument, så samtidige kampe sorterer som på Hjem og Tip. 1517 tests (3 nye), lint på loftet, grønt build. ✅ **`#73` er kørt i produktion og staging samme dag (ejeren), FØR mergen** — `B41`-bestillingen er lukket igen, backloggen er 33 → 32, og Tier 1–5 er alle tomme. Se [`DECISIONS.md`](./DECISIONS.md), [`CHANGELOG.md`](./CHANGELOG.md) og [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (toogfyrretyvende kørsel):** (🧹 **Backloggens indbakke er tømt: to gæld-rækker, ingen bygget.** Listen er 34 → 36. `G136` (Tier 2): skema-eksportens egen dato-garanti er blevet falsk — `#251`s eksport samme dag viste, at to kørsler i træk mod en UÆNDRET database gav en diff, fordi pg_dump 17 skriver et nyt tilfældigt `\restrict`-token i hvert dump, og tokenet ligger inde i den "krop", `schema-export.yml` sammenligner. På standardgrenen ville hver ugentlig cron-kørsel nu åbne en falsk "skema-drift"-pull request — den nøjagtige alarm, `G112` blev bygget for at undgå. `G137` (Tier 5): stillingens rundemodal kan vise en anden rækkefølge end Hjem og Tip — `competitionState.js` kalder `groupIntoRounds(ms)` uden navne-funktion, så kampe med identisk kickoff sorterer efter holdets uuid ét sted og efter navn de øvrige. Begge fundet undervejs i `B39`-arbejdet og noteret frem for rettet med det samme — ingen af dem hørte til den opgave. Ingen kodeændringer, intet at køre i Supabase. Se [`BACKLOG.md`](./BACKLOG.md).)

**Forrige (enogfyrretyvende kørsel):** (✅ **Det korte holdnavn gælder nu HELE appen — ejerens beslutning oven på `#248` i drift.** Gårsdagens afgrænsning til tip-rækkerne er **ophævet**: når Tip siger "Hull City – Man United" og Hjem, Admin og stillingen siger "Hull City AFC – Manchester United FC" om den samme kamp, er kælenavnet ikke et kort navn, men et andet navn. **Præmissen holdt heller ikke** — "alle andre visninger har plads til det fulde navn" var forkert netop på Hjem, hvis rundeliste **trunkerer** med ellipse, altså gør det, tip-rækken nægter at gøre. **Leverancen er, at reglen bor ét sted:** ny `src/lib/teams.js` med `teamLabel()`/`teamLabelMap()`/`TEAM_SELECT`, og otte visninger flyttet over (Hjems to, stillingens rundemodal, opret-flowets to, begge Admin-paneler og tip-skærmen selv). `TEAM_SELECT` er `*` og navngiver med vilje ingen kolonner — en navngiven kolonne, der ikke findes, er en 400, og en staging-database uden `#72` ville da vise hvid skærm; det er nu en påstand frem for en vane. Favorithold-listen sorterer efter det, der VISES ('Atleti' skal ikke stå midt i C'erne). Story Engine skriver kort navn i historierne, vogtet af en **kilde-læsende** vagt (`migration_syntax.test.js` vagt 4) af samme grund som `G89`s vagt 3: fixturen fyrer tre af otte dagsregler, og COLLECTIVE_MISS er ikke en af dem. 1514 tests, grønt build, alle 19 CI-SQL-tests kørt mod rigtig PostgreSQL, og ændringen SET i den kørende app med kunstigt lange demo-navne. Skærmbillederne er uændrede. 🔴 **Ejeren skal gen-køre `#47 story_engine_v3.sql` i Supabase** (idempotent, rører ingen rækker; historiernes tekst er frossen, så kort navn gælder fremad). Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (fyrretyvende kørsel):** (✅ **`B39` er BYGGET — kamprækkerne kan vise leverandørens korte holdnavn, og backloggens Tier 1–3 er alle tomme.** Tredje `B39`-kørsel på én dag: bestilt, aflæst, bygget. Ny migrering `#72 teams_short_name.sql` (additiv kolonne; `teams.name` er urørt og forbliver holdmatchens nøgle — korte navne ville skabe netop de kollisioner, `ambiguousTeamNames()` fælder). Begge providere bærer `shortName` i den normaliserede form (Sportmonks med vilje altid null — `short_code` er et badge, ikke et visningsnavn), og syncens holdskrivning er udskilt som den rene, testede `planTeamWrites()` med guarden `harShortName`: feltet skrives først, når `teams`-læsningen viser, at kolonnen findes, så **migrering og deploy er uafhængige begge veje** (ingen UDRULNING-fil — hver mellemtilstand er gyldig). **Valget pr. skærm: KUN tip-rækkerne** viser `short_name || name` — stedet, pladsen er trang, og stedet, ejeren meldte; Hjem, stillinger og Story Engine beholder de fulde navne, og kælenavns-tonen ("Barça", "Atleti") er accepteret med åbne øjne ét sted, hvor den kan revideres billigt (se [`DECISIONS.md`](./DECISIONS.md)). Skærmbillederne er uændrede (demo-datene har intet kort navn og falder tilbage). 1503 tests, lint på loftet, grønt build. 🔴 **Ejeren skal køre `#72` i Supabase** — sikker når som helst; feltet udfyldes af næste sync pr. turnering, aflæseligt som `shortNamesSet` i Admin → Drift. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (niogtredivte kørsel):** (✅ **`B39` er aflæst samme dag som bestilt — Tier 1 er tom igen, og kolonnen kan bygges.** Ejeren kørte bestillingen fra kørslen før (efter én rettelse: opslaget hentede tokenen fra en miljøvariabel, der kun findes i Vercel, og PS 5.1's `Invoke-WebRequest` fejler da med en intetsigende `NullReference` — det spørger nu med `Read-Host` og nægter at køre med en tom). Alle fire svar var de gode, og de står i [`docs/reviews/football-data-shortname-aflaesning-2026-08-20.md`](./reviews/football-data-shortname-aflaesning-2026-08-20.md): `shortName` findes for alle 78 hold i PD/PL/SA/BL1, er aldrig tomt, har ingen dubletter — og er markant pænere for netop de lange spanske navne, rækken kom af ("Real Racing Club de Santander" → "Santander", "RCD Espanyol de Barcelona" → "Espanyol"). **Fundet, bestillingen ikke forudså:** leverandøren sender kælenavne ("Barça", "Atleti", "M'gladbach", "HSV"), ikke afkortninger — en tone og ikke kun en længde, og dét skal skærm-valget tage stilling til, når kolonnen bygges; ene skævhed er "Brighton Hove" (mistet "&"). `B39` er flyttet Tier 1 → Tier 3 i [`BACKLOG.md`](./BACKLOG.md) — næste skridt er kode igen (additiv kolonne, `teams.name` forbliver nøgle; Superligaen/Scotland viser fortsat `name`). CL svarede 404, så `B28`s udløser er efterprøvet og stadig ikke indtruffet. Ingen kodeændringer, intet at køre i Supabase. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (otteogtredivte kørsel):** (✅ **Backloggens Tier 1 er kørt — for tredje gang uden en produktionsaflæsning, men denne gang med bestillingen som leverance.** Tieret bar én række, `B39` (kort holdnavn til visningen), hvis næste skridt er et kald til football-data.org, som pr. konstruktion ikke kan laves fra repoet — og det blev efterprøvet frem for antaget: udviklingsmiljøets proxy afviser CONNECT mod `api.football-data.org` med 403, og `FOOTBALLDATA_TOKEN` findes ikke i miljøet. `A32` står ved magt: aflæsningen er ejerens; det, en session kan gøre billigere, er **bestillingen** — og den lå ikke klar, for rækken henviste til kickoff-opslaget, som udtrækker `utcDate`/`status` og ikke kan svare på ét af `B39`s tre spørgsmål. Bestillingen ligger nu som [`docs/reviews/football-data-shortname-aflaesning-2026-08-20.md`](./reviews/football-data-shortname-aflaesning-2026-08-20.md): ét PowerShell-opslag over de fire aflæselige turneringer (CL tåler at fejle — det er `B28`s udløser), som svarer på de tre spørgsmål plus en fjerde billig (dublet-`shortName` inden for samme turnering), med rimelighedstjek på kamp- og holdantal, en tabel over hvad hvert udfald afgør, og et Svar-afsnit, udskriften skrives ind i. Opslaget rammer samme endpoint, syncen henter, ikke `/teams` — kolonnen skal udfyldes af det svar, syncen faktisk får. `B39` bliver stående i Tier 1, til svaret er skrevet ind. Ingen kodeændringer, intet at køre i Supabase. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (syvogtredivte kørsel):** (✅ **Indstillinger-skærmen findes nu, og push kan for første gang slås FRA i appen — `B40`.** Ejerens ønske: notifikationer skal kunne styres løbende. Nyt tandhjul i topbaren ved siden af ⓘ (admin-knappen afgav ikonet og bærer nu skruenøglen — kun admins mærker det) åbner `SettingsScreen.jsx` med to kort: **Notifikationer** — én samlet kontakt pr. enhed, drevet af den nye hook `usePushSetting` oven på de eksisterende `enablePush`/`disablePush`; kontakten er låst med en forklaring ved manglende browser-understøttelse, manglende hjemmeskærms-installation (iOS) og blokeret tilladelse — og **Profil** med "Skift brugernavn", flyttet fra karriereprofilen, præcis som `B29`-kommentaren forudsagde ("bliver der en indstillingsskærm, er dette dens første beboer"). **"Fra" sætter samme "nej tak"-flag som opt-in-kortets kryds**, så Hjem ikke straks beder brugeren fortryde; privatlivspolitikkens sætning om flaget er udvidet tilsvarende (G71-koblingen holdt: `legal.test.js` vogtede den). Alt-eller-intet uden databaseændringer — udsendelsen har ingen præference-model, og tilstanden er fortsat alene `push_subscriptions`-rækken. Skærmbillederne er taget om (fjerde knap i headeren). 1492 tests (6 nye), lint på loftet, grønt build. **Intet at køre i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (seksogtredivte kørsel):** (🔴 **Sæson-gaten holdt en færdigspillet periode åben — nu slipper den, når slutdatoen er passeret.** Ejerens skærmbillede samme aften: "TEST – Superliga", fire runder i august, 22/22 spillet, ingen vinder. `competition_status` regnede `time_range` som voksende alene på mode — og en periode midt i Superliga-sæsonen ville dermed have ventet på HELE sæsonen, i praksis til næste sommer, med pokal, vinderlinje, fejring og `COMP_*`-milepæle tilbageholdt. **Lempelsen er snæver og hviler på en strukturel umulighed, ikke en karens:** efterfyldningen kan kun optage kampe med kickoff inden for periodens eget vindue, og dens regel 3 gør en kamp i et passeret vindue umulig at tilføje — så dagen efter `end_date` er der intet at vente på. `<` og ikke `<=` (på selve slutdatoen kan dagens kampe stadig komme til), en periode uden slutdato gates som før, `full_season`/`team` er urørte, og resultat-kravet løsnes ikke. Efterprøvet med påstand 12–12e i `sql/tests/competition_status.sql` inkl. modprøven af, at `full_season` på de samme kampe stadig gates, og mutationsprøven med den gamle view-definition vælter testen. 🔴 **`#41 season_end.sql` skal GEN-KØRES i Supabase (produktion OG staging), og skema-eksporten bagefter** — kørslen er uafhængig af deployet, og indtil den er sket, gælder den gamle gate. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (femogtredivte kørsel):** (🔴 **"Ikke bekræftet"-markøren er fjernet helt — ejerens beslutning, truffet oven på `G135` samme dag.** Skærmbilledet fra runden 15.–21. september afgjorde det: 16.00-kampene stod som ubekræftede i en runde, der tydeligt HAVDE været igennem tv-flytninger (fredag 21.00, lørdag 13.30 og 18.30) — selv den strammede dominans-regel ramte netop de mest bekræftede kampe, og et gæt, der har gjort det én gang, læses ikke som information igen. **Reglen er nu den simple, ejeren selv formulerede:** har kampen et klokkeslæt, vises det uden forbehold — flytter leverandøren det, retter syncen det selv ved næste kørsel; har den ingen tid, viser `kickoff_tbd` allerede kun datoen, og det siger sig selv, at den ikke er fastlagt. **Alt maskineriet er revet ned, ikke kun visningen:** kolonnerne `kickoff_uncertain`/`kickoff_prev_at`, triggeren, `refresh_kickoff_uncertain()`, syncens RPC-kald, `showUncertain()`/horisonten i `scoring.js`, `~`-præfikset og tegnforklaringen i `time.js`, `ALLE UBEKRAEFTEDE`-halvdelen af `kickoff_coverage`-kontrollen, heartbeat-aflæsningen, den 449-linjers SQL-test og dens CI-trin. **Prisen er valgt med åbne øjne og står i DECISIONS:** kontrollen er igen blind for PL/PD/SA's opdigtede tider — den fejl retter sig selv via syncen, og alternativet var en markør, der løj den anden vej. `kickoff_tbd`, låsen og deadline-logikken er urørt. 🔴 **`#71 matches_kickoff_uncertain_drop.sql` skal køres i Supabase (produktion OG staging) EFTER mergen — rækkefølgen er omvendt af `#49`s** — og skema-eksporten bagefter; `#49`/`#70` er nu 🛑 og må aldrig gen-køres. `B39` (kort holdnavn) står stadig i Tier 1. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (fireogtredivte kørsel):** (🔴 **"Tid ikke bekræftet" stod på de kampe, der var mest bekræftede — `G135`.** En bruger sendte to skærmbilleder: tre ÆGTE Premier League-kampe lør. 22. august, en runde der lå **fire dage ude**, stod med `~ tid ikke bekræftet`, og to Primera División-kampe med "(ikke bekræftet)". Og brugerens eget følgespørgsmål — "kan det være tilsvarende i andre ligaer?" — var det rigtige: Serie A har samme form, og Superligaen, Scotland og Champions League er latente. **Årsagen står skrevet i aflæsningen fra 7. august, som begrundelsen for at forkaste kandidat A:** efterårspladsholderen ER turneringens typiske anspilstid — 14:00 UTC er 15.00 i London, altså blackout-slottet. Kandidat B blev valgt for at undgå den fælde, men B markerer også på klokkeslæt og *lærer* bare tabellen frem for at hardkode den. **B undgik A's kalibreringsproblem, ikke A's kollisionsproblem.** Værre endnu: markeringen falder først bort, når kampens EGEN tid flytter sig, og en ægte lørdag-15.00-kamp flytter sig aldrig — så netop de sikreste kampe bar markøren permanent frem til kampstart. **Rettelsen er dominans i runden:** et indlært klokkeslæt markerer kun, hvis det bærer over halvdelen af rundens ikke-spillede kampe. Det er den NEGATIVE form af en formodning, aflæsningen forkastede — og sikker netop hvor den positive ikke var, fordi den kun kan FJERNE markeringer. **Det største valg stod ikke i opgaven:** horisonten på ti dage ligger i `src/lib/scoring.js` og ikke i SQL, fordi `G84`s kontrol ellers ville blive stum kode — og beviset på, at snittet lå rigtigt, er at `sql/checks/kickoff_coverage.sql` og dens test kunne stå **fuldstændig uændret**. Den første udgave af rettelsen lagde horisonten i SQL med et injicerbart `p_now` og blev forkastet af to grunde, hvoraf den anden er værd at huske: et default-argument gør funktionen til en **overload**, og PostgREST kan da ikke binde syncens kald. **Fejlen blev reproduceret før den blev rettet**, og seks mutationer (tre SQL, tre JS) blev alle fanget. ✅ **`sql/matches_kickoff_uncertain_round.sql` (`#70`) er kørt i staging og produktion 17. august 2026.** Adfærdsændring ved kørsel: markeringer **fjernes**, ingen tilføjes. **Endnu ikke gjort:** kort holdnavn til visning er nu `B39` i backloggens **Tier 1**, bevidst ubygget indtil `shortName` er aflæst hos leverandøren — egress er blokeret fra udviklingsmiljøet, og en uprøvet dokumentationslæsning var netop fejlen 2. august. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (treogtredivte kørsel):** (🔴 **`A39` er afgjort: kampdagen er blevet personlig.** Dagskortet udgives nu, når alle kampe på dagen i **modtagerens egne konkurrencer** har et resultat — ikke når hver eneste kamp i alle syv turneringer har det. En Superliga-bruger, hvis sidste kamp var slut kl. 16, ventede før på La Ligas kamp kl. 22:45, og ved en udsat kamp ventede hun for evigt. **Afgrænsningen er alle dagens kampe i mine konkurrencer, ikke kun dem jeg har tippet** — kortet bærer stillingen og mini'en, og de flytter sig, når modstanderne får point. **Et udgivet kort fryses, hvis dagen vokser bagefter** (ny konkurrence oprettet om aftenen, ny deltager i min liga), målt på `payload.day_scope_matches`. **Slet-og-indsæt blev til et forlig:** motoren kaldes nu fire-fem gange på en stor lørdag frem for én, og uden det ville en afvisning genopstå og ulæst-prikken nulstilles flere gange på en aften — det lukker den hyppige halvdel af `G129`. Sletningen drives nu af det, der skal skrives, så invarianten *en tidlig udgang må aldrig efterlade mindre, end den fandt* er blevet **strukturel frem for argumenteret**. ⚠️ **Gevinsten gælder ikke på rundens sidste kampdag** — den udgang er stadig global, fordi rundekortet er pr. runde og ikke pr. bruger. ⚠️ **Første bagstopper-kørsel efter udrulningen kan lukke op til 20 dages huller på én gang**; et spring i `stories`-rækker er ønsket og ikke en fejl. **Kontrollen `day_card_coverage` tæller nu brugere frem for dage** — ellers ville dens værste alarm lyse på hver eneste delvist spillede dag — og den fik for første gang en aftager i `job-heartbeat.yml`; indtil nu blev `A39`s egen udløser aflæst af ingen. 🔴 **TRE MIGRERINGER I BINDENDE RÆKKEFØLGE: `#68 story_engine_personal_day.sql` → gen-kør `#47 story_engine_v3.sql` → gen-kør `#5 rating_trigger_optimization.sql`.** Hvert mellemtrin er en gyldig tilstand, og frontenden røres ikke — derfor er der ingen `UDRULNING-A39.md`.)

**Forrige (toogtredivte kørsel):** (🔴 **Ejeren spurgte "bør gabet være synligt? Kan ikke se det" — og svaret afslørede en fejl i selve aktivitets-målingen.** **Den første halvdel: rammen var forkert.** "Kiggede uden at spille" (kom forbi − spillede) forudsætter, at alle spillere også er besøgende, og det holder ikke: på produktionsdata stod *kom forbi* LAVERE end *spillede* i tre af fire runder (19/15, 20/18, 17/12), så feltet viste **nul i hver eneste runde**, fordi en `Math.max(0, …)` klampede differencen. Husets egen fejlklasse: **et signal gjort til et tavst nul.** Feltet, dets metrik og den afledte værdi er fjernet, og en test vogter, at rammen ikke sniger sig ind igen. **Den anden halvdel, som er den vigtige: `user_activity_days` var et gulv for HELE produktet.** `touchActivity()` blev kaldt ét sted — `completeAuth` — som kun kører ved BOOT, og **en PWA på en telefon bootes sjældent:** iOS holder den i live i dagevis, så en bruger kunne åbne appen hver dag i en uge og efterlade ÉN aktivitetsdag. Pinget fyrer nu ved app-start OG ved hver `visibilitychange`, med den eksisterende throttle på 1×/time som spærre. **Rækkevidden er større end den sektion, der fandt fejlen:** DAU/WAU/MAU, stickiness, retention, `groups_with_active_member`, liga-diagnosens aktive medlemmer og besøgstallet pr. runde har alle været for lave. ⚠️ **Tallene hopper op ved udrulningen, og det er ikke vækst** — en serie hen over datoen sammenligner to målinger, samme forbehold som `A21`s skift af enhed. **De to mål kan alligevel aldrig trækkes fra hinanden**, heller ikke efter rettelsen: *spillede runden* hører til den runde, kampene ligger i, mens *kom forbi* er kalenderugen, og tips kan gives i forvejen. Gab-feltet ville altså have været forkert selv med et perfekt ping. **Læren er `G73`s og `G115`s en tredje gang:** en klampning gør en måling, der ikke passer, til en måling, der ser rigtig ud — og den kom kun for dagen, fordi tallet blev sat på skærmen og aflæst af et menneske. 1488 tests, lint på loftet, grønt build. **Intet at køre i Supabase.** Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (tredivte kørsel):** (✅ **Analytics kan for første gang vise en UDVIKLING** — `B38`. Alt på fanen var indtil nu et vindue på 7/30/90 dage, som svarer på hvordan det står til, men aldrig på om det går op eller ned; de to søjlerækker, "Produktets sundhed" havde, målte oven i købet ISO-uger (mandag) og ikke runder. **Ny sektion, `admin_analytics_rounds`:** én søjle pr. spillerunde, ældst til venstre, med sin egen vælger (12/26/52 runder) — den eneste sektion, dagsvinduet ikke gælder for, fordi 7/30/90 dage skærer midt igennem en runde. **Runden er produktets egen enhed:** `round_key` går tirsdag til mandag i dansk tid og er global på tværs af turneringer, mens `date_trunc('week', …)` er mandag — forskudt et døgn, altså en anden spand end den, kampen tæller med i på Tip, i Championship og i notifikationerne. **To målinger ved siden af hinanden, og gabet er oplysningen:** *spillede* (havde en deadline og afgav mindst ét tip) mod *kom forbi* (havde blot appen åben i rundens uge) — samme skelnen som `active_groups` vs. `groups_with_active_member` og samme lære som liga-diagnosens bredde. **Den oplagte implementering er den forkerte:** at tælle `predictions` direkte ville tage tips med på kampe uden en deadline i nogen konkurrence og kunne give en deltagelse over 100 %; begge tal læses derfor af `analytics_completion_facts`, samme kilde som North Star. 🔴 **Runden i gang bærer intet tal:** `is_open` betyder "ikke alle kampe er låst endnu", altså at tallene stadig kan vokse, så runden vises som en stiplet søjle uden at bære hverken overskrift, retning eller gennemsnit — et delvist tal mod et helt giver et fald hver gang, uanset hvad brugerne gør (`G73`/`G115`s regel). **Nye spillere tælles på debut over hele historikken**, ikke inden for vinduet, og **`visitors = null` betyder umålt**, aldrig nul. **Verificeret på PostgreSQL 16.13 mod det rigtige skema** — ti påstande, mutations-testet med otte fejl, der faktisk kan ske, alle otte fanget. 1479 tests (17 nye), lint på loftet, grønt build. ✅ **`sql/analytics_dashboard.sql` (#17) er gen-kørt i produktionen 16. august 2026** (ejeren), før frontend-mergen — rækkefølgen er ægte den ene vej: en ny klient mod en database uden RPC'en ville få `404` og en fejlende runde-sektion. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (niogtyvende kørsel):** (✅ **Backloggens Tier 5 er kørt tom** — `B37` — og dermed er Tier 1, 2 og 5 alle tømt samme dag; kun Tier 6 (venter på en udløser) og Tier 7 (udadvendt) bærer rækker. Listen er 33 → 32. **Rækken var, at instrumenteringen var på plads og aflæsningen manglede:** `I22` og `I25` skriver begge deres delings-hændelse, begge navne står i kataloget — og intet panel læste dem. **Halvdelen krævede ingen SQL:** `admin_analytics_engagement` har hele tiden svaret `events` som en generisk optælling pr. hændelsesnavn, så begge totaler lå allerede i svaret og manglede kun et sted at stå. **Opdelingen krævede derimod SQL**, fordi den bor i `metadata.from` — `shares` skiller nu rundekort (v3's frames OG de historiske rækker helt uden `from`), dagskort, milepæl og stilling ad. Ændringen står i `sql/analytics_dashboard.sql` (#17) og ikke i en ny migrering: filen er registreret som *"sikker og forventet at blive gen-kørt"*, og en kopi af to lange RPC'er ville være netop den landmine, `sql/README.md` advarer mod ved `generate_stories()`. **Rækken afdækkede en forurening, ingen havde ledt efter:** karriereprofilens del-knap skriver milepælsnøglen som `rule`, og `MONTH_CHAMP` er bevidst BÅDE en milepælsnøgle og en story-regel, så karriereprofilens delinger blev talt med i regeltabellens `Delt`-kolonne. Nu filtreret fra og talt som sin egen flade. **Umålt må ikke ligne nul:** er RPC'en ikke gen-kørt, skriver panelet "ikke målt endnu" frem for fire nuller (`G115`s regel), og et tomt vindue svarer bevidst `{}` og ikke `null`. **Verificeret på PostgreSQL 16.13 mod det rigtige skema** — fem påstande, mutations-testet med de tre fejl, der faktisk kan ske, alle tre fanget. 1462 tests (4 nye), lint på loftet, grønt build. ✅ **Kørt i produktionen 16. august 2026** (ejeren), så opdelingen vises, så snart frontenden er ude. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (otteogtyvende kørsel):** (✅ **Backloggens Tier 2 er kørt tom** — `G126` — og med Tier 1 tømt tidligere samme dag står de to øverste tiers begge uden rækker. Listen er 34 → 33. **`sql/story_engine_v2.sql` er kun HALVT afløst, og det er dét, der gør den farlig:** filens afsnit 3 — `generate_daily_stories()`, godt 350 linjer og over halvdelen af filen — er erstattet af `story_engine_v3.sql`, men beskriver reglerne, vægtene og teksterne fuldstændigt og selvsikkert uden ét ord om, at det er historik. En læser, der åbner filen for at forstå, hvad et dagskort siger, får et komplet og forkert svar. **Advarslen fandtes, men ikke dér, hvor man læser:** `sql/README.md`s "Farlige at gen-køre" beskriver risikoen præcist — den rigtige plads for en gen-kørsels-rækkefølge og den forkerte for "hvad af dette gælder overhovedet". **Skellet står nu som et banner øverst i filen**, med to lister frem for en advarsel: dødt er `generate_daily_stories()` og `stories_day_uniq`; stadig gældende — og defineret KUN her, hvilket er grunden til, at filen ikke bare kan afskrives — er `stories.period`/`day_key` med deres constraints, `stories_round_uniq`, `stories_user_round_day_idx`, `latest_story` og bagstopperen `generate_stories_catchup()`. To markører mere står, hvor en læser faktisk lander: ved "Hvad v2 tilføjer" og som første linje i afsnit 3. **Registret modsagde sig selv, og det kom med:** rækken for #38 sagde "Aktiv" uden at nævne v3 og bar *"Gen-kør #8 bagefter"*, mens faresektionen i samme fil siger, at #8 ikke skal med, og at rækkefølgen er #38 → #47 → #48 — følger man rækken alene, ruller man v3's dagsmotor tilbage, uden at noget fejler. **Intet at køre i Supabase:** ændringen er kommentarer i én SQL-fil plus registerrækken, og ingen SQL-sætning er rørt. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (syvogtyvende kørsel):** (✅ **Backloggens Tier 1 er kørt tom — og for anden gang i træk uden en eneste ny produktionsaflæsning.** Tieret rummer det, hvis svar ligger uden for repoet, og `A32` (10. august 2026) har afgjort, at de aflæsninger er ejerens arbejde. **`A57` stod der på ét tal:** hvor mange konkurrencer findes der stadig uden liga? Begrundelsen for at ligge i Tier 1 og ikke i Tier 6 var, at nul ville gøre "Øvrige konkurrencer" i `LigaerTab.jsx` til et tomt overgangslag, der kunne slettes. **Nul kan ikke det**, og rækken bar selv modbeviset to sætninger længere nede: `competitions.group_id` er `on delete set null`, så en slettet liga lægger sine konkurrencer ned i laget — og sletteboksen i `GroupScreen.jsx` lover det udtrykkeligt: *"de flytter ud af ligaen og står videre under 'Øvrige konkurrencer' med stilling, tips og kåringer i behold."* Vejen ind ved oprettelse er lukket siden august 2026, men den er ikke den eneste vej ind, og den anden er en lovet funktion frem for en rest. **Nul havde i det hele taget ingen handling knyttet til sig:** blokken renderes kun, når `loose.length > 0`, så ved nul viser den allerede ingenting — en sletning ville ikke fjerne en pixel, kun evnen til at tage imod den næste ligasletning. **Afgjort frem for aflæst** (ejerens valg): laget er en **understøttet tilstand**, ikke et overgangslag. Ingen migrering, ingen ændring af ligasletningen — alternativet ville kræve, at en sletning enten slettede konkurrencer med stilling og historik eller auto-oprettede en ny liga. Vejen UD findes i forvejen: `move_competition_to_group()` er den enkelte opretters beslutning, og en bulk-migrering ville tage den fra dem. **Det, der VAR forkert, var ordlyden**, rettet fire steder: `liga-laget-v1.md` skrev, at sektionen *"forsvinder naturligt"*, `LigaerTab.jsx` og `liga/CompetitionCard.jsx` kaldte den "overgangslaget", og `DOCUMENTATION.md` §18 sagde allerede det modsatte og rigtige — de to dokumenter modsagde altså hinanden, og spec'en var den, en læser ville tro på. Vilkåret står nu i §12. **Mønsteret fra 13. august holdt en gang til:** en række, der beder om en aflæsning, skal først spørges, om svaret ændrer handlingen (`A45`), om det kan gemmes i stedet for hentes (`A46`), eller om det kan afgøres (`A47`) — `A57` faldt på det første. **Backloggen er 35 → 34.** Ingen adfærdsændring, intet at køre i Supabase, intet deploy-afhængigt. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (seksogtyvende kørsel):** (**Backloggens Tier 5 er kørt og tømt for gæld** — `G125` og `G127` — og listen er 34 → 35, fordi indbakkens tre linjer fik ID'er (`G129`, `A57`, `B37`). **Fællesnævneren er den lærerige slags: begge rækker beskrev et fravalg, der var rigtigt begrundet og forkert afgrænset.** Ingen af de to hoveder havde regnet forkert — begge havde skrevet "det eneste alternativ ville koste X" og haft ret i X. Det forkerte ord var *eneste*: et fravalg er en påstand om en LISTE over alternativer, og en liste forældes stille, mens argumentet oven på den bliver ved med at lyde rigtigt. **`G125`:** `story-eksempler.test.js` kunne ikke se en regel, en nyere motorversion havde FJERNET — den slog ordlyden op i den højeste fil, der nævnte reglen, og et afsnit bliver stående i den gamle fil. Hovedet kaldte hullet permanent og navngav `sql/schema.sql` (op til en uge bagud, `G124`) som eneste alternativ. Det tredje lå i SQL'ens egen struktur: et regelafsnit bor inde i en `create or replace function`, så en regel er levende, hvis den nyeste udgave af DEN FUNKTION stadig har den. **Mutations-testet** — fjernes `CONTRARIAN` fra v3, er vagten nu rød, hvor den før var grøn. **`G127`:** sitets OG-billede var lavet uden for repoet, og opskriften lå kun i hovedet på den, der lavede det — ordret den tilstand, `build-og-image.mjs`s hoved siger, det findes for at undgå. Fravalget hed "Node kan ikke male med en woff2", hvilket er sandt, men prisen var sat på skriftgengivelse i almindelighed: **repoet har siden `I23` tegnet PNG'er ved at køre Chromium direkte, uden en eneste ny afhængighed.** Billedet er nu en side, man kan åbne (`scripts/og-image-site.html`), plus et script, der fotograferer den, og det gen-genererede billede rammer den håndlavede fils geometri inden for 1–2 px. **Appens billede er urørt** — ren Node, wordmarket alene — fordi fravalgets ANDEN halvdel er en designbeslutning og ikke en pris. **Kom med, fordi opgaven forudsatte det:** browser-håndteringen er skilt ud i `scripts/chromium.mjs` og deles nu af begge aftagere (samme begrundelse som `png.mjs` fik under `I23`), og `npm run screenshots` er kørt igennem som bevis — de fire PNG'er er byte-identiske. **Intet skal køres i Supabase.** Se [`CHANGELOG.md`](./CHANGELOG.md) og [`DECISIONS.md`](./DECISIONS.md).)

**Forrige (femogtyvende kørsel):** (🔴 **`I25`s billede blev sendt sammen med sin egen tekst — meldt af ejeren dagen efter udrulningen.** En delt besked bar billedet **og** hele dets indhold som tekst under det: dagskortets overskrift og brødtekst stod både tegnet og skrevet, og stillingen blev sendt to gange, én gang som tabel i billedet og én gang som punktliste. **Årsagen var ét felt brugt to steder:** `shareImage()` tog kun `text`, og den blev både sendt med filen og brugt som faldback, når browseren ikke kan dele filer. De to har modsatte krav — sammen med billedet skal teksten sige HVAD det er, i faldbacken ER den hele beskeden — og med ét felt kunne kun det ene opfyldes. **Fejlen var usynlig i koden**, fordi den ene linje så rigtig ud begge veje, og kunne kun ses i en rigtig beskedtråd; det er samme klasse som de to fejl, `I25` selv fandt ved at tegne billedet i en browser, bare et lag højere oppe. Feltet er delt: `caption` følger billedet og er **kortets øjenbryn** — den adresse, koden allerede kaldte kortets egen — mens `text` er uændret i faldbacken. **Billedteksten er ikke tom**, selvom billedet kan stå alene: chat-apps viser teksten og ikke billedet i notifikationen, og en skærmlæser ville ellers få en besked uden indhold. **`?? ` og ikke `|| `**, så en bevidst tom billedtekst ikke falder tilbage på den fulde tekst og sætter duplikeringen tilbage ad bagvejen. Karriereprofilens milepæl er urørt — den deler ren tekst og har intet billede at være billedtekst til. Fire nye påstande vogter, at de to felter havner hvert sit sted, **mutations-testet**: sættes det gamle ene-felt tilbage, bliver to af dem røde. **Intet skal køres i Supabase for denne rettelse** — `#67` fra i går står stadig og venter. Se [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (fireogtyvende kørsel):** (✅ **`I25`: dagens kort kan deles og ryddes, og stillingen kan sendes som billede — og to spec-sætninger blev omgjort med vilje.** Story Engine v3 fravalgte begge dele: §7 skrev, at *«delefunktionen ligger kun her; hverdagskortet har den ikke længere»*, og §8, at der er *«intet at rydde»*, fordi kortet udløber efter 48 timer. **Begge argumenter holder stadig om det, de handlede om** — at udbrede deling udvander den, og at et kort med udløbsdato ikke SKAL ryddes — men de svarede på et spørgsmål, produktet ikke stiller: hvad brugeren har lyst til at gøre. Dagens facit **er** dét, man sender i en ligachat, og et kort, man er færdig med, vil man af med nu og ikke om halvandet døgn. Ingen af sætningerne stod i spec'ens «Låste beslutninger»; begge er rettet med en markeret rettelsesblok frem for slettet. **Afvis var UI og intet andet:** `dismissed_at`, `dismissStory()`, `loadDayCard`s filter og policyen `stories_update_own` fandtes allerede. Den er server-side og ikke et lokalt flag, modsat ulæst-prikken — *«jeg er færdig med denne historie»* er en egenskab ved historien, *«jeg har set den»* ved enheden. **Prisen for delingen er betalt tre steder frem for undgået:** påmindelseskortet (`variant = 'no_tips'`) har ingen Del-knap, ulæst-prikkens tærskel er urørt (knap uden prik på det dæmpede kort, vogtet af en test hvis eneste formål er, at de to ikke smelter sammen), og mini-stillingen kommer ikke med på billedet, fordi dens navne er afgrænset til modtagerens egen konkurrence. **Stillingens billede bærer kun `#`, navn og point** — Rating, 🎯 og Form kræver hver deres forklaring, og den står i en fodnote, der ikke kan rejse med et billede; over ti deltagere vises top-10, en `…`-række og **modtagerens egen række**. 🔴 **To arvede fejl blev synlige, første gang en rigtig canvas tegnede resultatet:** brødteksten var `slice(0, 46)` og klippede midt i et ord, og stillingens titel tog kun første ombrudte linje, så «Kontorets Premier League» blev til «Kontorets Premier» — tavst. **Enhedstestene kunne pr. konstruktion ikke se det:** de dækkede beslutningerne (hvilke rækker, hvem fremhæves), ikke om en tekst løber ud over kanten. Billedet blev derfor tegnet i en browser, før det blev meldt færdigt — samme lære som `G84`s «en test, man ikke har set fejle». **Maleren flyttede ud af `RoundStory.jsx`** til `src/lib/shareCanvas.js`; `share.js` er fortsat kun transporten, og det, der skal holdes ét sted, er **rammen**. 🔴 **[`#67 analytics_standings_share.sql`](../sql/analytics_standings_share.sql) skal køres FØR frontenden udrulles**, og fælden er, at intet går i stykker, hvis den ikke bliver det: `analytics.js` svælger en afvist insert stille, så Del-knappen ville virke, mens hver deling blev tabt — et tal, der står tavst på nul, læses som «funktionen bruges ikke». Testen læser den GAMLE liste ud af skemaet og kræver, at forskellen er præcis ét navn; efterprøvet mod PostgreSQL 16 og mutations-testet. ⚠️ **Kendt kant:** en gen-kørsel af `generate_daily_stories(p_day)` giver dagens kort et nyt `id`, så en afvisning kan genopstå, hvis et resultat rettes bagud på netop den dag — skrevet ned frem for bygget væk. **Skærmbillederne til `hjem` og `stilling` er taget om**, som `I23`s tjeklistelinje kræver. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)(✅ **`I25`: dagens kort kan deles og ryddes, og stillingen kan sendes som billede — og to spec-sætninger blev omgjort med vilje.** Story Engine v3 fravalgte begge dele: §7 skrev, at *«delefunktionen ligger kun her; hverdagskortet har den ikke længere»*, og §8, at der er *«intet at rydde»*, fordi kortet udløber efter 48 timer. **Begge argumenter holder stadig om det, de handlede om** — at udbrede deling udvander den, og at et kort med udløbsdato ikke SKAL ryddes — men de svarede på et spørgsmål, produktet ikke stiller: hvad brugeren har lyst til at gøre. Dagens facit **er** dét, man sender i en ligachat, og et kort, man er færdig med, vil man af med nu og ikke om halvandet døgn. Ingen af sætningerne stod i spec'ens «Låste beslutninger»; begge er rettet med en markeret rettelsesblok frem for slettet. **Afvis var UI og intet andet:** `dismissed_at`, `dismissStory()`, `loadDayCard`s filter og policyen `stories_update_own` fandtes allerede. Den er server-side og ikke et lokalt flag, modsat ulæst-prikken — *«jeg er færdig med denne historie»* er en egenskab ved historien, *«jeg har set den»* ved enheden. **Prisen for delingen er betalt tre steder frem for undgået:** påmindelseskortet (`variant = 'no_tips'`) har ingen Del-knap, ulæst-prikkens tærskel er urørt (knap uden prik på det dæmpede kort, vogtet af en test hvis eneste formål er, at de to ikke smelter sammen), og mini-stillingen kommer ikke med på billedet, fordi dens navne er afgrænset til modtagerens egen konkurrence. **Stillingens billede bærer kun `#`, navn og point** — Rating, 🎯 og Form kræver hver deres forklaring, og den står i en fodnote, der ikke kan rejse med et billede; over ti deltagere vises top-10, en `…`-række og **modtagerens egen række**. 🔴 **To arvede fejl blev synlige, første gang en rigtig canvas tegnede resultatet:** brødteksten var `slice(0, 46)` og klippede midt i et ord, og stillingens titel tog kun første ombrudte linje, så «Kontorets Premier League» blev til «Kontorets Premier» — tavst. **Enhedstestene kunne pr. konstruktion ikke se det:** de dækkede beslutningerne (hvilke rækker, hvem fremhæves), ikke om en tekst løber ud over kanten. Billedet blev derfor tegnet i en browser, før det blev meldt færdigt — samme lære som `G84`s «en test, man ikke har set fejle». **Maleren flyttede ud af `RoundStory.jsx`** til `src/lib/shareCanvas.js`; `share.js` er fortsat kun transporten, og det, der skal holdes ét sted, er **rammen**. 🔴 **[`#67 analytics_standings_share.sql`](../sql/analytics_standings_share.sql) skal køres FØR frontenden udrulles**, og fælden er, at intet går i stykker, hvis den ikke bliver det: `analytics.js` svælger en afvist insert stille, så Del-knappen ville virke, mens hver deling blev tabt — et tal, der står tavst på nul, læses som «funktionen bruges ikke». Testen læser den GAMLE liste ud af skemaet og kræver, at forskellen er præcis ét navn; efterprøvet mod PostgreSQL 16 og mutations-testet. ⚠️ **Kendt kant:** en gen-kørsel af `generate_daily_stories(p_day)` giver dagens kort et nyt `id`, så en afvisning kan genopstå, hvis et resultat rettes bagud på netop den dag — skrevet ned frem for bygget væk. **Skærmbillederne til `hjem` og `stilling` er taget om**, som `I23`s tjeklistelinje kræver. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (treogtyvende kørsel):** (✅ **`I9`s repo-halvdel og hele `B34` er leveret — og rækkens eget forslag ville have gjort skade.** `B34` skrev, at appens indekserbarhed skulle kures med enten `Disallow` i en robots.txt på app-originen eller en cross-domain canonical, og at *"valget mellem de to er rækkens egentlige indhold"*. Svaret er ingen af dem. **Appens origin er dén, der leverer link-previewet for et delt invitationslink** (`I7`): `middleware.js` genkender facebookexternalhit, WhatsApp, Twitterbot og LinkedInBot og omskriver til `api/invite-preview.js`, **og de crawlere respekterer robots.txt** — et `Disallow: /` ville altså ikke skjule appen for Google, men slukke previewet i hver eneste gruppechat, uden en fejl, en log eller et symptom på vores side. Canonical-vejen er svagere på begge led: et hint frem for et direktiv, og en påstand om, at loginskærmen og salgssiden er den samme side. Appen får derfor **`X-Robots-Tag: noindex, follow`** i `vercel.json`, og `public/robots.txt` **tillader alt med vilje** — filen findes for at bære advarslen. **Hjemmesiden fik resten af `B34`:** `og:locale` + `og:image:alt` på alle fem sider (sættet blev leveret 13. august som "komplet" og manglede de to overalt), `site/404.html` i sitets egen identitet med **rod-absolutte stier** — en relativ sti ville give en side uden stilark på enhver dyb adresse — og cache på `/css/`. **Én af de fem småting fandtes ikke:** der er intet footer-år i `site/`, og der er ikke skrevet et ind for at kunne rette det. **Ny runbog [`SEO.md`](./SEO.md)** samler register, ejerens fem trin i Search Console, seks beviser og fejlfinding, og **ny vagt `seo.test.js`** (15 påstande, 22 mutationer, alle fanget) holder begge origins — inkl. de to modsatrettede tavse fejl: et `Disallow`, der slukker invitationerne, og en fjernet header, der gør loginskærmen indekserbar. **Backloggen er 32 → 31**; `I9` bliver i Tier 7 og er skrumpet til de fem trin, der kræver en Google-konto. 🧹 **Indbakken er tømt i samme ombæring: 31 → 34.** Tre rå linjer blev til tre `G`-rækker, og **ingen af de tre præmisser holdt.** `G125` (Tier 5): vagtens hoved kalder allerede hullet en bevidst pris og navngiver `sql/schema.sql` som eneste alternativ — men et regelafsnit bor inde i en `create or replace function`, og `generate_daily_stories()` defineres i BÅDE v2 og v3, så en regel er levende, hvis den højeste fil, der definerer dens *funktion*, også indeholder den. Ingen skema-dump, ingen falsk rød. `G126` (Tier 2): `story_engine*.sql` er otte filer, ikke fire, og den forvirrende er kun HALVT afløst — dens dagsmotor er død, mens `stories.period`, de to indexes, `latest_story` og `generate_stories_catchup()` i samme fil stadig gælder. `G127` (Tier 5): linjen påstod "samme billede, to alt-tekster", men de to OG-billeder er forskellige — sitets bærer sælgesætningen malet ind, og `scripts/build-og-image.mjs` bygger kun appens, selvom dets hoved bruger to afsnit på, hvorfor ordlyden ikke kan males ind. **Det sidste rettede en fejl fra dagens egen leverance** frem for at blive en række: sitets `og:image:alt` beskrev det forkerte billede og er rettet. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige (toogtyvende kørsel):** (🔴 **`G124` rettet samme dag, og den anden fejl blev fundet, fordi nogen forsøgte at efterprøve den første.** Trinnet sprang tavst over med et `::notice::`, hvis basis-commit'en ikke kunne slås op — altså en **grøn vagt, der måler ingenting**, hvilket er præcis den form `A26` og `G43` er skrevet imod. Det skelner nu: lutter nuller på et `push` er et lovligt spring over og siges højt; en basis, vi fik at vide men ikke kan slå op, er rødt med kuren i fejlteksten. **Og vagtens virkning kunne slet ikke aflæses:** `sql`-jobbet giver over 9.000 loglinjer, sæson-simulatorens NOTICE-udskrift fylder halen, og GitHubs log-API svarer kun med den — trinnets egne linjer lå uden for vinduet. Beviset måtte bygges på en lokal gengivelse frem for en aflæsning. Trinnet skriver nu hver udgang i `$GITHUB_STEP_SUMMARY`, så resultatet står på kørslens egen side frem for i et log, der ikke kan hentes — men **kun for et menneske:** påstanden om, at det også står i check-runnets `output`, viste sig forkert og er rettet samme time. Syv tilstande efterprøvet i en rigtig shell.)

**Forrige (enogtyvende kørsel):** (✅ **Hele Tier 5 er kørt — otte punkter, og tre af dem beskrev noget andet, end de troede.** Tre vagter (`G110` story-eksempler, `G111` henvisninger i `docs/`, `G112` skema-dumpets eget hoved), ét fælles værn (`G113`: live-tidsgrænsen og budgettet flyttet til `api/_shared.js`, så football-data.org har dem), én migrering (`G114`: `#66 job_run_duration.sql` giver `admin_job_health()` varigheder), ét opslag (`G121`: leverandørens 5xx talt op i [`CRON.md`](./CRON.md)), ét CI-trin (`G124`: anon-vagten mod grenens egne migreringer) og én løsrivelse (`I24`: `npm run harness` kører appen uden Supabase). 🔴 **Det lærerige er ikke leverancerne, men at rækkerne ikke kunne tages for pålydende.** `G110` sagde, at vagten kun kendte sin ene SIDE — den kendte også kun sin ene MOTORFIL, og `funktioner.html`s citat står i `sql/story_engine.sql` og ikke i v3, så en vagt, der kun læste v3, ville have kaldt en ÆGTE formulering opdigtet. `G114` bad om en ny kolonne og skrev, at varigheden var "umulig at rekonstruere bagud" — begge kolonner har været der siden `#18`, så der blev ikke tilføjet noget, og diagnosen gælder nu hele historikken frem for kun fremad. Og `G111`s egen første udgave var **tomt grøn**, fordi filtret spurgte, om stien indeholdt en skråstreg, hvilket en repo-relativ sti altid gør. **Alle tre kom kun for dagen, fordi vagten blev prøvet af med en mutation frem for læst igennem** — `G84`s lære brugt igen: en test, man ikke har set fejle, er en formodning. **Fire fund uden for rækkerne:** `sql/tests/_schema.mjs` manglede i `sql/README.md` (fundet af `G111`s vagt ved dens første kørsel); `\b` betyder backspace og ikke ordgrænse i PostgreSQLs regex, så `G121`s opslag klassificerede fem af syv fejltekster forkert, indtil det blev kørt mod rigtige rækker; bash tillader ikke ikke-ASCII i variabelnavne, så `G124`s trin ville have fejlet ved første kørsel; og football-datas 429-pause kunne vare 60 sekunder inde i et vindue på 30. ✅ **Begge produktionskørsler er taget samme dag:** `#66` giver Drift varigheder for hele historikken, og `#64` gjorde Scotland Premiership officiel (`A55`) — betingelsen om at køre mellem to spillerunder blev efterprøvet med et opslag, der viste NUL skotske kampe i den igangværende runde. `B36` og `B35` er lukket og slettet, og **Tier 1 er tomt**.)

**Forrige (tyvende kørsel):** (✅ **`G122`: den løsning, backloggen foreslog, var allerede bygget — hullet lå et andet sted.** Rækken sagde, at `sync-matches` hver 12. time var eneste sikkerhedsnet, hvis `sync-live` var nede, og foreslog to veje: en hyppigere kadence, eller **«en genopfriskning af de seneste dages kampe i selve kampprogram-kaldet»**. Den anden er sand i forvejen: begge providere henter HELE sæsonen i `fetchSeasonFixtures()`, og `matchUpsertRow()` skriver score for alt, der er `finished`, ved hver eneste kørsel. **Der var ingen manglende genopfriskning at bygge; der var en LATENS.** 🔴 **Og den var kortere end antaget, hvilket flyttede hele rettelsen:** `sync-live` fanger allerede selv en misset kamp, fordi dens vindue går **6 timer tilbage** (`WINDOW_BACK_MS`) og spørger på kampe uden endeligt resultat. En halv times nedetid — backloggens eget eksempel — er derfor dækket i dag. **Det virkelige hul er kampen, der er mere end 6 timer gammel og stadig står uden resultat:** den er usynlig for live-syncen og venter på næste kampprogram-kørsel, op til 12 timer. **Rettelsen ligger derfor i `sync-live` og ikke i `sync-matches`** — det er dét job, der kører hvert minut og allerede kender vejen fra kamp til leverandør, så bagstopperen koster hverken et nyt cron-job, en ny række i [`CRON.md`](./CRON.md) eller en hyppigere kadence hos leverandørerne. **Tre værn, fordi den naive udgave punkterer jobbets forbrugsbegrænsning:** efterfejningen kører kun **ét minut i timen** (24 ekstra kald i døgnet i værste fald frem for 1.440), har en **øvre alder på 36 timer** (derefter er kampen et datapunkt for et menneske, ikke et tabt slutfløjt), og et **loft på 40 kampe** (Sportmonks' grænse pr. kald, så én turnering med noget galt ikke kan gøre bagstopperen til jobbets dyreste del). **Minuttallet er 41 og ikke tilfældigt:** 00, 05, 11, 15, 17, 23 og 29 er optaget af kampprogram-jobbene, og `CRON.md` beder udtrykkeligt om, at et nyt football-data-kald ikke lægges oven i et af dem. **Kvitteringen er tre-værdiet med vilje:** `staleChecked` MANGLER, når minuttet ikke var fejeminuttet, og står `0`, når der blev fejet uden fund — et felt, der altid var der, holder man op med at læse, og et felt, der kun var der ved fund, kunne ikke skelne «fejede, alt var fint» fra «holdt op med at feje». Det er `A11`s og `G43`s lære brugt på forhånd. **`staleRescued` er ikke en succes at glæde sig over**, men et spor efter et tabt slutfløjt: står tallet vedvarende over nul, skjuler bagstopperen et hul i live-syncen frem for at afsløre det. **Gatet er en ren funktion med fem påstande**, fordi det er den eneste del af jobbet, der kan lave et leverandørkald på et ellers tomt minut — forsvinder det, bliver 24 kald til 1.440, uden at noget i appen ser anderledes ud. **Intet skal køres i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (nittende kørsel):* (🔴 **`G119`: heartbeat'en blev rød af den migrering, der blev leveret to timer før — og den havde ret.** Tyve minutter efter at [`#65`](../sql/job_health_rate.sql) blev kørt i produktionen, meldte `job-heartbeat.yml`s femte kontrol `admin_job_health() … AABEN FOR ANON` med `via_public = t`, og gentog det hver halve time. **Årsagen er én manglende linje, og den er `drop function`s skygge:** filen SKULLE droppe funktionen (returtypen ændrede sig, og `create or replace` svarer `42P13`), rettighederne følger funktionen i graven, og filen gentog derfor `grant execute … to authenticated` med en kommentar om, at grant'en «ikke er pynt» — men `revoke execute … from public`, som forsvandt i nøjagtig samme sætning, blev ikke gentaget. Den nye funktion fødtes med PostgreSQLs indbyggede default, hvor PUBLIC og dermed `anon` har EXECUTE. **Fælden er asymmetrisk, og det forklarer, at kun den ene halvdel blev husket:** en glemt `grant` lukker driftskortet for ejeren i samme sekund og er rød i første påstand, en glemt `revoke` ændrer intet, nogen kan se. **Ingen data var åbne** — funktionen er `security definer` med en `is_admin`-vagt og svarer `forbidden` uden login — men bredden var meningen med `#56`, og det var dobbeltsikringen, der var væk. 🔴 **Reglen stod ordret i fejlfindingsloggen to dage før** (`G100`, 12. august) og blev overtrådt alligevel: **en regel, der kun findes i prosa, bliver overtrådt af den, der skrev den.** Rettelsen er derfor ikke kun linjen, men **påstand 6 i [`sql/tests/job_health_rate.sql`](../sql/tests/job_health_rate.sql)** — placeret hos migreringen selv, fordi CI's eksisterende vagt over samme regel måler `sql/schema.sql`, et øjebliksbillede fra om mandagen, og derfor ikke kan se en migrering skrevet i dag. **Begge retninger måles**, så en for bred revoke ikke bare bytter fejlen om; efterprøvet med to mutationer mod PostgreSQL 16. **Overvågningen gjorde præcis det, `G100` blev bygget til** — uden den havde fejlen stået åben i op til en uge. 🔴 **Udestår hos ejeren: `#65` skal gen-køres i Supabase** (anden gang samme dag), ellers er heartbeat'en rød hver halve time. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (attende kørsel):* (✅ **`G118`: dagskortet sagde det samme som kortet lige under det — i alle tre tilstande.** Fundet kom fra `I23`s eget skærmbillede: Hjem sagde "2 kampe mangler tips" to gange lige efter hinanden, som grøn knap i bunden af dagens historie og 40 px længere nede i deadline-kortet. **Det var ikke et sammenfald, men en garanti** — kortets fod og tips-kortet hang på ordret de samme udtryk (`tips.allTipped === false`), og duplikeringen fandtes i alle tre tilstande: manglende tips, "alt ok" og "intet at tippe lige nu" gentog hver især kortets fod, med nedtælling, rundenavn og kampnavne oveni. Foden kunne dermed **aldrig** være en faldback for noget, skærmen ikke allerede sagde bedre. **Prisen var kortets egen pointe:** et kort, der er VALGT af en nyhedsværdi-score til at bære dagens ene øjeblik, sluttede på en administrativ opgave frem for på sin mini-stilling — og rundestoryen, det større format, har aldrig haft en sådan fod. **Reglen, rækken efterlader:** et kort, der er valgt for at bære ét øjeblik, må ikke slutte på en opgave. **Spec'en er rettet frem for koden:** §8 opregnede foden, men sætningen hvilede på en forudsætning, der aldrig blev til noget — at dagskortet skulle ERSTATTE deadline-kortet — og linjen er streget over med begrundelsen skrevet frem. **`hjem.png` er taget om i samme ombæring**, som `I23`s egen tjeklistelinje kræver; det er første gang, den regel bruges, og den blev brugt af den ændring, der indførte den. **Intet skal køres i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (syttende kørsel):* (✅ **`I23`: manifestet fik `id`, `scope`, `lang` og fire skærmbilleder — og billederne er taget af appen selv.** Chrome viser den **rige** installationsprompt, når manifestet bærer skærmbilleder; uden dem er prompten en nøgen ikon-dialog, og installationen er hele distributionsvejen for en app, der ikke ligger i nogen butik. **`id` er den vigtigste af de tre små og ændrer ingenting i dag** — den er sat til `"/"`, altså præcis den værdi, browseren allerede har udledt af `start_url`, og pointen er, at identiteten dermed er FROSSET: flyttes `start_url` en dag, ville en installeret genvej ellers høre til en app, der ikke findes mere — uden fejl og uden opdateringer. **Skærmbillederne er genereret af den rigtige app** (`node scripts/screenshots/capture.mjs`): Vites udviklingsserver, appen i en headless Chromium med en **attrap i stedet for en database**, et tryk frem til fanen, og en beskæring til appens egen ramme. Alternativet — fire mockups i HTML — ville være hurtigere at lave og forkert dagen efter, fordi en attrap af en skærm kan vise noget, appen ikke kan. **Snittet ligger på `fetch`** og ikke på datalaget, så hver linje ovenover er den kode, der ligger i produktionen; prisen er et lille stykke PostgREST (seks operatorer, talt op i koden), og en forespørgsel med noget andet **kaster** frem for at svare tomt — et tomt svar bliver til en tom skærm, og en tom skærm i en PNG ser ud som et designvalg. Demo-dataene regner point og stillinger med appens egen `pointsFor()`, og uret er frosset, så to kørsler giver samme billede. **Tre ting kom med, som opgaven forudsatte:** PNG-codec'en er flyttet til `scripts/png.mjs` og deles nu med `build-og-image.mjs` (som gen-genererer `og-image.png` byte-identisk — beviset på, at flytningen var ren), ESLint har fået en browser-blok til harnessen, og `manifest.test.js` er ny: JSON kan ikke bære en kommentar, så begrundelserne for `id` og `scope` står i testen, som samtidig håndhæver, at filerne findes, at `sizes` passer på PNG'ernes egne mål, og at Chromes tre krav til målene er opfyldt. **Den halvdel, der ikke er gratis, er accepteret som manuel:** en PNG, der viser en forældet skærm, er en gyldig PNG, så en linje i tjeklisten før merge er værnet — samme form som `build-og-image.mjs` har haft siden `I7`. **Backloggen er 38 → 37, og Tier 3 er tom.** **Intet skal køres i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (sekstende kørsel):* (🔴 **`G117`: den yderste tidsgrænse stod uden for repoet — og var den strammeste.** `G116` reparerede `G109`s gen-forsøg; denne række fjerner det igen, og begge dele er rigtige. Da gen-forsøget begyndte at virke, blev en fejlende kørsel ~42 sekunder i stedet for 21,7 — og **cron-job.org afbryder kaldet efter 30 sekunder**, som er maksimum på planen. Rettelsen ville altså have flyttet hver eneste fejlende kørsel fra «inden for kalderens vindue» til «klippet af kalderen», så vores egen fejltekst aldrig nåede frem til det log, auto-deaktiveringen tæller på — den ene mekanisme, der kan slukke live-syncen helt og tavst. **Regnestykket afgør det:** ét kald à 20 s plus Supabase er ~22 s og passer; to kald er ~42 og gør ikke. Der er plads til **ét udgående kald pr. kørsel**; budgettet er sat til 25 s, så kørslen fejler før kalderen, og live-stien kalder `smFetch()` med **`retries: false`** — et valg med et navn frem for noget, aritmetikken tilfældigvis udelukker, hvilket er `G116`s lære brugt med det samme. **Gen-forsøget er jobbet selv:** `sync-live` kører hvert minut, så et mislykket minut prøves igen 60 sekunder senere af en frisk invokation — og et gen-forsøg inde i kørslen ville have fordoblet belastningen på en leverandør, der allerede var ved at drukne (samme afvejning som `G48`). **Adfærden i produktion er uændret**, netop fordi gen-forsøget aldrig fyrede. **Kæden af grænser er nu skrevet ned i [`CRON.md`](./CRON.md), yderste først** — 30 s > 25 s > 20 s, med `maxDuration` (60 s) som den bevidst løseste bagstopper. **En indstilling hos en tredjepart er lige så bindende som en konstant i koden**, og indtil nu stod den ingen steder. **To indstillinger rettet hos cron-job.org samme dag:** `Notify after` fra 1 til 3 fejl (med 1 ville `G109` have givet ~25 beskeder på en halv time), og dobbeltkørslerne er forklaret — ejerens egne testkørsler, ikke et gen-forsøg. **Intet skal køres i Supabase.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (femtende kørsel):* (🔴 **`G116`: gen-forsøget fra `G109` havde aldrig fyret én eneste gang — og selve slutfløjtet var ikke vores fejl.** Sagen begyndte hos ejeren (livescoren opdaterede ikke, resultatet måtte tastes) og endte tre lag nede. **Lag 1 er lukket:** kørslerne i `job_runs` viser, at `sync-live` meldte kampen i gang helt frem til 21:05 og skrev det endelige resultat 21:06 — **Sportmonks holdt kampen live i et kvarter efter det rigtige slutfløjt**, på en aften hvor dens endpoints skiftevis timede ud og svarede `503`. Koden gjorde det rigtige: et endeligt resultat skrives kun, når kilden melder slut, og at gætte ud fra en live-stilling ville gøre point til et øjebliksbillede. **Lag 2 var et tal, der ikke passede:** efter `G109`-deployet fejlede stadig 12 af 30 kørsler. Halvdelen var `503` fra leverandøren; den anden halvdel skulle gen-forsøget have taget. Beviset for at det ikke skete, var ikke fejlteksten — den var uændret — men **VARIGHEDEN**: 21,7 sekunder hos cron-job.org, hvor to forsøg ville koste ~41. **Årsagen var to konstanter, der gik præcis op:** budgettet var 2 × tidsgrænsen, og gen-forsøget krævede en HEL tidsgrænse tilbage — mens et timeout pr. definition har brugt hele grænsen. Betingelsen var falsk hver eneste gang, og `G109`s egen kommentar skrev regnestykket ordret uden at nogen bemærkede, at et regnestykke, der går præcis op, ikke har plads til virkeligheden. **Testen så det ikke, fordi dens `fetchImpl` kastede ØJEBLIKKELIGT** — uret var ikke rykket, når budgettet blev spurgt, så den var grøn for både den rigtige og den forkerte implementering. **En test af et tidsbudget skal bruge tid**; der er nu to, og de var røde før rettelsen. **Rettelsen fjerner koblingen frem for at give den luft:** gen-forsøget får `min(perCall, resten)` og kræver kun `LIVE_MIN_CALL_MS`. At hæve budgettet til 45 s ville have virket i dag og stillet den samme fælde igen ved næste ændring af et af de to tal. **Intet skal køres i Supabase for denne række.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (fjortende kørsel):* (🔴 **`G115`: driftskortet stod grønt hele vejen igennem en nedetid, det selv målte.** Rækken kom fra ejeren: livescoren opdaterede ikke under Viborg FF–AGF, resultatet måtte tastes i hånden — og Admin → Drift sagde **OK, 0 fejl i træk**. Kortet løj ikke, det svarede på et andet spørgsmål: `consecutive_failures` tæller fejl SIDEN SENESTE VELLYKKEDE KØRSEL og nulstilles af enhver succes, så `sync-live` kunne fejle to ud af tre minutter i en time — hvilket det gjorde under `G109` samme aften — uden nogensinde at forlade tilstanden `ok`. **En fejlSERIE er ikke en fejlRATE**, og tælleren var blind for præcis det mønster, den skulle fange. Det er `B8`s og `G44`s fejlklasse en tredje gang: en sund måling, der skjuler en syg. **Rettelsen:** `admin_job_health()` svarer også `recent_runs`/`recent_failures` over de sidste 24 timer ([`#65`](../sql/job_health_rate.sql)), og et job vises som `ustabil`, når mindst 10 % af mindst fem kørsler fejlede — med rå tal på kortet ("25 af 37") og procenten som detalje, fordi nævneren selv er oplysningen. 🔴 **Rettelsens egen første udgave havde ét vindue på 24 timer og ville IKKE have fanget hændelsen:** ejerens opslag i `job_runs` viste bagefter, at `G109` var 25 fejl ud af 37 kørsler på 33 minutter — 68 % af timen, men 1,7 % af et døgn. Det er samme fejl en etage højere: **en målings opløsning skal være finere end det fænomen, den skal se.** Funktionen svarer derfor TO vinduer, time og døgn, som dækker hver sin form — den intense og korte, og den langsomme blødning hos et job, der kører for sjældent til, at timen bedømmes. **Vinduerne er TIDSvinduer og ikke «de sidste N kørsler»:** 30 kørsler er en halv time for live-syncen og en halv måned for kampprogrammet, og databasen kender ingen kadencer. **Grænsen på fem kørsler beskytter kampprogram-jobbene** mod at blive gule af «1 af 2» — et kort, der ofte er gult uden grund, lærer man at ignorere. **Raten kan hæve til `ustabil`, aldrig til `fejler`:** den tilstand er heartbeat'ens og hører til et job, der er holdt op med at virke. ⚠️ **Migreringen gør `#18 job_runs.sql` umulig at gen-køre i sin helhed** — dens gamle `create or replace` kan ikke ændre en returtype og stopper med `42P13`; det er den gode retning at fejle i, men rækkefølgen efter en gendannelse er herefter #18 → #65 → #60. **Udestår hos ejeren:** `#65` skal **gen-køres** (den kørte udgave er den med ét vindue), og **selve udgangsspørgsmålet står stadig åbent** — hvorfor de GRØNNE kørsler efter `G109`-deployet ikke færdigmeldte kampen. Det kræver et opslag i `job_runs.detail` for den time og ligger i backloggens indbakke. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (trettende kørsel):* (✅ **Backloggens Tier 2 er kørt TOM: tre uenigheder mellem hjemmeside og app fik tre forskellige svar. Listen er 40 → 38.** Fællesnævneren var én fejlklasse — to flader, der beskriver det samme produkt forskelligt — men svarene blev ikke ens, og dét er pointen: en uenighed mellem to flader er ikke i sig selv en fejl. **`A54` lukkede uden en kodeændring:** sitet beholder "Skotske Premiership", `leagues.name` beholder "Scotland Premiership", præcis som `A49` afgjorde for La Liga/Primera División dagen før — sitet er marketing, `leagues.name` er data, og sitets brødtekst taler i forvejen det danske ord fire steder. **`A55` blev afgjort den dyre vej: Scotland Premiership forfremmes til officiel** (`sql/tournament_scotland_promote.sql`, `#64`) frem for at sitets copy tager forbehold — forudsætningen for `is_official = false` var generalprøven, og den er overstået efter to uger med rigtige kampe, rigtige tips og en live-sync. **Prisen er accepteret og virker bagud:** stillingerne er views og tæller skotske tips med i alle historiske runder ved næste opslag, og `recompute_ratings()` er en fuld genopbygning — derfor kalder migreringen den selv, så skiftet sker på ét kendt tidspunkt frem for ved den næste tilfældige målscoring. **Den største rettelse stod ikke i nogen af rækkerne:** `sql/tournament_scope.sql` satte `is_official = false` på Skotland midt i det script, der definerer `round_standings` og `monthly_standings` — altså i en fil, der SKAL gen-køres, hver gang de views ændrer sig, og en almindelig gen-kørsel ville have rullet forfremmelsen tilbage tavst og flyttet rating og titler for alle brugere. Sætningen er fjernet: den var en starttilstand skrevet som en regel, samme fejl som `G65`. **`A56`: appen bærer nu "Beta"** ved ordmærket (`src/ui/Wordmark.jsx`), så `A48`s exit-kriterium gælder begge flader — alternativet ville have rullet `A48` tilbage to dage efter, den blev truffet. **Hverken sitet eller Championship-fanen skulle røres:** *"rating opdateres i alle turneringer"* bliver sandt, når `#64` køres, og `scopeNote()`-linjen forsvinder af sig selv, fordi funktionen returnerer `null` uden uofficielle turneringer — ordret som spec'en forudsagde 31. juli. **Udestår hos ejeren:** `#64` er ikke kørt i produktionen og står som `B35` i Tier 1 — den skal køres mellem to spillerunder. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (tolvte kørsel):* (🔴 **`G109`: livescoren fejlede i to ud af tre minutter — og leverandøren havde ikke sendt én eneste fejl.** `sync-live` stod **USTABIL** i Admin → Drift med `Tidsgrænse: intet svar fra …/fixtures/multi/<id> inden for 10000 ms` på ét fixture-id og den letteste include-kombination, endpointet kan få. **Diagnosen lå i de kørsler, der LYKKEDES:** de tog 7-13 sekunder, altså lige under grænsen på 10 — signaturen på en leverandør, hvis svartid vandrer omkring vores eget loft, ikke på et nedbrud. **Den generelle regel:** en fejlrate, der ikke er 100 %, skal læses på de grønne kørsler; ligger de lige under grænsen, er grænsen fundet. Rettelsen er to ting, der kun giver mening sammen — en egen tidsgrænse for live-opslaget (20 s mod standardens 10, fordi `G19`s grænse blev valgt mod kald, der **hænger**, og et svar på fjorten sekunder hænger ikke) plus ét gen-forsøg ved timeout, **bundet af et samlet budget** for hele opslaget (40 s): uden budgettet ville det højere loft blot flytte afklipningen op til `maxDuration`, hvor kørslen fejler **uden at efterlade en fejl at læse**. Gen-forsøget gælder KUN timeouts — en 403 er et *svar*, og et gen-forsøg på et svar er bare et kald mere. **Ingen data var i fare undervejs:** `skipped`-sættet holdt den fejlede leverandørs kampe ude af beslutningsløkken, så ingen igangværende kamp fik ryddet sin live-markering; prisen var, at live-stillingen stod stille i de fejlende minutter. 🧹 **Backloggens indbakke er tømt i samme ombæring: 30 → 40 åbne punkter.** Elleve rå linjer blev til ti ID'er og ét lukket hul. Tre uenigheder mellem hjemmeside og app fik hver sit spørgsmål (`A54` turneringens navn · `A55` `is_official=false` mod sitets salg af Scotland Premiership som live-flagskib · `A56` beta-mærkatet, der kun står ét af stederne) og ligger samlet i Tier 2 — alle tre er ejerens beslutning. Fem vedligeholds-rækker blev til `G110`–`G114` i Tier 5, `I23` (manifestets installationsfelter) til Tier 3, og `B34` (app-originens indekserbarhed plus fem site-småting) til Tier 7 ved siden af `I9`. **Ét punkt blev lukket frem for nummereret:** spørgsmålet om `B31`s manglende arkivpost krævede ingen række — rækken kunne læses ordret ud af git-historikken og var en ren bogholderi-række, hvis leverance allerede står arkiveret i 13. august-indslaget. **To af de elleve linjer kom fra dagens egen leverance** og er dermed reglen om at notere frem for at udvide opgaven, brugt på sig selv. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (ellevte kørsel):* (🧹 **Fuld konsistensgennemgang af app, hjemmeside og dokumentation — dokumenterne er bragt i trit med koden.** Seks parallelle gennemlæsninger holdt `DOCUMENTATION.md`, drift-runbøgerne, `sql/README.md`, hjemmesiden og styringsdokumenterne op mod repoet og rettede ~60 uoverensstemmelser: forældede antal (moduler, regler, metrikker, afhængigheder — flere er gjort tælleløse efter `G21`s princip), døde fil:linje-ankre, `RESTORE.md`s kopi af gen-kørselslisten, der var drevet fra kilden (fire rækker mod tolv — kopien er nedlagt til fordel for et opslag), to migreringer (`#18 job_runs.sql`, `#27 security_hardening.sql`), der genskaber policies i den gamle `is_admin`-form, som `#60` har afløst, og som nu står på listen — samt fem faktuelle fejl i hjemmesidens copy (vigtigst: "stillingen følger med live" — point og rating flytter sig først ved slutfløjt — og "justér frit indtil kickoff" — tips låser en time før). `public/manifest.json` bar stadig Prediction-Champ-æraens beskrivelse og er nu den sjette fil under `saelgesaetning.test.js`s vagt. `A52` er hermed også ført ind her (Vercel Web Analytics fravalgt i appen, sat op og rullet ud igen samme dag — se `DECISIONS.md`/`CHANGELOG.md`; den manglede kun i denne fil). CLAUDE.md's rutetabel har fået de manglende rækker (Story Engine v3, milepæle, hjemmesiden, `UDRULNING-A43.md`, `docs/reviews/`), og backloggens indbakke bærer ni nye rå linjer fra gennemgangen. Leverancen står i [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (tiende kørsel):* (✅ **Backloggens Tier 5 er kørt og TØMT — `G106`, `G107` og `G108` er leveret. Listen er 33 → 30.** Alle tre var **rester**, der faldt ud af `G101` og `A53` dagen før og blev noteret frem for taget med, og fællesnævneren var, at ingen af dem var en fejl, nogen oplevede — enogtyve brugere, ingen liga i nærheden af PostgRESTs loft, ingen kendt genindtræden. De var i stedet hver især et sted, hvor **to svar på det samme spørgsmål kunne nå at afvige.** **`G106`: ligaens to tal tælles nu i databasen** — `loadMyGroups()` hentede én række pr. medlem og pr. konkurrence på tværs af ALLE brugerens ligaer for at tælle dem, og PostgREST klipper tavst ved 1000. Svaret blev viewet [`#62`](../sql/group_counts.sql) og **ikke** `G101`s fan-out: dér er antallet af kald bundet af én ligas konkurrencer, her ville nævneren være ligaer GANGE TO. **Reglen, der kom ud af det** (nu i `DOCUMENTATION.md` §13): vælg kur efter, om antallet af kald er BUNDET — og `G35`s synlige loft passer kun på en LISTE, man kan handle på, aldrig på en optælling. `security_invoker` er bærende og ikke pynt: uden det ville tallene svare uden om RLS, og med det ændrer migreringen **ikke ét tal på skærmen**. **`G107`: karriereprofilens indbyrdes opgør bærer nulpunktet** — et møde tæller kun, hvis kampen låste, EFTER begge meldte sig til. Rækken var stillet som en BETYDNING og ikke som en fejl, og det, der afgjorde den, var ordet *adgang*: opgøret spurgte slet ikke om adgang, så *"I jeres fælles konkurrencer har I mødt hinanden N gange"* var ikke bred — den var **usand**. Begge blokke i `career_profile.sql` er rettet sammen, fordi spec'ens testcase 41 er invarianten mellem dem. 🔴 **Funktionen havde ingen SQL-test overhovedet** og har nu sin første. **`G108`: nulpunktet overlever en framelding** ([`#63`](../sql/competition_participant_baseline.sql)) — `A53` tømte hele sæsonen, når nogen forlod en FÆRDIGSPILLET konkurrence og kom tilbage. **`A53` svækkes ikke:** en helt ny deltager har ingen historik og starter fortsat på 0. At spærre for framelding blev valgt fra, fordi netop den gren findes, for at man kan forlade en konkurrence, man HAR spillet. 🔴 **Den migrering brækkede to helt almindelige handlinger, før guarden kom på:** en `on delete cascade` kører EFTER forældrerækken er væk, så en skrivende AFTER DELETE-trigger kunne ikke længere tåle, at man slettede en konkurrence eller lukkede en konto (`23503`) — fanget af testens påstand 5, som sletter FORÆLDEREN og ikke kun barnet. ✅ **Alle tre migreringer er kørt i Supabase 14. august 2026** — `#62` før frontend-mergen, som dens ene rækkefølge-regel kræver, plus `#63` og en gen-kørsel af `career_profile.sql` (`#10`). ✅ **Skema-eksporten er kørt samme dag** og gik gennem en pull request, hvor hele CI blev kørt mod det nye dump før mergen — så `sql/schema.sql` er igen en gyldig reference. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (niende kørsel):* (✅ **`A53` er afgjort og leveret: en deltager starter på 0 i den konkurrence, hun melder sig til.** Rækken kom fra en bruger, ikke fra backloggen — først et skærmbillede af en TOM stilling (`G107`, samme dag), og da den var rettet, spørgsmålet under den: hun havde 1 point, selvom hun lige havde meldt sig til. **Hun havde ikke tjent det, men havde heller ikke fået det ved en fejl:** `predictions` er én række pr. `(bruger, kamp)` og har ingen konkurrence-dimension — **der findes ikke sådan noget som "et tip i en konkurrence"** — så hendes gæt fra en anden ligas Superliga-konkurrence talte med, i det sekund hun meldte sig til. **Princippet var afgjort i forvejen, og det er rækkens vigtigste fund:** `filterTippable` beskytter allerede en ny KONKURRENCE mod at starte med point, og `DOCUMENTATION.md` §3 begrunder det med ord, der er brugerens situation ordret (*"deltagerne ville ikke stå lige, for den, der ikke havde tippet kampen andetsteds, kan ikke nå det"*). Værnet sad bare på oprettelsen og havde ingen pendant på tilmeldingen. Reglen er nu `match_lock_at(...) > joined_at`, båret ét sted i JS (`wasTippableAt`) og ét sted i SQL ([`#61`](../sql/competition_join_baseline.sql), som rammer BÅDE `competition_match_points` og kåringsfunktionen). **To grunde til at lukke frem for at beskrive, og den første vejer tungest:** en sen tilmelding kan gøre **allerede udsendte historier forkerte bagud** — kort er sendt, milepæle er permanente — og adfærden kunne spekuleres i. **Rækkevidden er ALLE deltagere**, ikke kun fremtidige: prisen er, at den, der allerede er meldt sent til noget, taber point ved næste åbning, men alternativet var en fast dato i to filer, som ingen kan læse sig til, og det forkerte tal ville overleve rettelsen. **Bagud omskrives intet** — alt frossent står stille. 🔴 **Rækken gjorde to ældre migreringer farlige at gen-køre** (`#37`, `#26`), så `sql/README.md`s liste er vokset fra ti filer til tolv. 🔴 **Og den fandt en landmine, der først ville være sprunget om en uge:** `simulate_season.sql` lod `joined_at` tage sin default, mens den bakker kampene i tid — under den nye regel blev hele den simulerede sæson TOM (ingen kåringer, ingen historier), og testen ville være blevet rød i det sekund, skema-eksporten kørte. Fundet ved at køre alle femten skema-indlæsende tests fra den anden side af dumpet. **Backloggen er 31 → 33** (`G107`, `G108` — begge noteret frem for taget med). ✅ **`#61` er kørt i staging og produktion 14. august 2026**, og alle tre verifikationer er aflæst — leddet står begge steder, og begge nul-kontroller gav nul. 🔶 Tilbage står skema-eksporten. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (ottende kørsel):* (✅ **Backloggens Tier 5 er kørt — `G101` og `G103` er leveret, og `G106` er åbnet af den første.** Begge rækker beskrev en kobling, ingen holdt øje med, og **begge oplyste deres egen præmis forkert** — den ene undervurderede sin grund, den anden overvurderede sin påstand. **`G101` undervurderede sig selv:** rækken bar `A43`s måling (2,2 ms for en liga med otte konkurrencer) og konkluderede, at ombygningen ikke løste noget akut. Målingen står ved magt, men den målte det forkerte — **prisen ved at hente én række pr. deltager er ikke tiden, det er loftet.** PostgREST leverer højst 1000 rækker og siger ikke, at den klipper, så "N deltager" på et konkurrence-kort ville en dag være for lavt uden en fejl nogen steder; det er den fælde, der kostede "· 0 kampe" i Opret → Sæson (§13). Tællingen sker nu med `db.count()` pr. konkurrence, kørt samtidig med det opslag, der i forvejen spørger om MIG. 🔴 **Den idiomatiske løsning blev fravalgt, fordi den ikke kunne prøves af:** PostgRESTs indlejrede `competition_participants(count)` ville have været ét kald frem for otte, men repoet bruger ingen indlejrede selects, og netværkspolitikken afviser Supabase, så end ikke et tomt svar kunne bekræfte syntaksen. **Afvejningen er asymmetrisk** — gætter man rigtigt, spares syv kald på en side, der ikke er langsom; gætter man forkert, fejler hele liga-siden. `A32`s snit en gang til, bare på en syntaks frem for på et tal. **`G103` overvurderede sig selv:** rækken skrev, at sitets story-eksempler ER motorens ægte formuleringer, og ingen af de fire er det ord for ord — sitet sætter punktum, hvor motoren ikke gør, og stime-kortet er overskriftens 🔥 sat foran brødtekstens ordlyd, fordi mockup'en har én linje, hvor appen har to. Alle afvigelser er redaktionelle, så vagten (`story-eksempler.test.js`, søskende til `saelgesaetning.test.js`) måler **ordene mellem værdierne**: sitet markerer sine variable med `<span class="story-var">`, hvert kort siger med `data-story-rule`, hvilken regel det citerer. 🔴 **Den var grøn to gange, før den målte noget** — ordlyden står også i en `--`-kommentar, og "Du sluttede dagen" bruges også af regel 45; begge fundet ved at ødelægge SQL'en med vilje. **En vagt, der ikke er set fejle, er ikke set.** **To fund er noteret frem for taget med:** `loadMyGroups()` har `G101`s fejl tolv linjer længere oppe i samme fil, men ikke dens kur (nævneren er brugerens ligaer gange to) — nu `G106`; og backloggens **indbakke** bar femten linjers referat under en overskrift, hvis egen regel forbyder det, så `docs/backlog.test.js` måler nu reglen begge steder. **Intet skal køres i Supabase.** **Backloggen er 32 → 31.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 14. august 2026 (syvende kørsel):* (✅ **Hjemmesiden er LIVE på `leagly.app` — `I8` er leveret.** Ejeren har oprettet Vercel-projektet med root directory `site` og intet build command; rækken har været åben siden 3. august 2026. **`A50` er bevist i samme ombæring og af den billigste mulige aflæsning:** to skærmbilleder af `/robots.txt` og `/sitemap.xml`, som begge svarer fra roden med indholdet fra `site/` — og det samme skærmbillede beviser, at root directory er sat rigtigt, for ellers ville filerne ligge på `/site/…`. **De to sidste aflæsninger er kørt af ejeren samme dag, og begge er bestået** (bevis 6 og 7 i [`DOMAENE.md`](./DOMAENE.md)): `leagly.app` svarer 200 med CSP'en fra `site/vercel.json` **ord for ord** — filen havde aldrig kørt i produktion, **og en headers-blok, der ikke bliver læst, fejler tavst** — og `www` svarer 308 mod apex. **Svaret bar tre led, beviset ikke bad om:** fontfilerne fik også CSP'en (`source: "/(.*)"` gælder alt, som skrevet), forsidens `max-age=0, must-revalidate` og HSTS-headeren kommer fra **Vercel og ikke fra vores fil** — så led ikke efter dem i `site/vercel.json`, hvis de en dag mangler. Aflæsningerne er ejerens, fordi udgående HTTPS til `leagly.app` afvises af arbejdsmiljøets proxy: `A32`s snit en gang til. **`I10`s trin 2 er dermed kørt, og `I9` er ikke længere gated** — der er nu en publiceret side at indeksere. **Backloggen er 34 → 32.** Se [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 13. august 2026 (sjette kørsel):* (✅ **`I8` er udrulningsklar — hjemmesidens resterende arbejde i repoet er gjort, og fire backlog-rækker faldt med det.** Sitet er stadig **ikke publiceret**, men **copy er godkendt af ejeren samme dag** på et klikbart preview af alle fem sider, uden rettelser. Tilbage er ét punkt, og det kan ikke skrives i en fil: **trin 2 i [`DOMAENE.md`](./DOMAENE.md)** — et nyt Vercel-projekt med root directory `site`. **`I8` gater dermed ikke længere `I10`s trin 2.** **Ingen af de fire rækker ventede på en udløser; de ventede på et valg.** `A48`: Beta-mærkatet spurgte, hvornår det måtte *fjernes*, men det fandtes ikke i `site/` — det står nu i headeren på alle fem sider og ryger igen efter en måned publiceret uden fundne fejl. `A49`: sitet beholder "La Liga", appen "Primera División" — **rækkens forudsætning om, at de to skulle sige det samme, er dét, der falder**, ikke navnet; sitet møder folk på det ord, de søger på, og et navn i `leagues.name` er data. `B33`: clean URLs slås **ikke** til, skrevet som `"cleanUrls": false` i nye `site/vercel.json` — en betinget række uden nogen, der vil trykke på betingelsen, står i Tier 6 for evigt. `I22`: **rækken oplyste sin egen pris forkert** — den skrev, at en burger-menu ville koste fravalget af JavaScript, men en skjult checkbox og en `<label>` gør det samme i ren CSS. Det er anden kørsel i træk, hvor svaret er, at rækkens pris var sat for højt (jf. `A47`). Målt i Chromium: headeren var **159px høj på en 390px-skærm** og er nu 69px, mens de fem punkter står på én linje helt ned til 701px — så burgeren slår til ved 700px og ikke ved 880px, som rækken gættede. **To ting, ingen række bad om:** `site/vercel.json` bærer nu en CSP, hvor `script-src 'none'` **håndhæver** sitets egen regel om ingen JS i stedet for at lade den være en sætning i en spec (appens `vercel.json` røres ikke — to origins deler ikke headere), og en **vandret scroll under 340px** er rettet: `.phone` havde `width: 300px`, og en fast bredde gør grid-sporets min-content 300px. Fejlen lå præcis mellem de to bredder, udkastets egen verifikation målte. **Backloggen er 38 → 34.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 13. august 2026 (femte kørsel):* (✅ **Backloggens Tier 1 er tømt — og for første gang uden en eneste ny produktionsaflæsning.** Tieret rummer det, hvis svar ligger uden for repoet, og `A32` (10. august 2026) har afgjort, at de aflæsninger er ejerens arbejde; det, der kunne gøres billigere, var **bestillingen**. Alle tre rækker var skrevet som aflæsninger, og ingen af aflæsningerne blev nødvendige. **`A46` — spørgsmålet flyttet ind i appen:** `CRON.md`s `<app>`-pladsholder skulle efter rækkens ordlyd slås op ved at åbne ni jobs i cron-job.org, men `req.headers` har hele tiden båret værtsnavnet. `createRunLogger()` har fået `setHost()` ved siden af `setAuth()`, så hver kørsel skriver `host` i `job_runs.detail` — og Admin → Drift viser det uden en eneste UI-ændring, fordi jobkortets "Seneste resumé" i forvejen dumper detaljen som JSON. **Det er `A11`s fremgangsmåde en gang til**, og `CRON.md` havde allerede skrevet læren ned med rene ord: svaret lå aldrig i en brugerflade uden for repoet, det lå i jobbenes egne kørsler, som bare ikke gemte det. Rækken er i Tier 6, fordi de ni jobs skal have kørt én gang efter udrulningen (langsomste skema er hver 12. time) — ikke fordi nogen venter. **`A45` — diagnosen droppet, kuren udført:** rækken spurgte, om `recovery.html`s kommentarhoved kom med i Supabase 9. august, men begge udfald fører til samme handling (indsæt brødteksten igen), så spørgsmålet var ikke en beslutning. **Det, der VAR en fejl, er rettet:** `confirm-signup.html` havde advarslen mod at paste sit eget hoved ind, mens `recovery.html` og `MAIL.md`s trin 4 ikke havde den — to skabeloner med samme risiko og én advarsel. `templates.test.js` kræver den nu af enhver skabelon plus at brødteksten faktisk begynder ved `<table role="presentation">`, som advarslen henviser til. ✅ **Kuren er kørt og efterprøvet samme dag:** brødteksten er indsat på ny i Supabase, og "vis original" på en modtaget nulstillingsmail viser en `text/html`-del, der begynder direkte på `<table role="presentation">` — ingen `<!--`. Mailen blev sendt EFTER indsættelsen, så den måler den nuværende skabelon; 9.–13. august forbliver ubesvaret, hvilket er beslutningens egen konsekvens og ikke en mangel. **Samme mail bestod `I10`s bevis 4, som ingen var gået efter** — `redirect_to=https://app.leagly.app/` bygger Supabase af Site URL, så trin 4 er nu bevist frem for meldt, og af trin 8 mangler kun `?liga=`-linket, et login og `og:url`. **`A47` — afgjort frem for aflæst** (ejerens valg): de to `.vercel.app`-adresser i redirectet er accepteret som hele listen, fordi de er Vercels standardsæt for et team-projekt og projektet aldrig er omdøbt. **Rækkens egen risiko var sat for højt** — en overset alias ville ikke "knække", den serverer samme deployment og redirigerer bare ikke, så prisen er en ikke-kanonisk origin. **Mønsteret er værd at tage med til næste tier:** en række, der beder om en aflæsning, skal først spørges, om svaret ændrer handlingen (`A45`), om det kan gemmes i stedet for hentes (`A46`), eller om det kan afgøres (`A47`). **Backloggen er 40 → 38.** Intet skal køres i Supabase — `job_runs.detail` er `jsonb`. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 13. august 2026 (fjerde kørsel):* (✅ **Hjemmesidens andet udkast er merget, og `B30` var betingelsen.** Udkastet lå uden for repoet, fordi det ville have gjort CI rødt: `saelgesaetning.test.js` (`G97`) kræver, at `site/index.html`s `description` **begynder** med samme sætning som `index.html`s, og udkastets nye ordlyd gjorde det ikke — rækkefølgen var derfor bindende, omformulér først og merge bagefter. **Sælgesætningen er nu fodbold-eksplicit** — *"Gæt resultaterne af ugens fodboldkampe mod dine venner. Opret en liga, tip kampene, og se hvem der er bedst."* — og står i alle fem filer, vagten navngiver. **Turneringsnavnene blev holdt UDEN for den**, fordi sætningen også skal kunne stå på login-skærmen, i et link-preview og i README'en, hvor en liste over ligaer enten er forkert i morgen (`B28`/`B32`) eller irrelevant; `leagues`-tabellen er sandheden om, hvilke turneringer der findes. **Vagten talte fem filer og fandt to mere, den ikke selv talte:** `src/screens/Auth.test.jsx` påstod tre gange en hårdkodet delstreng af sætningen, og vagtens **eget kommentarhoved** citerede ordlyden ordret — begge er nu væk (testen læser ankeret, kommentaren beskriver frem for at citere). Det var første omformulering siden vagten blev bygget (`G97`, 12. august 2026), altså første gang den blev afprøvet på det, den findes til. **Hjemmesiden selv:** turnerings-sektion med live-mærkater, ugerytmen skrevet om fra syv dagskort til tre kort (sandere — kampene kan ligge alle ugens dage), ægte Story Engine v3-formuleringer i story-kortene, og SEO-metadata der **lukker `I17`** (canonical, favicon, apple-touch-icon, theme-color, `og:`/`twitter:` på alle fem sider, `og:image` 1200×630, `robots.txt`, `sitemap.xml`). **`I9` er skrumpet** til det, der kræver en publiceret side. **Backloggen er 42 → 39:** `B30`, `B31` og `I17` lukket og slettet; `G103` flyttet fra Tier 6 til Tier 5, fordi dens udløser — at udkastet blev merget — er indtruffet. **Sitet er stadig ikke publiceret**, og `I8`s rest er ejer-godkendelse af copy, domænets trin 2 og selve publiceringen. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 13. august 2026 (tredje kørsel):* (✅ **`I10` er udrullet: `#196` er merget, og appen svarer på `app.leagly.app`.** Bevis 1 og 3b er bestået 14:57 UTC — `prediction-champ.vercel.app/` svarer `308` med `Location: https://app.leagly.app/`, og `/api/sync-live` svarer `401` og **ikke** 308. **Det andet bar et led mere, end beviset bad om:** 401'eren havde `Content-Security-Policy: font-src 'self'` med, altså appens EGEN header fra `vercel.json`, så kaldet nåede helt frem til funktionen — et 401 alene beviser kun "ikke redirigeret", headeren beviser "ramte det rigtige". **Runbogen har fået et PowerShell-forbehold, og det er ikke kosmetik:** beviserne køres fra PowerShell, hvor `curl` i 5.1 er et alias for `Invoke-WebRequest`, og hvor den nærliggende rettelse (`-Method Head`) **følger redirectet af sig selv** og svarer 200 — altså en kontrol, der består ved at måle noget andet, end den tror. `curl.exe` med endelsen virker i begge versioner og følger ikke redirects. **Tilbage af `I10`:** resten af trin 8 (det gamle `?liga=`-link hele vejen, et login, en nulstillingsmail, `og:url`) og `B21`s GitHub-omdøbning. **Stadig uaflæst:** `prediction-champ-predictor-champ.vercel.app` er aldrig spurgt, og aliaslisten er aldrig aflæst i Vercel → Domains. Trin 2 og dermed `leagly.app` er fortsat gated af `I8`.)

*Forrige opdatering, 13. august 2026 (anden kørsel):* (✅ **`I10`s trin 1, 3 og 5 er kørt af ejeren — `app.leagly.app` svarer, Turnstile-værtsnavnet er udvidet, og spærringen på koden er ophævet.** Repo-halvdelen (trin 6 + 7) kan dermed udrulles. **Trin 4 (Supabase Site URL) bør køres først:** det var tidligere den farligste rækkefølge, fordi destinationen ikke fandtes — nu er det omvendt, for står Site URL på den gamle adresse efter udrulningen, skal hvert nulstillingslink reddes af redirectet. Det virker (en 308 uden fragment i `Location` bevarer browserens eget `#access_token=…`), men er et led at være afhængig af på netop den rejse, hvor brugeren i forvejen er låst ude. **Registrene skelner nu "meldt" fra "bevist"** — [`DOMAENE.md`](./DOMAENE.md) og [`OPRETTELSE.md`](./OPRETTELSE.md) noterer handlingen som udført af ejeren, mens beviset udestår; udgående HTTPS er spærret i arbejdsmiljøet, så ingen af adresserne er blevet spurgt af en maskine. **Trin 8 er dét, der gør de to til det samme.** Tilbage efter udrulningen: trin 4, trin 8 og `B21`s GitHub-omdøbning; trin 2 er stadig gated af `I8`.)

*Forrige opdatering, 13. august 2026 (første kørsel):* (🛑 **`I10`s repo-halvdel er skrevet og må IKKE udrulles endnu — `app.leagly.app` findes ikke.** Domæneflytningen er otte trin, og kun to af dem ligger i repoet: **trin 6** (redirect af de to gamle `.vercel.app`-værtsnavne i `vercel.json`, 308, betinget på den GAMLE host) og **trin 7** (`B21`s tekstdel: 23 CTA'er i `site/` + README'ens live-link → `app.leagly.app`). Dertil to faldback-adresser i `vite.config.js` og `api/invite-preview.js`, som kun rammes uden for produktion. **De seks andre trin er dashboards** — Vercel, DNS, Supabase Site URL, Turnstile-værtsnavn — og kan ikke køres herfra; udgående HTTPS var desuden spærret, så **ingen af adresserne er blevet spurgt**. **Rækkefølgen er bindende: trin 1 → trin 3 → merge.** Udrulles koden før `app.leagly.app` svarer, sendes hver eneste bruger til en adresse, der ikke findes — den værste af runbogens tre rækkefølgefejl og den eneste, der rammer alle på én gang. **`B21` er dermed skrumpet til ét dashboard-skridt** (omdøb GitHub-repoet); tekstdelen kørte med `I10` netop for ikke at skifte de samme 23 links to gange. **Dagens beslutning ligger i én regex:** redirectet undtager `/api/` (`/:sti((?!api/).*)` frem for runbogens `/(.*)`), fordi de ni cron-jobs kalder `https://<app>/api/…` med `x-sync-secret`, og et 308 til et nyt værtsnavn kræver, at cron-job.org både følger redirectet og gensender headeren — to uefterprøvede antagelser, hvis fejl viser sig som at live-resultater holder op med at komme. Undtagelsen gør flytningen af jobbene valgfri frem for samtidig. ⚠️ **Reglen kan pr. konstruktion ikke efterprøves før produktion:** `has` er betinget af produktionsværtsnavnet, et preview har sit eget, og CI kører ikke Vercels router. Runbogen har fået **bevis 3b** til gengæld — `/api/sync-live` på den gamle adresse må ikke svare 308. **Registerets svageste påstand står nu skrevet som sådan:** listen over gamle `.vercel.app`-aliasser er aldrig aflæst i Vercel → Domains. Se [`DOMAENE.md`](./DOMAENE.md), [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 12. august 2026 (syvende kørsel):* (**`A43` er afgjort og bygget: læsefladen smalnes — `profiles` på KOLONNER, deltagerlisten på RÆKKER.** Rækken stillede ét spørgsmål til to tabeller, og svarene er modsatte. **`profiles`:** `authenticated` må herefter læse nøjagtig tre kolonner (`id`, `display_name`, `anonymized_at`), mens `read profiles`-policyen er **urørt** — Rating, Månedsligaen og Championship (`scope='ALL'`) publicerer hvert visningsnavn med vilje, og en rækkepolicy ville tømme hver eneste stilling. Det, der lukkes, er `is_admin`, `last_seen_at`, `created_at` og `display_name_changed_at`, som fulgte med i ethvert REST-kald. **`competition_participants`:** hele appens sociale netværk kunne hentes med ét kald; reglen er nu *din egen række, eller en række i en konkurrence, du kan se*. **Rækken pegede på ét kaldested, der skulle rettes først — der var fire:** ud over `select=*` i `competitionState.js` læser `App.jsx` og `sikrProfil()` ens EGEN række bredt (og `MainApp` afgør admin-fanen på `profile.is_admin`), mens Admin → Brugere læser præcis de fire lukkede kolonner. De går nu gennem `my_profile()` og `admin_profiles()`. 🔴 **Rækkens dyreste linje er ikke en policy, men en RETURNING-klausul:** `Prefer: return=representation` returnerer alle kolonner uden et `select=`, og `returning` kræver LÆSE-privilegiet på hver af dem — så en upsert eller et navneskift fejler med `permission denied`, selvom skrivningen er lovlig. Samme mekanik som `#55`, bare på privilegier frem for på policies. 🔴 **Og deltagerpolicyen måtte hverken slå op i sin egen tabel eller i `competitions`:** de to ville pege på hinanden og give `42P17 infinite recursion` — en fejl, der **også** slukker for `select * from competitions`. Reglen bor derfor i `is_competition_visible()`, som `competitions_select_involved` nu kalder med. 🔴 **Og det, ingen gennemlæsning kunne finde: TRE POLICIES læste `profiles.is_admin`** — `job_runs_read_admin` og de to på `matches`. Et policy-udtryk evalueres med den kaldende rolles privilegier, så de ville FEJLE med `42501` frem for at filtrere, og **Admin → Drift ville være brækket for alle, også for en administrator.** Reglen bor nu i `is_platform_admin()`. Fundet af `write_surface`-testen kørt mod et skema, hvor migreringen allerede var kørt — §13-reglen om at efterprøve fra begge sider af dumpet, som samme sweep også brugte til at rette `competition_matches_read.sql` FØR eksporten frem for en uge efter. **To ting er no-ops ved konstruktion** (`user_id = auth.uid()`-leddet og omskrivningen af konkurrence-policyen), og testen siger det frem for at lade en læser tro, de er dækket — otte af ti mutationer fanget, de to sidste er netop dem. ✅ **Begge migreringer er kørt i staging og produktion 12. august 2026, og alle ti trin i runbogen er igennem** ([`UDRULNING-A43.md`](./UDRULNING-A43.md)) — `#59` før mergen, `#60` efter deployet, og **ingen bruger oplevede et vindue, hvor appen ikke kunne logge ind.** Runbogen er dermed anden forekomst af `A40`s todelte form, altså et mønster frem for en engangsopfindelse. 📈 **Trin 5 var en MÅLING og ikke et flueben, og svaret er 2,2 ms i alt** for en liga med otte konkurrencer — en ØVRE grænse for policyens pris, altså under tærsklen, så liga-sidens deltagerantal skal ikke bygges om. 🔶 Tilbage står skema-eksporten, som mangler `#56`–`#60`. Backloggen er 30 → 29; `A44` står tilbage som rækkens ærlige rest og er en produktbeslutning. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 12. august 2026 (sjette kørsel):* (**`A43` er læst igennem og delt i to — ingen kode rørt, ingen beslutning truffet.** Rækken foreslog `A40`s vej for begge tabeller: afgræns til de brugere, læseren deler en liga med. Gennemlæsningen af kaldstederne viste, at **de to tabeller har modsatte svar.** For `competition_participants` holder forslaget — deltagerlisten er ikke publiceret nogen steder, og hvert eneste læsekald i klienten er allerede filtreret på `competition_id`/`user_id`, så en policy ville ikke knække noget; den skal blot gå gennem en `security definer`-helper for hverken at slå op i sin egen tabel eller i `competitions`, hvis egen læsepolicy peger tilbage. For `profiles` ville forslaget **tømme hver eneste stilling** og samtidig skjule det ene felt, produktet selv publicerer — Rating og Championship (`scope='ALL'`) viser alle visningsnavne til alle med vilje. **Problemet dér er kolonner og ikke rækker:** `is_admin`, `last_seen_at`, `created_at` og `display_name_changed_at` følger med i ethvert kald, selvom klienten kun læser `id,display_name,anonymized_at` om andre. Værktøjet lå allerede i repoet i modsat retning — `username_change.sql` gav kolonne-grants på SKRIVNING, netop fordi en policy kan afgrænse rækken og ikke kolonnen — og samme greb på læsning lukker halvdelen uden at røre en policy. Det gør til gengæld fire kaldesteder til fejl, der skal rettes FØR og ikke efter: `select=*` i `competitionState.js:23`, de to `select=*` på ens EGEN række (`App.jsx:97` og `profile.js:46`, hvor `is_admin` bærer admin-fanen) og Admin → Brugere, som læser præcis de fire kolonner, der skal lukkes. **Rækken efterlader `A44`:** uanset hvor stram policyen bliver, publicerer den globale rating hvert navn til enhver med en konto — så det er en produktbeslutning om, hvad tavlen skal VISE, og ikke en adgangsregel. **Udløseren er sprunget imens:** `B26` bar den samme og blev kørt tidligere samme dag, så begge halvdele er forsinkede og ikke ventende. Rækkefølgen er `A40`s additive → udrul → indsnævrende, og den ene omkostning, der skal måles i staging før policyen låses, er `groups.js:52`, som henter deltagere for alle konkurrencer i alle brugerens ligaer. Se [`BACKLOG.md`](./BACKLOG.md).)

*Forrige opdatering, 12. august 2026 (femte kørsel):* (**`B26` er kørt helt igennem: bot-værn og e-mailbekræftelse står nu foran oprettelsen.** Alle otte trin i [`OPRETTELSE.md`](./OPRETTELSE.md) er kørt samme dag og alle ni beviser bestået — inklusive de to, der ikke kan snydes: et signup-kald uden kvittering afvises med `captcha_failed`, og en rigtig konto er oprettet, bekræftet gennem mailen og landet i appen **med sit brugernavn**, hvilket er den eneste kontrol, der rører `sikrProfil()`. **Ingen kode er rørt;** klientsiden blev bygget 10. august og har ligget inert siden, og dagens arbejde er de knapper uden for repoet, den ventede på. **Det, der spærrede, kunne afprøves gratis:** at widgetens værtsnavn accepteres, aflæst med `window.turnstile.getResponse()` mens Bot Protection stadig var slået fra — hele forskellen på 10. augusts hændelse og en knap, der kan trykkes tilbage. 🔴 **Runbogen fejlede tre gange på FORMEN og ikke på indholdet, og det er dagens lære.** Kontrollen bad om at se en grå linje FORSVINDE; i Managed-tilstand løses udfordringen på ca. et sekund, så linjen nås aldrig at ses, og første læser aflæste i stedet widget-boksen, der bliver stående med "Succes!", og meldte kontrollen fejlet — mens tokenen var udstedt. **Et fravær er et dårligt bevis, når det indtræffer hurtigere end øjet.** Dertil to `curl`-kald, der var bash-only og fejler i PowerShell på tre måder, som alle ligner et svar fra Supabase, og et "indsæt indholdet af filen", der tog skabelonens kommentarhoved med ud i mailens kilde hos hver modtager. **Fire dokumenter beskrev en verden, dagen lukkede** — `DOCUMENTATION.md` §12 (*vilkår, der ikke laves om*) sagde "alle kan oprette konti uden godkendelse", `MAIL.md`s to registerrækker, og skabelonens eget hoved, der sagde "IKKE I BRUG ENDNU", mens den blev sendt til rigtige modtagere. `G99`s lære en gang til. **Udløseren var ikke sprunget:** rækken er kørt i forvejen efter ejerens valg, fordi en uafprøvet runbog er dyrere at køre første gang samtidig med, at fremmede står i døren — begrundelsen står i [`DECISIONS.md`](./DECISIONS.md). 🔴 **`A43` står nu ALENE med udløseren og bør afgøres FØR den åbne deling:** et bot-værn hæver prisen på en fremmed konto uden at fjerne den, og `profiles`/`competition_participants` er stadig en komplet brugerliste for enhver indlogget. Backloggen er 31 → 30. Rollback er én kontakt pr. halvdel. Se [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 12. august 2026 (fjerde kørsel):* (**`G98` er kørt: `groups`' SELECT-policy har ikke længere et "eller".** `#55`s `or created_by = auth.uid()` er fjernet med [`#58 groups_select_member_narrow.sql`](../sql/groups_select_member_narrow.sql), så reglen er `is_group_member(id)` alene — **du kan se en liga, hvis du er medlem af den, punktum.** Prisen, leddet kostede, er betalt tilbage: en opretter, der har forladt sin egen liga, kan ikke længere læse den og dens `invite_code`. **Rækken var en ventetid og ikke et stykke arbejde:** `G95` (`create_group()`, `#57`) fjernede årsagen 12. august ved at skrive ligaen og opretterens admin-række i ÉN transaktion som ejer, men leddet bar den GAMLE klients `insert … returning`, indtil deployet var ude. **Udløseren var derfor en afprøvning og ikke en migrering** — ejeren oprettede en liga i produktionen efter udrulningen. **`#55` er nu den tiende fil, der ikke må gen-køres,** og den eneste, hvor "kør den nyere bagefter" ikke altid er svaret: den ER tilbagerulningen, hvis en gammel klient mod forventning dukker op igen. ✅ **Migreringen er kørt i staging og produktion 12. august 2026**, før mergen; 🔶 tilbage står skema-eksporten, som mangler `#56`, `#57` og `#58`. **Fundet inde i rækken, og det er dens vigtigste linje:** `sql/tests/create_group.sql`s negative kontrol hentede en ligas id med et opslag i `groups` som den indloggede bruger, og det opslag bliver tomt efter `#58` — kontrollen ville være blevet et **tavst no-op** i samme sekund skema-eksporten kørte, grøn uden at måle noget. Tredje gang samme fælde er stillet (`G94`, `invite_lookup.sql`), så §13-reglen er skærpet: **en test må ikke læse sin før-tilstand af snapshottet — heller ikke indirekte, gennem et opslag, en policy kan lukke.** Begge tests er kørt mod en rigtig PostgreSQL 16.13 fra BEGGE sider af dumpet sammen med de tolv andre skema-indlæsende tests, og efterprøvet med fem mutationer. Backloggen er 31 → 30. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Tidligere opdatering, 12. august 2026 (tredje kørsel):* (**`G100` er kørt: `G96`s regel har fået en vagt mod PRODUKTIONEN.** Vagten var en CI-påstand mod `sql/schema.sql`, og dumpet er først sandt efter en eksport — migreringerne køres i hånden i SQL-editoren, og eksporten er en ugentlig mandagskørsel, så en funktion kunne stå åben for `anon` i produktionen i op til en uge, uden at nogen påstand nogen steder var rød. Ny femte kontrol i `sql/checks/` ([`anon_routine_reach.sql`](../sql/checks/anon_routine_reach.sql)) med test og CI-trin — **og et sted, der kører den:** `job-heartbeat.yml` hver halve time. Uden det sidste ville rækkens problem stå uændret, for et CI-trin efterprøver kontrollen og ikke produktionen. Den melder **begge** retninger: en rutine for meget er en dør uden login, en for lidt er oprettelsen af en konto, der ikke virker. **Fundet undervejs:** `revoke … on all functions` dækker ikke PROCEDURER (efterprøvet mod 16.13) — der findes nul i dag, men både `#56`s trin 2 og den eksisterende tests `prokind = 'f'` ville være blinde for den første; kontrollen filtrerer derfor ikke på `prokind`. Én af tolv mutationer slap igennem testens første udgave og gav den en påstand mere. **Intet skal køres i Supabase.** Backloggen er 32 → 31. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Tidligere opdatering, 12. august 2026:* (**Backloggens Tier 5 og Tier 2 er kørt — `G95`, `G96`, `G97` og `G99`.** **`G99` var den billigste og den mest ubehagelige:** `DOCUMENTATION.md` §18 beskrev `groups` som læsbar for enhver indlogget bruger — `A40`s hul — **ti linjer under `A40`s egen rettelse i samme afsnit**, og fire andre påstande i samme sætningsblok var overhalet af `A21`, `liga_admin.sql` og `G63`. Ingen kode rørt; men de fire vigtigste af dem er adgangsregler, altså dét, man slår op for at være sikker. Fællesnævneren var, at ingen af dem var en fejl i dag: tre steder, hvor det, der holdt, holdt af én grund og ikke af to. **`G95`:** oprettelsen af en liga er nu ÉN skrivning (`create_group()`, migrering [`#57`](../sql/create_group.sql)) frem for to PostgREST-kald, så vinduet, hvor en liga kunne stå tilbage uden medlemmer, er lukket. **`G96`:** `anon` kan nøjagtig to funktioner i `public` ([`#56`](../sql/anon_grants_functions.sql)) — og rækken beskrev kun den halve opgave: `anon` kommer ind ad to veje, og den ene, PostgreSQLs indbyggede EXECUTE til PUBLIC på hver ny funktion, **kan ikke lukkes ved kilden**. Følgen er en regel, hver ny migrering skal følge, og en vagt, der måler hele skemaet ([`DECISIONS.md`](./DECISIONS.md)). **`G97`:** sælgesætningen stod fem steder og ikke tre; en test holder dem nu i trit uden selv at skrive en sjette kopi. **Rækken efterlader `G98`** — `#55`s policy-led kan først fjernes, når klienten er udrullet. ✅ **Begge migreringer er kørt i staging og produktion 12. august 2026**, før mergen; 🔶 tilbage står skema-eksporten. **Tidligere samme dag: `I10` er afgjort — appen flytter med på domænet, og indbakken er ryddet.** **Rækken havde et hul, ingen af dens to muligheder kunne dække:** `I10` spurgte "eget domæne eller en sti på Vercel-projektet", men begge svar handlede om HJEMMESIDEN, og appens adresse var overladt til `B21`, som løste den ved at omdøbe Vercel-projektet — altså til endnu en `.vercel.app`. Det er den forkerte adresse at lade blive, fordi invitationslinks bygges af `window.location.origin`: det er appens URL og ikke hjemmesidens, brugerne deler indbyrdes. **Valgt: `leagly.app` til hjemmesiden, `app.leagly.app` til appen**, begge på Vercel som to projekter — ét projekt med en rewrite blev valgt fra af den grund, dagens anden rettelse lige havde demonstreret: appen er en SPA på roden, og en rewrite på en sti, hvor der ligger en fil, fyrer aldrig. **`B21` mistede sin farligste halvdel:** Vercel-projektet omdøbes IKKE — med et custom domain er projektnavnet dashboard-kosmetik, og de gamle adresser bliver i stedet stående som `permanent: true`-redirects betinget på værtsnavn, så ingen delte `?liga=`/`?join=`-links dør. Tilbage i `B21` er de 23 CTA'er, README'ens live-link og GitHub-omdøbningen. **Intet af det er kørt, og intet af det rører koden endnu:** CSP'en bliver stående (to origins deler ikke headere), og `og:`-adressen stempler sig selv om via `VERCEL_PROJECT_PRODUCTION_URL` — den antagelse er skrevet ind som et bevis, der skal aflæses, ikke som en sandhed. **Udførelsen har fået sin egen runbog: [`DOMAENE.md`](./DOMAENE.md)** — register, den rækkefølge der kan gøre skade (Site URL, Turnstile-værtsnavn, redirect uden `has`), otte trin, fem beviser og en fejlfindingstabel; `MAIL.md` og `OPRETTELSE.md` peger nu på den. 🔴 **Udløseren for hjemmesidens halvdel er stadig `I8`**, men appens halvdel bør køres tidligt: en installeret PWA er bundet til sin origin, så prisen stiger med brugertallet. **Backloggens indbakke er ryddet samme dag:** syv rå linjer blev til to åbne beslutninger (`A42` browser i CI, `A43` læserettigheder på `profiles`/`competition_participants`), tre stykker teknisk gæld (`G95`–`G97`, som fylder det hidtil tomme Tier 5) og to ideer (`I20` QR-kode, `I21` OG-billede pr. liga). Listen er 26 → 33 — ingen række er åbnet af nyt arbejde; det ubyggede er skrevet ned frem for husket. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 11. august 2026 (tredje kørsel):* (🔴 **Ingen kunne oprette en liga — rettet med `#55`, som kan køres med det samme.** Fundet af ejeren under afprøvningen af `I7`: "Opret liga" svarede `new row violates row-level security policy for table "groups"` i både staging og produktion, for alle brugere. **Årsagen var `A40`s `#53` fra samme dag, og fejlteksten pegede det forkerte sted hen** — INSERT-policyen var uskyldig. `db.insert` sender `Prefer: return=representation`, så PostgREST kører `insert … returning *`, og en RETURNING-klausul anvender **SELECT**-policyen på den nye række; `createGroup` skriver ligaen FØR sin egen medlemsrække, så `is_group_member(id)` var falsk. Samme fælde som den, `is_group_creator()` i `#52` blev bygget for, bare ét statement tidligere. **CI var grøn, fordi testens skrive-hjælper kørte uden `returning`** — den blindvinkel er nu lukket med `skriv_retur()` og påstand 10c, efterprøvet med en mutation. Rettelsen er ét led, `or created_by = auth.uid()`, og prisen (en opretter, der har forladt sin liga, kan stadig se den) er accepteret og skrevet ned. Den generelle regel — *en SELECT-policy afgør også, hvad man kan SKRIVE, så længe klienten beder om svaret tilbage* — står i [`DOCUMENTATION.md`](../DOCUMENTATION.md) §13 sammen med sweepet af alle fem `db.insert`-tabeller. Se [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 11. august 2026 (anden kørsel):* (**`I7` er leveret: invitationsflowet er gennemgået som én oplevelse.** Fem huller sad efter hinanden i den samme tragt, og de ramte alle den NYE bruger. **Linket havde intet udseende** — `index.html` havde kun `<title>`, så et invite-link i Messenger eller WhatsApp var en nøgen URL; nu er der OG-tags og et 1200×630-billede, bygget af `scripts/build-og-image.mjs` ud af det wordmark, der allerede lå ubrugt i `public/`, og adressen stemples ind ved build, så den følger med den dag `leagly.app` tages i brug. **Crawlere får desuden den ENKELTE invitation** via `api/invite-preview.js`; omskrivningen kræver både en kode og en crawler-agent, så et menneske pr. konstruktion ikke kan ende i funktionen — "fejl åben" er dermed en egenskab ved opsætningen. **Modtageren ser nu, hvad de er inviteret til, før de opretter en konto** (`invite_preview()`, migrering `#54`, åben for `anon`, men kun navn og medlemsantal — afvejningen mod `A40` er `A41` i beslutningsloggen). **Invitationen overlever omvejen over oprettelsen**, hvilket er dét, `B26` ellers ville have brudt. **Liga-invitationen har fået en afsender**, og de to del-tekster er nu den samme sætning. **Og tragten kan måles:** `invite_landed` er trinnet mellem sendt og accepteret, vist i Admin → Analytics. **`B20` blev bevidst ikke trukket ind**, men flowet er formet, så den kan sættes ind bagefter. **Et fund undervejs:** `anon` har EXECUTE på hver funktion i `public` via Supabases default privileges — `G50`/`G58` lukkede tabeller og sekvenser, ikke funktioner; intet hul i dag, men det gør funktionernes egen `auth.uid()`-vagt bærende, og testen måler den nu som adfærd. Backloggen er 27 → 26. Spec: [`features/invitationsflow-v1.md`](./features/invitationsflow-v1.md). Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 11. august 2026:* (**`A40` er udrullet i produktion, og hullet er lukket.** Begge migreringer er kørt — `#52` før frontend-mergen, `#53` efter — og rækkefølgen holdt: ingen bruger oplevede et vindue, hvor en invitation ikke kunne tages imod, hvilket var hele formålet med at dele den i to. Efterprøvet i produktionen med begge linktyper og med et opslag, der svarede fire nye policies, nul gamle, tre funktioner og nul ligaer uden medlemmer. **Enhver indlogget bruger kunne indtil i dag læse hver eneste liga i appen inkl. `invite_code` og melde sig ind uden en invitation;** nu kræver både læsning og tilmelding, at man er med i forvejen — eller fremviser koden. Samme deploy bar `B29`s navneskift, `A26`s godkendte holdpar, fire lukkede beslutninger, en `Modal`-fokusfejl der ramte hvert tekstfelt i en dialog, og `selectIn()`, som løste at Hjem og Tip ikke kunne hente kampe, når id-listen oversteg URL-loftet (778 kampe ≈ 29 KB). **Runbogen [`UDRULNING-A40.md`](./UDRULNING-A40.md) bliver stående som skabelon** for den næste todelte migrering. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 10. august 2026 (fjerde kørsel):* (**`A40` er bygget — invitationskoden er hemmeligheden igen.** Rækken blev åbnet af revisionen efter `B29` samme dag og lukket igen frem for at vente på sin udløser: `B26` (åben oprettelse) er dét, der gør fremmede konti mulige, og en fejl i join-flowet ville da ramme rigtige nye brugere frem for de enogtyve, der allerede er inde. **`groups_select_all` var `using (true)`**, så hver indlogget bruger kunne læse hver liga inkl. `invite_code`, og et id var nok til at melde sig ind. Reglen er nu: *du kan se og tilmelde dig, hvis du allerede er med — eller hvis du fremviser koden.* Opslaget og tilmeldingen er flyttet til `invite_lookup()` og `accept_invite()`, og `A8`-reglen (konkurrence i en liga melder ind i begge) bor dermed i databasen frem for i hvert kaldssted. **Testens anden halvdel fandt to ting, ingen læsning havde fundet:** insert-policyen på `group_members` spørger `groups`, som netop var blevet smalnet, så **hele oprettelsen af en liga var brudt** indtil `is_group_creator()` blev lagt ind — og en kommentar påstod, at invarianten ville AFVISE en deltagelse uden medlemskab, hvor den i virkeligheden UDFYLDER den. Elleve mutationer, alle fanget. 🔴 **Migreringen er delt i to** — `#52` (funktioner, sikker når som helst) → udrul → `#53` (policies) — fordi "kør samtidig" ikke er en instruks, et menneske kan følge. Runbog: [`UDRULNING-A40.md`](./UDRULNING-A40.md). Backloggen er 28 → 27. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 10. august 2026 (tredje kørsel):* (**`sql/username_change.sql` er kørt i staging og produktion — og revisionen bagefter siger, at fladen er dækket.** `B29`s fejlklasse (en policy afgrænser rækken, ikke kolonnen) er efterprøvet på hele skemaet: af de 29 objekter med `grant all` til `authenticated` er ti helt lukkede af RLS uden en eneste skrive-policy, resten er scopet til `auth.uid()`, `created_by`, `is_admin` eller `is_group_admin`, og alle seks views, rollen kan nå, er `security_invoker=on` og ikke auto-opdaterbare. **Svaret er lagt ned som `sql/tests/write_surface.sql`** frem for som et notat — et svar fra en bestemt dag er ikke en kontrol. Den måler RÆKKEANTAL og ikke fravær af en fejl, og dét er ikke pedanteri: første måling meldte fem huller, som alle var falske, fordi RLS uden en policy bare skjuler rækkerne. **Ét fund, `A40`, og det er en anden fejlklasse:** `groups_select_all` er `using (true)`, så enhver indlogget bruger kan læse hver liga inkl. `invite_code` og melde sig ind. Policyen gør præcis, hvad der står i den — spørgsmålet er, om det er den rigtige regel, og rettelsen kræver, at join-flowets kodeopslag flyttes til en `security definer`-funktion. Rækken står med `B26`s udløser og **foran den**. Backloggen er 27 → 28. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 10. august 2026 (anden kørsel):* (**Fem ventende punkter lukket, to bygget — og et rettighedshul fundet undervejs.** Ejeren afgjorde fire spørgsmål, der aldrig ventede på kode: `A5` (emojis bliver i historie-kortene), `A27` (`competitions.rules` bliver stående, i tilfælde af at pointvariation bliver aktuelt), `A32` (aflæsninger i produktion er ejerens arbejde — der bygges ingen vej udenom) og `A36` (kontolukningens ligaregel står, som den er; det, der manglede, var en kommentar i `sql/checks/league_admin_coverage.sql`, som stadig kaldte spørgsmålet åbent). **`A26` er kørt:** `ambiguousTeams` filtreres nu mod `GODKENDTE_HOLDPAR`, så kontrollen igen kun melder det, ingen har set på, og `ambiguousKnown` er tallet, der viser, at filteret bider. **`B29` er bygget** — brugernavnet kan skiftes fra ens egen karriereprofil — **og rækken fandt noget, den ikke handlede om:** `grant all on profiles to authenticated` + policyen `update own profile` lod enhver indlogget bruger sætte sin egen `is_admin` og dermed passere admin-vagten i fem funktioner; en policy afgrænser rækken, ikke kolonnen. Efterprøvet mod `sql/schema.sql`: `UPDATE 1`. Rettelsen er kolonne-privilegier og ligger i samme migrering, før funktionen. 🔴 **`sql/username_change.sql` (#51) skal køres i Supabase FØR frontend-mergen.** Backloggen er 32 → 27. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 10. august 2026:* (**Opret-flowet spurgte aldrig, hvornår konkurrencen skulle begynde — og feltet "Kampe pr. runde" lod ét tilfældigt tidspunkt bestemme resten.** Puljen blev hentet fra `nu` og frem, så startrunden var en konsekvens af, hvornår man trykkede: en Quick League oprettet søndag aften fik ugens to resterende kampe som hele sin første runde, og skærmen sagde det ikke — den skrev "(1 i nærmeste runde)". **Samme tal var samtidig feltets `max`**, så med én kamp tilbage kunne man kun bede om én kamp i et felt, der gælder alle seks runder frem, også dem med fyrre kampe at vælge imellem. **Nu er der et valg og en nævner:** de fire typer med en startrunde (Quick League, Quick Pick, Ugens kupon, Custom/periode) spørger indeværende eller ny runde, og vælges indeværende, står der *"5 af 6 kampe i indeværende runde er allerede i gang eller spillet"*. Nævneren krævede et opslag, puljen af kommende kampe per definition ikke kan lave — `loadCurrentRoundMatches` henter hele runden på `round_key`. **Loftet på kampantallet er nu teknisk** (`MAX_MATCHES_PER_ROUND` = 50); udbuddet er oplysning, for Quick League som spændet over de valgte runder ("4–38 kampe pr. runde"). **Læren er `G35`s, en tredje gang:** et sandt tal uden nævner kan ikke bruges til at beslutte noget — hverken "0 kampe" i turneringsvælgeren eller "1 i nærmeste runde" her. **Arbejdet trak to ældre fejl med sig ud:** de tilfældige typer kunne trække kampe, der allerede var låst (puljen filtrerer på `kickoff_at >= nu`, låsen falder en time før kickoff), og **0-punkts-reglen brød sit eget løfte inde i en runde** — `filterFromNextUnfinishedRound` tog hele den første ikke-færdigspillede runde med, altså også dens spillede kampe, så en konkurrence oprettet onsdag gav point fra første sekund til den, der havde tippet tirsdagens kamp i en anden konkurrence. Begge er nu ét begreb, `filterTippable`, brugt hele vejen fra opret-skærmens pulje til materialiseringen af `full_season`, `team` og `time_range`; runde-reglen er slettet, fordi kamp-reglen indeholder den. Efterprøvet med to mutationer. **`B26` er uændret:** rækken blev delvist kørt 10. august og rullet tilbage — trin 1–3 i [`OPRETTELSE.md`](./OPRETTELSE.md) er bestået, begge knapper i Supabase står på fra, og 🔴 rækkefølgen er ufravigelig: `VITE_TURNSTILE_SITE_KEY` i Vercel FØRST, Bot Protection bagefter. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 9. august 2026:* (**Dagskortet ventede ikke på kampdagen — `G92`.** En bruger sendte et skærmbillede kl. 18.03 søndag: "Kampdag 09.08 · Dagens facit: 0 point" stod på Hjem, mens rundekortet lige under bar **LIVE** og **2/4 spillet**. **Reglen fandtes, men lå ét sted for lidt** — `match_day_complete()` blev spurgt i matches-triggeren og intet andet sted, og bagstopperen `generate_stories_catchup()` omgår triggeren pr. definition og filtrerer kun pr. KAMP, så **én færdigspillet kamp kvalificerede hele dagen**. `G90`s grace-vindue var det eneste, der havde maskeret det, og dets fjernelse blev begrundet med, at værnet lå i motoren — hvad det ikke gjorde. **Kortet var ikke bare tidligt, men forkert:** "1 kamp" er dagens kampe MED resultat, ikke dagens kampe, og "nr. 2 af 6" var en stilling, der stadig kunne flytte sig. **Værnet ligger nu i motoren**, efter sidste-dag-udgangen og før dens `delete`, så alle fem kaldere arver det frem for at skulle huske det. **Prisen er sagt højt:** en ufuldstændig dag får nu et kald, der returnerer straks, så bagstopperens dagsløkke ikke er selvafsluttende for en dag, der aldrig bliver færdig — loftet på 20 gør prisen endelig, samme svar som `G90` gav runde-løkken. **Testen er set fejle før den blev grøn:** uden rettelsen udgav bagstopperen ALENE syv kort for en uafsluttet dag. **Kontrollen var blind for netop denne fejl** og melder nu `KORT PÅ EN DAG, DER SPILLES`; et manglende kort er et udeblevet svar, et for tidligt kort er et forkert svar, brugeren allerede har læst. Ingen oprydning og ingen frontend-ændring — kortet for 09.08 retter sig selv ved dagens sidste slutfløjt. 🔴 **`sql/story_engine_v3.sql` (#47) skal køres i Supabase — produktion og staging — og INGEN andre, særligt ikke #38.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 8. august 2026:* (**Det opdigtede klokkeslæt har fået en markør — `G85`.** Football-data.org sender et opdigtet klokkeslæt for Premier League, Primera División og Serie A, og der er intet felt i svaret at kende det på. **Kandidat B er valgt**, og rækkens egen påstand om låsen holdt ikke: `match_lock_at()` og `lockAtOf()` regner den ved HVER læsning, så en rettet tid retter låsen af sig selv — skaden er displayet af fjerne kampe, ikke tipsvinduet. Det tog hastværket ud af kandidat A og efterlod dens pris: efterårspladsholderen ER turneringens typiske anspilstid, så en tabel over pladsholderværdier ville have markeret ægte kampe på netop de klokkeslæt, de rigtigt spilles på. **Reglen er tre trin og ét lånt tal:** en trigger gemmer den forrige `kickoff_at`; bærer mindst tre kampe i samme sæson samme forrige UTC-klokkeslæt (`G84`s gulv, ikke et nyt), er det en indlært pladsholder; turneringens øvrige kampe på det klokkeslæt markeres, og markeringen rydder sig selv. **Det største valg stod ikke i rækken:** `kickoff_tbd` gør tre ting, og kun den ene var ønsket, så markøren er ny og **display-only** — låsen, RLS og påmindelserne er urørte, og det er en påstand i testen frem for en hensigt i en kommentar. `G84`s kontrol var grøn for præcis de ramte turneringer og melder nu også `ALLE UBEKRAEFTEDE`. **14 af 14 mutationer fanget efter rettelse; to slap i første omgang, og begge huller var i påstanden.** På skærmen bliver tiden stående med et `~` foran og en tegnforklaring i dagsoverskriften — en tid, man kan planlægge efter, er bedre end ingen tid, så længe det står, at den ikke er bekræftet. Backloggen er 33 → 32. 🔴 **`sql/matches_kickoff_uncertain.sql` skal køres i Supabase — produktion og staging — og FØR næste heartbeat-kørsel**, fordi kontrollen nu læser `matches.kickoff_uncertain` hvert 30. minut. Adfærdsændring ved kørsel: ingen. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 8. august 2026 (aften):* (**Årsagen blev fundet — af sporet, `A38` byggede.** Instrumenteringen blev afprøvet i staging, og dens **første** rigtige kørsel svarede `ok = false · error = "UPDATE requires a WHERE clause"`. Det er `pg_safeupdate`, som Supabase indlæser via `session_preload_libraries` på rollen `authenticator` — den, PostgREST forbinder med — mens SQL-editoren forbinder som `postgres` og ikke indlæser den. Story Engine v3's dagsmotor havde én `update` uden `where`, så den **virkede hver gang et menneske kaldte den i hånden og fejlede hver gang triggeren gjorde**, tavst bag exception-guarden. Det forklarer hele §13.8's mysterium, inklusive de observationer, der pegede væk fra en fejl. **Det retter samtidig en slutning fra dagen før:** at rollen var irrelevant, fordi kæden er `security definer` — sandt om rollen, forkert om sessionen. Rettelsen **fjerner** sætningen frem for at give den en `where` (`where true` kan planlæggeren folde væk); `news_value` beregnes nu i en ydre `select`, og `story_engine_daily.sql` påstår de præcise tal mod en rigtig PostgreSQL. Fejlklassen er bevogtet af vagt 2 i `sql/migration_syntax.test.js`, og symptomet står i fejlfindingsloggen. 🔴 **`sql/story_engine_v3.sql` skal gen-køres i Supabase — produktion og staging.** Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026 (nat):* (**En brugerrapport blev til en større sag — `A38`.** Rundestoryen for 28.07 – 03.08 stod på Hjem og sagde *"Du er nu foran Lis04"*, mens STILLING viste Lis04 over brugeren. **To ure:** stillingen er live pr. kamp, mens dagskortet — afløseren — først skrives, når hele kampdagen er færdigspillet globalt. Kortet trækker sig nu også, når den indeværende runde er strengt senere end storyens og har mindst ét resultat; øjenbrynet bærer runden (`Rundens historie · 28.07 – 03.08`, udledt af `round_key` og derfor gyldig for eksisterende rækker), og tre regler er sat i datid. **Prisen er sagt højt:** top-slottet kan stå tomt, til dagskortet lander. **Undersøgelsen fandt noget større: v3's dagsmotor havde aldrig skrevet en eneste række i produktion**, og fire hypoteser døde undervejs — udrulningstidspunkt, fejl i motoren, `statement_timeout` (hele triggersætningen måler 141 ms) og kaldende rolle. **Årsagen kunne ikke fastslås og kan formentlig aldrig blive det:** `matches.updated_at` vedligeholdes ikke, og de rækker, der eventuelt blev rullet tilbage, er væk. **Derfor blev observerbarheden rettet frem for en formodet årsag** — triggeren skriver nu `job = 'story-engine'` i `job_runs` uden for guardens subtransaktion, og `sql/checks/day_card_coverage.sql` aflæser resultatet. To rettelser virker uanset årsagen: bagstopperens dagsløkke har mistet sit grace-vindue (med to betingelser, der gør den selvafsluttende), og den tidlige udgang i `generate_daily_stories` står nu før dens `delete`. 🔴 **Fire SQL-filer skal køres i Supabase, i denne rækkefølge:** `story_engine_v2.sql` → `story_engine_v3.sql` → `story_engine.sql` → `rating_trigger_optimization.sql`. Rækkefølgen er de to opsætningers egen: `sql/README.md` #38 siger "gen-kør #8 bagefter", og v3's opsætning siger v3 før gen-kørslen af `story_engine.sql` — altså skal `story_engine.sql` køres **sidst af de tre**. Alle fire er idempotente, og på den eksisterende produktionsdatabase findes alle objekter i forvejen, så rækkefølgen er et værn og ikke en hård afhængighed. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026 (sent aften):* (**Seks backlog-rækker kørt på én gang — `G80`, `G83`, `G79`, `G82`, `G77` og `G78`.** `G80`: `/api/*` findes ikke på `npm run dev`, og Vites history-fallback svarer `index.html` med **status 200**, så fem kaldesteder oversatte den samme observation til tre forskellige løgne — værst "Kontoen kunne ikke lukkes" om en handling, serveren aldrig blev spurgt om. `apiFetch()` i ny `src/lib/api.js` afviser nu et svar, der ikke er JSON, og siger hvad der er galt, forskelligt i udvikling og produktion. `G83`: **ratingen har fået sin første bagstopper.** `sql/recompute_derived.sql` regner alt afledt om i den bindende rækkefølge, og `docs/RESTORE.md` scenarie 1 — som foreskrev `--disable-triggers` og så tav — har fået kaldet skrevet ind. **Genberegningen er bevidst IKKE sat på et skema:** det er kontrollen (`sql/checks/rating_freshness.sql`), der kører hvert kvarter, og en gentagelse ville skjule både fejlen og dens årsag. Ni mutationer prøvet mod den; **to slap igennem den første udgave af testen.** `G79`: docs-SQL-kørslen måler nu, om `sql/schema.sql` dækker migreringernes 76 objekter, og skriver dommen øverst — **men fælder ikke kørslen**, fordi en migrering lovligt er forud for eksporten. Leverancen udløser den selv og siger det ved navn. `G82`: sæson-simulatorens 1.069 linjer har CI-dækning, otte påstande mod det rigtige skema; **testens første udgave meldte simulatoren ikke-deterministisk, og fejlen var testens.** `G77`: `analytics_events` ryddes efter 18 måneder af heartbeat'en — **intet nyt job**, arkitekturvalg #3 er uændret — og privatlivspolitikken siger det nu. `G78`: v3's scoringstal står ét sted, **og den migrering, rækken forudsagde, var ikke nødvendig** — JS-kopien var død kode, og `priority < 180` betyder allerede `news_value >= 45`. 🔴 **To SQL-filer skal køres i Supabase** (`recompute_derived.sql`, `analytics_retention.sql`), og den anden af dem før næste heartbeat-kørsel. **`G77` og `G78` havde ikke fået deres udløser** og blev kørt efter direkte anmodning; det er værd at vide, når man læser dem. Backloggen er 37 → 31. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026 (aften):* (**Football-data.orgs tidsfelt er aflæst, og `kickoff_tbd` virker kun ét sted.** `G81` bad om ét kald mod én turnering; det blev til fire turneringer og 1.446 kampe, læst direkte hos leverandøren uden om appen ([`reviews/football-data-kickoff-aflaesning-2026-08-07.md`](./reviews/football-data-kickoff-aflaesning-2026-08-07.md)). **Midnats-antagelsen var ufuldstændig, ikke forkert:** sand for Bundesliga (261 af 306 kampe), tom for Premier League, Primera División og Serie A — nul midnat i 1.140 kampe, og `SCHEDULED` på hver eneste, også dem med rigtige tider. De tre får i stedet et OPDIGTET klokkeslæt: turneringens typiske anspilstid lokalt frem til nytår, 12:00 UTC derefter. **Det er værre end fejlen fra 2.–6. august**, fordi den er stillere — dengang stod kampene synligt forkert uden tid, nu står de forkert MED en tid, og låsen sættes efter den. `G84`s kontrol er blind for præcis de ramte turneringer. **Fire formodninger blev forkastet undervejs** (`status`, midnat som universel regel, formen på en runde, `lastUpdated`), alle før der blev skrevet kode. `G81` er lukket og Tier 1 tømt for sjette gang; resten hedder `G85`, og **rettelsen er bevidst IKKE valgt endnu** — de to kandidater står i rækken. Ingen kodeændring, kun kommentarer og dokumentation. Se [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026 (nat, anden runde):* (**En lukket konto overdrager sin liga og forlader den.** Beslutningen (`A36` + `A37`) er truffet af produktejeren og bygget samme dag: ved kontolukning overdrages administratorrollen til det ældste levende medlem (`group_members.joined_at`), den lukkede konto forlader de ligaer, hvor den ikke har en deltagelse tilbage, og er der ingen medlemmer tilbage, bliver ligaen bare stående. **Rækkefølgen er selv reglen** — overdrag FØR framelding, ellers er oplysningen om, hvilke ligaer der mangler en administrator, væk. Frameldingen har invarianten som grænse: hvor der er spillet historik, bliver pseudonymet stående, men rollen degraderes til `member`. `sql/liga_admin.sql` bærer en engangs-backfill for de konti, der allerede var lukket, og den konvergerer — tredje kørsel er et no-op. **Fem mutationer prøvet mod funktionen, alle fanget**, og kørt mod både testens skema og `sql/schema.sql`. 🔴 **`sql/liga_admin.sql` skal gen-køres i Supabase** — der er ingen frontend-ændring at merge sammen med. Backloggen er 40 → 37, og Tier 1 er tilbage til ét punkt (`G81`): `G76` blev besvaret af ét `select deleted_at`-opslag, som viste, at hele kontolukningen allerede var kørt i produktion 5. august. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026 (nat):* (**Kontolukningen er prøvet af, og den fandt en frossen liga.** `G76`s databasehalvdel er kørt for første gang — ikke i staging, men mod `sql/schema.sql` i en lokal PostgreSQL 16 under ægte RLS — og den var ren: det personlige ryddes, historikken bevares, `A25`s framelding rammer præcis den konkurrence, der hverken er begyndt eller har brugeren som eneste deltager, og funktionen er idempotent. **Staging var ikke nødvendig for den halvdel, der bar risikoen** — `sql/schema.sql` ER produktionsskemaet — mens den halvdel, der faktisk kræver et miljø (knappen i Profil, `api/delete-account.js`, soft-sletningen i `auth.users`), står tilbage i Tier 1. **Kørslen fandt `A37`, som ingen læsning havde fundet:** admin-rollen uddeles kun ved oprettelsen, der findes ingen forfremmelse, og en lukket konto kan ikke logge ind — så en liga, hvis eneste administrator lukker sin konto, kan aldrig omdøbes, slettes eller ryddes op i igen. Hver liga har præcis én administrator, nemlig sin opretter, så det er ikke et hjørnetilfælde. Ingen af de tre led er en fejl; fejlen er, at ingen havde ganget dem sammen. Ny `sql/dev/anonymize_rehearsal.sql` med to read-only opslag — det ene må køres mod produktionen og svarer på, om en liga allerede står sådan. Backloggen er 39 → 40, og Tier 1 er vokset fra to punkter til fire. `A36` er ikke besvaret, men flyttet: den afgøres efter `A37`, fordi de to har modsatrettede rettelser. `G81` kunne ikke køres (kræver et token), men dens forudsætning er efterprøvet. Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

*Forrige opdatering, 7. august 2026:* (**Story Engine v3 er BYGGET.** Motoren vælger nu frem for at udgive: ét `period = 'day'`-kort pr. bruger pr. dag på tværs af alle ligaer, valgt på nyhedsværdi (grundvægt + størrelse + nærhed) med publiceringstærskel 45. Karrusellen er væk, kortet udløber efter 48 timer, rundens sidste dag udgiver kun rundekortet — nu som tap-through med 4–5 frames — og milepæle kaprer dagens slot i stedet for at lægge sig oveni. **Tre ting blev anderledes end spec'en**, og de står i spec'ens §13: ét-slot-indekset måtte have `and news_value is not null`, fordi spec §9's snævre prædikat ikke kan sameksistere med kravet om at beholde v2-rækkerne; `MILESTONE` scorer fast 120 uden størrelsesbidrag, ellers ville motoren og cron-kapringen skrive forskellig `news_value` for samme kort; og `DAY_RESULT` får kun afstandsleddet, fordi spec §5's regnestykke (8 + 12 + 20 = 40) ellers ikke holder — med hele størrelsesloftet ville dagens facit nå 58 og kunne udgive sig selv som dagens historie, og så ville fald-tilbagen aldrig findes. **Nærheden viste sig at være hele arbejdet:** leddet giver kun mening, hvis en kandidat kan gå til en modtager, der ikke er hovedpersonen, så tre regler fan-outer nu med tredjepersons-tekster, afgrænset strukturelt af `competition_participants`. **Acceptkriterie 10 holdt, men ikke gratis** — forholdet til `recompute_ratings()` er 2,26 mod v2's 2,28, og det krævede at frames-bygningen præ-aggregeres (886 → 38 ms) og at dagsmotorens temp-tabeller indekseres. Backloggen er 34 → 33: `B27` er lukket, mens `A35` (er tærsklen 45 rigtig?) og `G78` (scoringstallene står to steder) nu står med opfyldt udløser. Spec: [`features/story-engine-v3.md`](./features/story-engine-v3.md). Se [`DECISIONS.md`](./DECISIONS.md) og [`CHANGELOG.md`](./CHANGELOG.md).)

**Forrige opdatering: 5. august 2026** (**`A25` er afgjort — og den er afgjort FØR sin egen udløser, fordi den udløser ikke kunne bruges.** Rækken ventede på "den første rigtige kontolukning", men en kontolukning er uigenkaldelig og sker én gang pr. person: i det øjeblik udløseren springer, ER prisen betalt, og det eneste, der retter det bagefter, er håndarbejde på en brugers rækker i produktionen. **Reglen:** `_anonymize_account()` melder den lukkede konto af de konkurrencer, hvor ingen kamp er låst eller spillet, og kun når mindst én anden deltager bliver tilbage. Alt, der er spillet, bevares uændret — også en deltagelse uden ét eneste tip, for den står stadig i en stilling, de andre har set. Begrundelsen er `A30`s skel skåret på konkurrencen frem for på listen: er der historik, bliver rækken; er der ikke, er pseudonymet en deltager, der aldrig kommer til at spille. **`A29`s forudsigelse holdt:** opdelingen i `_anonymize_account()` blev lavet med ordene "den dag anonymiseringen skal rydde en tabel mere" — det er dén dag, og `account_anonymization.sql`s ældre kopi af kroppen er dermed ikke længere den milde af de ni gen-kørselsfælder. Politikken, brugerens bekræftelse og administratorens siger nu det samme, og `LEGAL_OPDATERET` er flyttet, fordi behandlingen selv er ændret. **SQL'en er kørt samme dag:** `B24` (gen-kør `sql/liga_admin.sql`) blev oprettet og lukket inden for leverancen, anden gang på én dag efter `B23` — så reglen er levende i databasen og ikke kun i koden. Backloggen er 27 → 26. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Backloggens Tier 2 og Tier 5 er kørt — fire rækker leveret, og `G1` er skrumpet til én fil og flyttet til Tier 6.** `G73`: `admin_analytics_stories` tæller nu `viewable` — kort skrevet, mens deres egen runde stadig var den nuværende — og bruger den som nævner under BÅDE visnings- og afvisningsraten. 197 af 280 historier var efterfyldte dagskort, der aldrig kunne nå en skærm, så raten målte efterfyldningen og ikke brugerne; `generated` står uændret ved siden af, og forskellen mellem de to tal er selv oplysningen. Definitionen fanger desuden et runde-kort, hvis runde først blev spillet færdig efter den var forbi — det er den halvdel, der kan ske igen. `G71`: `LOKALE_NØGLER` er bundet til privatlivspolitikken med en oversættelsestabel, fordi teksten beskriver nøglerne i almindeligt sprog og et navne-match aldrig kunne virke — og **testen fandt straks en nøgle uden sætning**, `pc_onboarding_v1_card`. `G74`: hver ` ```sql `-blok i `docs/` bygges nu om til et `prepare` mod hele `sql/schema.sql` i CI. **Rækken spurgte, om det kunne gøres UDEN produktionsskemaet, og svaret er nej** — `B12`s `42803` opstår i parse-analysen og kræver, at serveren kender tabellerne, så et syntakstjek ville have sagt god for den. Det behøver den heller ikke: skemaet ligger i repoet. `G70`: **ROADMAP'ens beslutningslog er tom** — 46 rækker fordelt frem for kopieret, 18 ført til `DECISIONS.md`, 6 efterladt som rene leverancer i `CHANGELOG.md`, og regnskabet står i modtagerfilen, så udvælgelsen kan efterprøves. `G1`: de fire filer uden for `A23` er delt (`AdminScreen` 434 → 67, `HjemTab` 672 → 411, `ProfileScreen` 480 → 241, `CreateCompetitionScreen` 444 → 394); gevinsten er ikke linjetallet, men at to `useEffect`-tunge dataopslag blev til `data/createSources.js` og to nye funktioner i `data/home.js` med 27 tests. **Tier 1 blev fyldt igen af leverancen selv** (`B23`: `analytics_dashboard.sql`) — præcis som `B19` blev det — **men denne gang blev filen kørt af ejeren samme dag**, så `G73` er levende i databasen og ikke kun i koden, og tieret er tomt igen. Det er første gang, køen med to betjeninger holder trit. Backloggen er 31 → 27. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Tier 1 er kørt tom anden gang samme dag — og begge fund svarede "ikke en fejl".** `G72`: `select count(*) from competitions where mode_params ? 'awards'` svarer **0**, så Story Engines to kåringsregler er **uafprøvede og ikke døde** — de har aldrig haft en konkurrence at fyre på og fyrer af sig selv, første gang nogen slår kåringer til. Anderledes end `G8`, fordi kæden er dækket af en SQL-test i CI; stien er afprøvet, bare ikke af produktionsdata. Noteret i analytics-spec'ens §5D: **ALDRIG er et spørgsmål og ikke en dom.** `G75`: cron-job.org siger hver 12. time, altså det registret hele tiden har sagt — de fem jobs blev oprettet forkert 31. juli og er **siden rettet**. **Det var vores fejl og ikke registrets**, og det er dagens mest lærerige række: et `count(*)` over fjorten dage er ét tal for hele perioden og kan ikke skelne "har altid kørt forkert" fra "kørte forkert og blev rettet". Metoden validerede sig selv på `sync-live` og `send-notifications` og virkede derfor mere overbevisende, end den fortjente — de to havde bare ikke ændret skema undervejs. Skal spørgsmålet stilles igen, kræver det afstanden mellem **nabokørsler**, ikke et gennemsnit. Samme aflæsning bekræftede alle syv minuttal, inklusive Scotlands 15, som indtil da kun var udledt. Backloggen er 33 → 31. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Backloggens indbakke er ryddet: ti linjer blev til ni rækker** — `A32`–`A33` og `G70`–`G76`. Listen er 24 → 33 punkter, og **Tier 1 er fyldt igen samme dag, den blev tømt**, af to fund kørslen selv efterlod: `G72` (kåringsreglerne `AWARD_WEEK`/`AWARD_MONTH` har aldrig udløst — ét opslag afgør, om de er død kode eller bare uden opt-in) og `G75` (cron-registret siger hver 12. time, kørselstallene siger hver ~1,8 time for fem jobs). Tieret hedder nu "svaret ligger uden for repoet", fordi `G75`s svar ligger i cron-job.org og ikke i Supabase. **Én linje blev rettet med det samme frem for at få et ID:** bruger-flag-tællingen i `DOCUMENTATION.md` §13 og §20 — arbejdet var at tælle efter (ti navne, ni bruger-bundne), og så var rettelsen to ord plus en manglende nøgle. **Ingen blev forkastet eller foldet sammen**, og de to nærmeste kandidater holdt ikke: `G71` ville have fanget den drift, der lige blev rettet, men er ikke samme rettelse. **Runden har en kilde, ingen tidligere har haft:** alle ti kom af at *køre* noget, ikke af at læse. `G72` og `G76` er begge kode, der aldrig har kørt i produktion — og det ser ingen gennemlæsning. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Backloggens Tier 1 er kørt tom — alle fem aflæsninger er lavet, og tre af svarene var "ikke endnu".** Tieret havde været "det næste" i tre runder uden at kunne røres, fordi opslagene krævede en produktionsadgang, arbejdsmaskinen ikke har; de blev samlet til ét paste og kørt af ejeren. **To rækker er slettet:** `I16` (0 lukkede konti af 24) og `A11` (`header` for alle ni jobs, nul `query` — `?secret=`-fallbacken er fjernet af koden, og `CRON.md`s kolonne er udfyldt). **Tre er flyttet til Tier 6 med rigtige udløsere:** `G8` (nul multi-turneringskonkurrencer), `B12` (1 oprettelse efter mærkatet — n=1 kan ikke måle en fordeling) og `A5` (280 historier, **0 delinger** — Del-knappen er præcis det, højdepunkt-tieret har, så uden en deling findes signalet ikke; udløseren er nu den første deling og ikke "et par runder"). **Tre fund, der ikke er `A5`s spørgsmål, ligger i indbakken:** `AWARD_WEEK`/`AWARD_MONTH` har aldrig udløst, selvom v1.2 blev leveret for at give kåringerne en stemme (sandsynligvis fordi kåringer er opt-in og ingen har valgt dem til); 197 af 280 historier er dagskort, som karusellen aldrig kan vise, fordi den henter på nuværende `round_key`; og `DAY_RESULT` alene er 44 % af al output. **Analytics-spec'ens påstand om, at en visningsrate over 100 % er umulig, er rettet** — tabellen viste 200 % og 400 %, fordi tæller og nævner ikke tæller det samme, og fordi `logEventOnce` kun afgrænser pr. sidens levetid. **Det, resultatet siger mest om, er tieret selv:** de fem blev kaldt aflæsninger, som om svaret lå og ventede — men to opslag kunne ikke køre, ét pegede på den forkerte kilde, og tre svarede med tal for små til at bære deres egen beslutning. Backloggen er 24 punkter. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**`B12`s forespørgsel var skrevet, ikke kørt — og den kunne ikke køre.** Backloggen havde i to døgn teksten "forespørgslen er skrevet, tilbage står at køre den". Da den blev kørt, svarede PostgreSQL `42803`: vinduet i analytics-spec'ens §5F partitionerede på `(e.created_at < m.fra)`, et udtryk uden for `group by`, mens perioden blev udledt i `select`-listen. Perioden udledes nu i en CTE, og rettelsen er **efterprøvet mod PostgreSQL 16.13 med plantede data og en negativ kontrol** — 20 % → 60 % på `full_season`, procenter der summer til 100 pr. periode, og en `login`-række holdt ude af filteret. **Anden kørsel samme dag afslørede, at kilden var forkert valgt:** hændelsesloggen svarede med tre oprettelser i alt, alle `random` — plausibelt ved ~20 testbrugere, men ubrugeligt, fordi `analytics_events` først findes fra 30. juli 2026, så "før mærkatet" var to døgn og ikke appens historik. `competitions.mode` bærer samme oplysning som rigtige rækker over hele historikken, så §5F er byttet om: tabellen er primær kilde, hændelsen er kontrol. Efterprøvet med plantede data, hvor loggen kun så `random`, mens tabellen genfandt fordelingen 33 % → 60 %. **Læren er ikke "loggen er dårlig"** — den er lossy by design — men at et gulv kun duer, når man har rigeligt af det, man tæller. **Tredje kørsel gav svaret, og det er "ikke endnu":** 6 konkurrencer før mærkatet og **1** efter — syv i hele appens historik. En fordelingssammenligning med n=1 kan ikke måle noget, hvilket er rækkens eget første forbehold med et tal på, og ikke det samme som "mærkatet virker ikke". `B12` er flyttet til **Tier 6** med en udløser, der aflæses med samme opslag: tosifret `antal` efter mærkatet. Indtil da hviler mærkatet på produktjudgement — legitimt, bare ikke målt. **`I15`-påstanden var en regnefejl og er fjernet:** `mode = 'random'` dækker både Ugens kupon-kortet og en håndlavet Quick Pick, og `mode_params` skiller dem ikke, så "to rækker for ét opslag" holdt kun for den ene. **Tier 1 er nede på ét punkt** (`A5`). **En SQL-blok i et dokument har ingen CI bag sig** og er derfor en påstand om, hvad der ville virke — samme form som `G8` tidligere samme dag, hvor en godkendt testcase blev forvekslet med en skrevet række. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**`A11` er lukket: `?secret=`-fallbacken er fjernet, og opslaget bar et svar, ingen havde spurgt om.** Fallbacken lagde hemmeligheden i request-logs, og alle var enige om, at den skulle væk — det, der manglede, var et tal, fordi et forkert gæt giver 401 og en syncen, der står stille. Opslaget i `job_runs.detail` svarede entydigt over fjorten dage: `header` for **alle ni jobs**, **nul** `query`, tre `admin-token` fra manuelle "Hent nu". `(ukendt)`-rækkerne kunne i princippet have skjult en `query`-kørsel, men de har alle sidste kørsel 1. august 20:23–21:17, altså i udrulningsminuttet for feltet, og ikke én efter. **`CRON.md`s kolonne "Hemmelighed sendes som" er dermed udfyldt for alle ti jobs efter at have stået med `?` i en måned** — og kan ikke længere have en anden værdi, fordi alt andet end headeren giver 401. Begrundelsen for `?` var, at svaret lå i cron-job.orgs brugerflade; det passede ikke, det lå i jobbenes egne kørsler. **Og så bar samme opslag et fund, ingen havde spurgt om:** kørselstallene siger, at job 6–10 kører hver ~1,8 time og ikke hver 12. time — ~65 sync-kørsler i døgnet mod de ~10, registret lover. Metoden validerer sig selv, fordi `sync-live` og `send-notifications` regner ud til præcis deres dokumenterede skemaer. Ikke rettet, men noteret: tallene kommer fra `job_runs`, ikke fra cron-job.org. **Backloggen er 24 punkter, og Tier 1 er nede på to** (`B12`, `A5`). Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Tier 1 er endelig påbegyndt — de to første aflæsninger er kørt af ejeren, og begge svarede "ingenting", men hver sin slags.** `I16` er **slettet**: 0 lukkede konti ud af 24 profiler. Tællingen var hele opgaven, og den er lavet — `A25`s udløser er dermed et tal og ikke en henvendelse, man skal håbe på, den er bare ikke sprunget. `G8` er derimod **flyttet til Tier 6**, fordi rækkens egen præmis faldt: nul rækker med `mode_params.tournaments` betyder, at `B2`s testcase 3 blev klikket igennem og **ikke gemt** — en godkendt test og en skrevet række er to forskellige ting, og kun den ene kan aflæses bagefter. Nul rammer desuden bredere end antaget, fordi `A22`s Favorithold med flere hold også skriver feltet: ingen af de to skrivere har nogensinde kørt i produktion. **Forskellen på de to udfald er dagens egentlige lære:** et tomt svar lukker en række, når rækken bad om et tal, og åbner den, når rækken gættede på et. Det er fjerde gang på to dage, at en produktionsaflæsning siger nej til vores egen påstand om, hvad der står i basen — de tre første var skema-eksporten. **Tre aflæsninger udestår** (`A11`, `B12`, `A5`), og backloggen er 25 punkter. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**`B22` er kørt: `rating_core.sql` er gen-kørt, ratingene er regnet om, og `G68` er dermed levende i produktion.** En anden skema-eksport samme dag bekræfter, at `recompute_ratings()` nu bærer `order by score desc, exacts desc` — og at diff'en er 19 linjer og ikke ~2.400, hvilket er `G5`s eget bevis: filens CRLF overlevede indsætningen i SQL-editoren. Havde den ikke, ville hver linje i hver funktionskrop stå som ændret, og en rigtig ændring kunne gemme sig i støjen. **Backloggen er 26 punkter, og Tier 1 er rene aflæsninger.** Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Alle fem ventende SQL-filer er kørt i produktion, `B19` er lukket — og Tier 5 er kørt.** Kørslerne er bekræftet med en tilstands-forespørgsel frem for en huskeliste, så #41, #42, #43, #10 og #3 nu står i databasen. **Tier 1 er dermed rene aflæsninger igen.** Af Tier 5's fire rækker er tre lukket: `G60` (deadline-nøglen regner i dansk tid — filens eneste dato, der ikke fulgte husreglen, rettet mens den var harmløs), `G68` (`rnk` i `rating_history` bruger nu opgørets eget tiebreak, `score desc, exacts desc`) og `G2`, som er lukket som et **nej**. **`G68` er den eneste ændring i dag, der flytter historiske tal**, og det er derfor den frosne reference i `sql/tests/rating_equivalence.sql` er opdateret for anden gang nogensinde — pr. `CLAUDE.md` **er** den opdatering selve beslutningen. Tiebreaket har fået sin egen sektion i testen, fordi begge sider nu har det (samme dobbelthed som `A17`), og begge retninger er efterprøvet mod PostgreSQL 16.13. **`G2`s præmis holdt ikke:** de syv lint-advarsler er ikke ét mønster, men tre "hent data i en effekt" plus fire af helt andre slags — så et data-bibliotek, den beslutning rækken ventede på, ville kun have fjernet tre af syv. Loftet bliver på 7, og vilkåret står i `DOCUMENTATION.md` §12. **`G1` skrumpede:** `MainApp`s to invitations-flows er udskilt til `src/lib/data/invites.js` og har nu elleve tests. Linjetallet faldt kun 618 → 582, og dét er pointen — gevinsten var, at flowene kunne testes overhovedet, hvilket var netop den omkostning, `A23` stod og bar. **Skema-eksporten er kørt bagefter og fandt et hul i leverancen:** `rating_core.sql` (#0) er ikke gen-kørt, så produktionens `recompute_ratings()` rangerer stadig `rnk` på score alene — `G68` er merget som kode og **inert** i databasen. Det er ny række `B22`, og det er præcis dét, en frisk eksport er til for: den er den eneste kilde, der kan sige nej til en påstand om, hvad der står i produktion. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Backloggens Tier 4 er kørt tom for anden gang — fem lunter, hvor intet var galt i dag, og alle fem blev galt af sig selv.** `G58`: `anon` mistede sine tabel-grants med #34, men **sekvenserne** blev aldrig dækket — ordet "tables" stod tre steder i formuleringen — og de to stillings-view-filer sluttede med en `grant select … to anon`, som en helt almindelig gen-kørsel ville have givet rollen adgang tilbage med. Ny migrering #43 lukker sekvenserne; grants'ene er væk af filerne; og **tre kontroller i job-heartbeat'en aflæser nu tilstanden hver halve time** frem for at kræve en skema-eksport og et menneske. Rækkens (a) viste sig at være mindre farlig end den lød: `ALTER DEFAULT PRIVILEGES FOR ROLE x` gælder kun objekter, der oprettes **af** rolle x. `G59`: `round_standings`-opslaget i notifikationerne pagineres — `board.length` ER det N, beskeden gør en pointe ud af, og det er `G51`s fejl et nyt sted. `G62`: karriereprofilens tre GLOBALE komplethedsjoin grupperede `matches` uden join til `seasons`/`leagues`, så én uspillet **uofficiel** kamp kunne tilbageholde en global titel, brugeren havde vundet — **verificeret med negativ kontrol mod PostgreSQL 16.13: 0/0 før, 1/1 efter.** `G63`: brugernavnets unikhed og opslagsfunktion fandtes kun i den genererede `schema.sql` — et hul i gendannelsesvejen, ikke i produktion — og har nu en versioneret migrering med en test, der ER den vej. `G66`: det sidste udgående kald uden tidsgrænse. **To nye SQL-tests i CI.** ⚠️ **Fem filer skal køres i Supabase** (`B19`) — heartbeat'en er rød på tre kontroller, indtil #43 er kørt. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 5. august 2026** (**Backloggens Tier 2 er kørt tom for anden gang — fire steder, hvor teksten ikke passede på koden.** `G69`: privatlivspolitikken nævnte ni af ti `localStorage`-nøgler; `pc_pwa_onboarded` manglede sin linje, selvom oprydningen ved kontolukning og to guard-tests alle kender nøglen ved navn. `LEGAL_OPDATERET` blev bevidst stående — dokumentet lover kun at flytte datoen ved en ændring af behandlingen, og flaget har ligget der hele tiden. `G61`: `KIND_LABEL` kendte tre af fem beskedtyper, så Drift-forhåndsvisningen viste `award:` og `newleague:` som rå nøgler; fallbacken er tænkt som en overgang og var blevet tilstanden. `G65` **var større end rækken sagde**: `provider` og `live_enabled` manglede uden at gøre skade, fordi deres defaults tilfældigvis er Sportmonks' rigtige værdier — men `is_official` defaulter til `true`, så en turnering oprettet efter skabelonen ville begynde som officiel og ændre, hvad en titel betyder. `G67` **var et undertal**: `DECISIONS.md` manglede ikke bare Tier 2–5, men omkring tredive rækker; de fem, rækken navngav, er ført over, resten er noteret i indbakken, fordi udvælgelsen er et spørgsmål og ikke en kopiering. **Tier 1 er stadig urørt og stadig det næste** — anden gang i træk, at et tier under den køres, mens `B19` og de fem aflæsninger står stille, fordi de kræver en produktionsadgang. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 4. august 2026** (**Produktet hedder nu Leagly** — det forrige navn viste sig at være i brug af en lignende app. Navneskiftet er kørt i ét hug gennem app, PWA-manifest, ikoner, push- og delingstekster, vilkår og privatlivspolitik, hjemmesiden, SQL-filhoveder, backup-filnavnet og al dokumentation — historikken medregnet, så kun ét navn figurerer. **Championship-fanens tre konkurrencer hedder nu Rundens, Månedens og Sæsonens Champion**, og strukturen bag dem runde-, måneds- og sæsonchampionship; ordet *liga* er dermed frigivet til brugernes egne ligaer, som det hele tiden også betød. "Ugens Champion" var det oprindelige ønske og blev fravalgt, fordi konkurrencen kører på spillerunde og ikke kalenderuge — og fordi *Ugens* allerede var optaget af "Ugens bedste" og "Ugens kupon". **To-niveau-navnereglen er uændret:** kun den samlede stilling bærer titlen. Visuelt: `C.gold` er rettet til logoets `#F2C14E`, og kronen er afløst af pokalen fra logoet, tegnet som SVG i nye `src/ui/Wordmark.jsx` og efterprøvet mod ikonfilen (97,9 % overlap). De gamle `icon-192/512.png` er slettet, og manifestet har nu også en maskable-variant. `sql/story_engine.sql` er **gen-kørt i Supabase 4. august 2026**. **Udestår hos ejeren:** GitHub-repoet og Vercel-projektet er ikke omdøbt, så hjemmesidens links peger stadig på det gamle domæne — med vilje, så de virker. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 3. august 2026** (**Hjemmesiden (`I8`) har fået sit første udkast — fem statiske sider i ny mappe `site/`, IKKE publiceret.** Forside, sådan virker det, funktioner, om os (+ kontakt) og hent appen, bygget i appens visuelle identitet (kopierede selv-hostede fonte, tokens fra `theme.js`, håndbyggede telefon-mockups i ren HTML/CSS) og med ét formål: at få en besøgende til at oprette eller joine en liga — hvert CTA peger på appen. Mappen ligger uden for Vite-buildet og når aldrig et deploy; ingen JS, ingen analytics, nul eksterne requests, ingen opfundne brugertal. `I8`-rækken i backloggen står tilbage med det, der udestår: ejer-godkendelse af copy, kontakt-mail (pladsholder i `om.html`, noteret i indbakken), domæne (`I10`) og SEO (`I9`). Spec: [`features/hjemmeside-v1.md`](./features/hjemmeside-v1.md). Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 3. august 2026** (**Backloggens indbakke er ryddet: femten linjer blev til fjorten rækker** — `A27`, `B17`–`B18` og `G58`–`G68`. To linjer blev til én række, fordi de fører til samme tilstand: `#34`s efterladte default privileges og `grant select … to anon` nederst i view-filerne er begge veje ind i "`anon` har adgang igen", og de lukkes af samme arbejde (`G58`). **`B17` står øverst i Tier 1**, fordi den som den eneste på listen betyder et brudt løfte og ikke en manglende forbedring: indtil `sql/account_anonymization.sql` er gen-kørt i Supabase, lader produktionen `client_errors.user_id` stå, mens privatlivspolitikken siger det modsatte. Tier 2 og 4 blev tømt om formiddagen og fyldt igen om aftenen — de fjorten fandtes hele tiden, de stod bare ingen steder. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere samme dag: 3. august 2026** (**Dokumentationen er gennemgået mod appen** — over hundrede forældede påstande rettet på tværs af `DOCUMENTATION.md`, README, CLAUDE.md, `sql/README.md`, alle feature-specs og plan-filerne; specs bærer synlige "rettet efter levering"-noter. De tre tvivlsspørgsmål er afgjort af ejeren samme dag: `time_range`-filteret er bevidst (dokumenteret), cron-minutterne er aflæst og rettet i CRON.md, og anonymiseringen nuller nu også `client_errors.user_id` — migreringen skal gen-køres i Supabase. Fuld beskrivelse i [`CHANGELOG.md`](./CHANGELOG.md). Før det, samme dag: **Tier 5** — fejltelemetri (`G42`/`B16`), se changeloggen.)

**Tidligere samme dag: 3. august 2026** (**Tier 4 er kørt: fem lunter, der alle først tændte den dag, noget nyt kom til.** Ingen af dem gjorde skade i dag — det var hele grunden til, at de stod tilbage. **`G11` + `G32` er leveret samlet**, fordi rækkerne selv sagde det: `round_key()` var markeret `IMMUTABLE`, men brugte `ts::date`, som læser sessionens tidszone, og en genereret kolonne *kræver* immutabilitet — så Postgres tog markeringen for pålydende. Under UTC lå rundegrænsen mandag kl. 02.00 dansk. Nu aflæses datoen i `Europe/Copenhagen` på **begge** sider: databasen og klienten, hvor `APP_TZ` gør klokkeslæt, dagsgruppering, rundens etiket og TBD-låsen danske uanset hvor telefonen står. Prisen er valgt: en bruger i Californien ser kampens danske tid. **`G50` spurgte, om `grant all` til `anon` var bevidst — svaret er nej**, og det ændrer opgaven: de 22 grants kommer fra Supabases default privileges for `public` og gælder hver ny tabel, så bredden var en REGEL og ikke en liste. En oprydning uden det andet trin ville være rullet tilbage af den næste migrering. **`G52`**s foldning var det svære valg, rækken lovede — retningen følger af NFD, og grænsen ("ue" foldes ikke) står nu i en test. **`G4`** gør `npm run dev` højlydt frem for tavst: fallbacken til produktion gælder kun i et produktions-build. **To migreringer er kørt i produktion 3. august 2026** (`round_key_timezone.sql`, `anon_grants.sql`), og to nye SQL-tests kører i CI. Se [`DECISIONS.md`](./DECISIONS.md).)

**Tidligere: 3. august 2026** (**Tier 3 er kørt: de lokale kåringer har fået en stemme.** `competition_awards` har siden 1. august persisteret "Ugens/Månedens bedste" i en opt-in-konkurrence — men kåringen kunne kun ses ét sted, på boardet, og rækken blev kun skrevet, når en klient tilfældigvis åbnede det. **`B10` og `B11` er derfor én leverance:** Story Engine v1.2 giver kåringen to kort (65 `AWARD_WEEK`, 15 `AWARD_MONTH`), og notifikations-jobbet giver den både en besked og en **skriver** — hver kørsel kalder `award_competition_periods()` som `service_role`, præcis den vej, `A22`s guard blev skrevet med for øje. **Reglerne læser kåringen, de regner den ikke:** den er frossen ved sit eget kriterie og vises allerede på boardet, så en historie, der udregnede sit eget svar, kunne modsige den tabel, brugeren kan slå op i. **Regel 70 tier, hvor en kåring dækker** — "du vandt runden i X" og "du er Ugens bedste i X" er det samme øjeblik med to navne, og en bruger får kun ét kort pr. runde. **`B9`** er den eneste beskedtype uden en modtager-regel, og prisen for den bredde er strammere betingelser: turneringen skal være synlig OG have kampe, for rækken oprettes før den første sync. **`I5` viste sig halvt leveret** — Hjem-kortet har haft en Del-knap siden v1.1; det, der manglede, var arkivet i karriereprofilen, altså dét sted man kigger, når kortet er væk fra Hjem igen. Kæden fra kåringsrække til kort er testet i CI mod en rigtig PostgreSQL, fordi den går gennem tre filer og fejler tavst bag matches-triggerens exception-guard. **`sql/story_engine.sql` er gen-kørt i produktion 3. august 2026.** Se [`DECISIONS.md`](./DECISIONS.md).)

**Og før det: 3. august 2026** (**Tier 2 er kørt: syv steder, hvor koden sagde noget andet, end den gjorde.** Tier-listen blev skrevet i går, og dens andet trin var det, der kunne leveres i én ombæring uden en eneste beslutning i vejen. **`G54`** er den, en bruger kunne mærke: "Ny konkurrence"-knappen var gated på `groups.length > 0` — en betingelse fra dengang en liga-løs konkurrence var mulig — så vejen til kernehandlingen var skjult for netop den, der ikke havde en liga endnu. Det er **anden gang samme fejl rettes**: knappen blev flyttet ud af "Øvrige konkurrencer"-blokken i juli med præcis den begrundelse, men betingelsen fulgte med over — og den samme forældede præmis stod i dette dokuments egen 3. august-note, så begrundelsen er rettet sammen med koden. **`G3`** fjernede et argument, der aldrig kunne have en anden værdi: seks skærme hentede `competitions.rules` og sendte det gennem fire lag ned til `pointsFor()`, mens databasen har hardkodet 3/1 hele vejen. Point er nu konstanten `POINTS`, som også farvelægningen slår op i — og klienten skriver ikke længere feltet, fordi kolonnens default er præcis den værdi, den sendte. **`G57`** samlede fem håndrullede folde til én `Collapsible`; den ene af dem var en `<tr onClick>` uden hverken tastaturadgang eller aria, altså appens eneste fold, hvis indhold ikke kunne nås uden mus. Dertil `G53` (liganavnets regel fandtes to steder, kun det ene kendte begge grænser), `B15` (`font-src 'self'` gør `B4`s fjernede Google-kald til noget, browseren nægter), `G55` (efterfyldningens regel 3 kendte ikke `kickoff_tbd`) og `G56` (ingen test på, hvad syncen faktisk skriver). **`G5` krævede et svar uden for repoet og fik det:** skema-eksporten blev kørt, og alle 25 funktionskroppe i produktion har CRLF — advarslen i `rating_core.sql` var altså sand om databasen, mens filen selv var normaliseret til LF og dermed var blevet dét, den advarede imod. Kroppene er hentet ordret tilbage og er nu byte-identiske med `prosrc`; en ny `.gitattributes` holder dem der. Samme eksport lukkede `G21`s sidste punkt: `schema.sql` er frisk igen. **Tier 2 er dermed tomt.** Migreringen `sql/competitions_rules_cleanup.sql` (#32) er den eneste i mappen, det ikke gør nogen forskel at springe over. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 3. august 2026** (**Backloggen har fået en rækkefølge igen — 43 punkter i syv tiers.** Indbakkens elleve linjer er ryddet (`A25`–`A26`, `B15`–`B16`, `G53`–`G57`, `I16`; `B2`s testcase 3 blev foldet ind i `G8`), men det egentlige var, at listen ikke kunne læses som andet end et opslagsværk: siden ROADMAP'ens gamle prioriterede liste blev delt op 31. juli, har de fire ID-tabeller stået sorteret efter **ID**, altså efter hvornår noget blev opdaget, og et punkt fundet i går stod dermed over et, der havde ventet i tre uger. Den nye rækkefølge følger fire regler, og den første er den, der flytter mest: **et svar, vi allerede har, er gratis.** Tier 1 er derfor fem *aflæsninger* og ikke fem opgaver — `A11`, `B12`, `G8`, `I16` og `A5` kræver alle sammen kun, at nogen kigger i data, vi selv har skrevet, og tre af dem har stået som **åbne beslutninger** i ugevis, uden at nogen ventede på en beslutning. To af dem lukker eller skrumper hinandens rækker: `B12`s opslag besvarer også `I15`, og `I16`s tælling er det eneste, der giver `A25` en udløser. `G8` er formentlig allerede død — testcase 3 blev godkendt 2. august, og den ER den kodesti, rækken kalder uafprøvet. Rækkefølgen står i [`BACKLOG.md`](./BACKLOG.md); dette dokument beholder de syv leverede trin nedenfor som produktets historik.)

**Endnu tidligere: 3. august 2026** (**Onboardingen var der hele tiden — et flag fra en anden bruger slukkede den.** En nyoprettet konto mødte en helt tom Hjem-skærm: dato, "Hej &lt;navn&gt;", intet andet. Onboarding v1 var leveret i juli og uden feature-flag; den blev slået fra af `pc_onboarding_v1_complete` sat af den FORRIGE bruger i samme browser. Flaget fik proben til at blive sprunget over, så `onboarding` stod som `null` — og dermed forsvandt **begge** lag på én gang, for checklisten kræver en tilstand, og guidens gate afviser `null`. **Flagene beskrev en bruger, men lå på enheden.** `clearAllLocalState()` ryddede dem kun ved kontolukning, med den begrundelse at et log ud efterlader den samme person; det holder for log ud → log ind, men ikke for log ud → **ny konto**, som er den vej en tester og en delt telefon går. Bruger-id'et ligger nu i **værdien** og ikke i nøglenavnet — navnene bruges også af oprydningen, guard-testen og privatlivspolitikken — og greb er lagt på **alle seks** bruger-flag, ikke kun det, symptomet viste. Gamle flag uden ejer ignoreres frem for at blive migreret: at tildele dem den, der logger ind først, ville gen-indføre præcis fejlen. **Den anden halvdel er mere generel:** alle Hjems kort lå bag én betingelse uden else-gren, så "ingen data" og "noget gik galt" var den samme blanke side. Der ligger nu et fallback-kort under checklisten, som peger på Ligaer — ikke på opret-skærmen, for dér kunne en bruger uden liga lave en liga-løs konkurrence. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 3. august 2026** (**`B4` leveret: appen siger nu, hvad den gemmer — og man kan komme af med det.** Porten før enhver offentlig deling. **Skrifterne selv-hostes**, fordi `@import`en mod Google sendte hver besøgendes IP dertil allerede på login-skærmen — altså FØR nogen kunne acceptere noget; en politik kan beskrive en videregivelse, men ikke gøre den rimelig, når der ikke findes et samtykke at give. **Privatlivspolitik og brugervilkår** ligger som DATA i `src/lib/legal.js` og vises af én komponent, der hverken tager token eller callbacks — dét er kontrakten, der gør, at teksten også kan læses fra login-skærmen, hvor hverken session eller `MainApp` findes. Ved oprettelse står samtykket og 13-årsgrænsen under knappen. **Kontolukning er anonymisering**, og det er ikke den billige løsning: en rigtig sletning ville have kaskaderet brugerens LIGAER væk sammen med alle de andre medlemmers medlemskab (`groups.created_by`), og været blokeret helt for enhver, der havde oprettet en konkurrence (`competitions.created_by` uden `on delete`-regel). Tips, rating og kåringer bevares under et pseudonym, fordi de er vennernes stillinger. Funktionen har **nul parametre** — der findes ikke et bruger-id at forfalske. Lint-loftet faldt fra 23 til 14 som sidegevinst. **To pladsholdere står tilbage med vilje**, og migreringen skal køres FØR mergen. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 2. august 2026** (**`A15` og `B2` er begge lukket — og begge svar lå i den samme kørsels resumé.** Ingen af dem ventede på en beslutning, kun på at nogen åbnede Admin → Drift. **`A15`** spurgte, om Sportmonks' gratis-plan er "180 kald i timen pr. entitet" eller kontosidens "3.000 API-kald". Svaret er **begge dele, delt forkert op**: enheden er pr. entitet — leverandøren sender selv `requested_entity: "Fixture"` — og tallet er 3.000, ikke 180. Kørslen brugte fire kald og fik `remaining: 2996` tilbage i et vindue på 3.600 sekunder. Det forkerte tal er rettet i `DOCUMENTATION.md` §8 og `live-resultater-v1.md`, hvor det havde stået, siden live-syncen blev bygget. **`B2`** — turnering #2 — er dermed **færdig**: 198 af 198 kampe uden stille trunkering (præcis grundspillets 12 hold × 33 runder ÷ 2), dublet-kontrollens ene par (`Dundee` / `Dundee United`) er en ægte navnelighed og ikke et fejl-link, og ejeren har kørt testcases 2–6 samme dag. **Det, der bliver stående, er ikke en huskeregel men to kontroller:** `ambiguousTeams` i hver sync og `STAGE_LABELS`-testen — de dækker automatisk turnering #3. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 2. august 2026** (**`G49` lukket: data har fået en sikkerhedskopi, og sikkerhedskopien har fået en prøve.** Repoet kunne genskabe skemaet og ikke en eneste række — mens produktets værdi netop er rækkerne. Første skridt var at få stillet spørgsmålet, backloggen bad om: hvad giver Supabase-planen allerede? Svaret er **ingenting** — planen er Free, som hverken har automatiske backups eller PITR. Derfor tager `.github/workflows/data-backup.yml` nu dagligt et fuldt dump af `public` + `auth`, krypterer det (repoet er offentligt, og dumpet bærer e-mails og password-hashes) og lægger det som et 90-dages artefakt. **Det egentligt nye er ikke dumpet, men at det prøves:** hver kørsel gendanner sin egen kopi i en tom PostgreSQL og måler på nul `pg_restore`-fejl OG rækketal, der holder. En sikkerhedskopi, der aldrig er prøvet gendannet, er en formodning. Runbogen er [`RESTORE.md`](./RESTORE.md), og de tre ting, kopien IKKE dækker — 24 timers tab, Vercels miljøvariabler og VAPID-nøglerne — står skrevet dér, fordi de ellers opdages på det værst tænkelige tidspunkt. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 2. august 2026** (**Brugerne kan sige til (`B14`) — og leverandør-id'erne fik constraints (halve `G7`).** Der fandtes ingen vej fra en bruger til udvikleren: en fejl, en forvirring eller et ønske døde, hvor det opstod. Kortet "Sig til" ligger nederst i **Sådan virker det** — ét tryk fra ⓘ i toppen af enhver skærm, og nabo til versionsstemplet, fordi de to besvarer hver sin halvdel af samme spørgsmål. **Meldingen bærer version, skærm og browser af sig selv**, og de tre står synligt i formularen, før der sendes; en videregivelse om brugeren skal kunne ses, når de trykker, ikke i en politik, ingen læser. En tabel med RLS frem for en `mailto:`, som efterlader ingen liste — og med to bevidste afvigelser fra `analytics_events`' mønster: skrivningen er **ikke** fire-and-forget (brugeren venter på svaret), og `user_id` er `on delete set null`, fordi en fejlmelding er sand efter kontoen er væk. **`G7` blev halveret og viste sig samtidig at være forkert formuleret:** to af de tre globale unikke ville have fejlet på produktionsdata, fordi Arsenal spiller i to turneringer og fem turneringer deler sæson-id. Constraints lukker i stedet en anden fejlklasse — to samtidige sync-kørsler, der begge indsætter det samme hold — mens præfiks-halvdelen er blevet et **valg** om en permanent trigger mod en fejl, der retter sig selv. To migreringer skal køres FØR frontend-mergen. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Ni gældsrækker lukket i én oprydning — den slags, der hver for sig ikke er værd at åbne en PR for (`G12`, `G18`, `G21`, `G36`, `G37`, `G38`, `G39`, `G40`, `G41`).** Fællesnævneren er, at de alle sammen er kode eller tekst, der **lyver lidt**: en kommentar, der beskriver et nøgleformat, koden aldrig har skrevet (`G12`); en ternær med samme svar i begge grene (`G37`); 17 eksporter uden forbruger og 12 modulhoveder, der stadig kaldte filerne "auto-genereret" fra en opdeling, der var færdig for længst (`G36`); og et testtal i dokumentationen, der var forældet **syv gange på ét døgn** (`G21`) — det er nu fjernet i stedet for opdateret, fordi `npm test` skriver tallet selv. To af dem er rigtige rettelser: `leagueId` valideres nu som UUID, før den interpoleres i en **service-role**-URL (`G18`), og et 500-svar før autorisationen navngiver ikke længere miljøvariabler (`G38`) — navnene står i logs, hvor kun vi kan læse dem. Og tre er ting, der manglede: **repoet har nu en README** (`G39`), `/api/*` kan nås i lokal udvikling via `npm run dev:api` eller en opt-in-proxy (`G40`), og værktøjskæden er koblet sammen med `.nvmrc`, `engines` og `test:watch`/`coverage` (`G41`). Gældsrækkerne er dermed gået fra 42 til **13** siden gennemgangen begyndte. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Tier 6's anden halvdel: fire punkter, der alle handler om, hvad der sker, NÅR noget går galt (`G25`, `G27`, `G42`s halvdel + error boundary pr. skærm).** Jeg holdt dem tilbage sidst, fordi de hang på spørgsmål, der ikke var stillet. Her er svarene. **Error boundary pr. skærm:** den leverede boundary sad om roden og kunne kun tilbyde en genindlæsning, så ét render-kast tog hele appen med sig — inklusive den navigation, brugeren skulle bruge for at komme videre. Nøglen `tab:screen` ER nulstillingen: en boundary, der har fanget, bliver siddende, fordi den ikke kan vide, om årsagen er væk, så teksten beder om at skifte fane, og der er bevidst ingen "prøv igen"-knap. **`G25`:** `db.select` tager nu et valgfrit signal, og `BoardScreen` afbryder ved skift af konkurrence — den kæde på seks opslag, hvor en `cancelled`-guard alene lod den gamle indlæsning løbe færdig. Skrivende kald tager bevidst ikke signalet: en afbrudt POST kan have nået serveren. **`G27`:** service workeren blev kun registreret inde i push-tilmeldingen, så en bruger, der ikke slog notifikationer til, aldrig blev en installerbar PWA — halvdelen af løftet var låst bag en helt anden beslutning. Nu registreres den ved opstart, og `beforeinstallprompt` giver et ét-tryks installations-knap, hvor browseren har en. **Caching er fortsat fravalgt, ikke glemt:** beslutningen om ingen fetch-handler står uændret, fordi en fejlbehæftet en er den klassiske måde at mure en PWA inde på. **`G42`s billige halvdel:** commit-SHA'en står nu både nederst i "Sådan virker det" og i hver `job_runs`-række — svaret på "hvilken version så du?" og på "kørte rettelsen overhovedet?", som kostede `B8` tre merges. Fejltelemetrien selv står tilbage. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Appen opfører sig nu som en app dér, hvor hænderne er (`G22`, `G28`, `G31`, `G34`, `G35` + halvdelen af `G13`).** Seks punkter, hvis fællesnævner er, at de sad på de kontroller, brugeren faktisk rører. **Login-skærmen — den første, enhver ny bruger møder, og helt utestet — var ikke en `<form`>:** Enter sendte ikke, telefonen fik et tastatur uden @, adgangskode-managere hverken udfyldte ELLER GEMTE, og felterne havde intet navn for en skærmlæser, fordi en placeholder forsvinder, så snart der skrives i feltet. **`ScoreInput` — appens mest brugte kontrol, og selve kernehandlingen — havde intet navn:** en skærmlæser sagde "redigeringsfelt, tal" om begge felter i en kamp uden at kunne skelne hjemme fra ude. Etiketten er nu en **påkrævet** prop, så et kaldsted ikke kan udelade den uden at det ses i diff'en. `Modal` er blevet en rigtig dialog med fokus, der kommer tilbage, hvor den kom fra, og de klikbare `<div>`s er blevet knapper. **Sletning brugte `window.confirm` og meldte ikke fejl** — den native boks brød det visuelle sprog netop dét sted, hvor handlingen er uoprettelig, og fejlede kaldet, blev rækken bare stående, som om intet var sket. **Opret-skærmen bad om `limit=1000`, præcis PostgRESTs loft**, så den 1001. kamp forsvandt uden fejl; den beder nu om én række mere, end den viser, og siger højt, at der er flere. Og **Admin hentes først, når den åbnes:** første-load faldt fra 425 til 363 kB (gzip 125 → 107). **Fire punkter af Tier 6 er IKKE med** — se beslutningsloggen for hvilke og hvorfor.)

**Endnu tidligere: 1. august 2026** (**Tre åbne beslutninger ventede ikke på en beslutning — de ventede på en aflæsning, der lå et sted, ingen kunne nå (`A11`, `A15`, `B12`).** Fællesnævneren er, at spørgsmålet var stillet, men svaret krævede noget uden for appen: Vercels logs, en henvendelse til Sportmonks' support, eller en forespørgsel, ingen havde formuleret. Alle tre er nu flyttet ind i appens egne data. **`A11`** — hver kørsel skriver, HVORDAN den autoriserede sig (`header`/`query`/`admin-token`), i `job_runs.detail`. `isAuthorized()` har altid vidst det; værdien blev bare kasseret, så det eneste spor var en advarsel i Vercels logs — hvor **fravær af advarsler ikke kunne skelnes fra fravær af kørsler**, og et deaktiveret cron-job så ud præcis som et, der kaldte rigtigt. Nu er det ét SQL-opslag med 30 dages historik, hvor et job uden kørsler simpelthen mangler i svaret. **`A15`** — Sportmonks lægger sit eget regnskab i hvert svar, og feltet `requested_entity` er netop dét, uenigheden handler om ("180 pr. entitet" eller "3.000 kald"?). Tallet står nu i `sync-live`s resumé, som er det job, der kommer tættest på grænsen. **`B12`** — forespørgslen er skrevet og står klar med de tre forbehold, svaret skal læses med; bl.a. at kort-rækkefølgen blev vendt samme dag som "Anbefalet"-mærkatet kom på, så de to ikke kan skilles ad i data. **`A5` er bevidst urørt:** den venter på, at der går tid, og instrumenteringen har været på plads siden 30. juli. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Fire huller, der alle ender i tavshed (`G17`, `G19`, `G47`, `G48`).** Fællesnævneren er ikke, hvor de sidder, men hvordan de fejler: ingen af dem giver en fejl, nogen kan læse — de giver en kørsel, der bare holder op med at eksistere. **Intet udgående kald havde en tidsgrænse.** En *hængende* leverandør — ikke en fejlende — stoppede hele kørslen, indtil Vercel klippede funktionen over, og en funktion, der klippes over, når hverken at skrive sin `job_runs`-række eller at rydde op efter sig. **Notifikations-loopet kunne tabe beskeder for altid:** claim-før-send er en ægte race-garanti, men den betyder også, at en claimet række aldrig forsøges igen — så løb kørslen tør for tid midt i loopet, var hver resterende række tabt, uden at nogen havde prøvet at sende den. Rettelsen skelner mellem **forsøgt** og **ikke forsøgt**: kun de uforsøgte får deres claim slettet, så næste kørsel samler dem op, mens en besked, hvis afsendelse fejlede, beholder sin række — vi ved ikke, om den nåede frem, og en dublet er værre end en manglende besked. **Timeouten var i øvrigt aldrig valgt**, den var platformens standard på 10 sekunder: repoet havde ingen `vercel.json`, så hverken varighed, region eller cache-headere var et valg. Funktionerne kørte i US-East mod en database i Irland. Og **Sportmonks-provideren gen-forsøgte straks ved 429** — et ekstra kald mod en grænse, der lige er ramt, hvert minut. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Fem mobil-først-løfter, appen ikke holdt (`G26`, `G29`, `G30`, `G33`, `B13`).** Produktet er en PWA bygget til kampdage på en telefon, og alle fem svigtede præcis dér. **Bundnavigationen lå under iPhonens home-indikator** — den primære navigation, delvist dækket. Rettelsen er tre indstik, der skal leveres samlet: `viewport-fit=cover` alene gør det værre, og fordi status-baren er `black-translucent`, skulle den øverste med. **Sessionen døde i baggrunden:** fornyelsen hang udelukkende på et `setInterval` på 45 minutter, og det er netop dét, mobile browsere suspenderer — en bruger, der lagde telefonen fra sig og vendte tilbage, mødte tomme skærme frem for en fornyet session, fordi ingen kaldte 401 ved navn. Nu fornys der ved `visibilitychange`, og en fornyelse, der bliver AFVIST (4xx), logger ud i stedet for at efterlade en død session — mens et netværkshul ikke gør, hvilket er hele grunden til, at fejlen nu bærer sin status. **Scroll-positionen fulgte med over i den næste skærm,** fordi der kun er én scroll-container og navigation er `useState` og ikke ruter (`A23`): åbnede man en konkurrence fra bunden af Hjem, startede stillingen langt nede i sit eget indhold. **Hjem fyrede sine seks kald pr. konkurrence forfra ved hver af MainApps gentegninger** — ikke på grund af filtreringen, men fordi et nyt array-objekt i fire skærmes afhængighedslister invaliderede deres effekter; belastningen voksede lineært med antallet af konkurrencer, netop for de mest aktive brugere. To `useMemo` lukker hele klassen ét sted. Og **"Dine placeringer" og rating-snapshottet hentede kun ved montering**, så en bruger med appen åben under en kampdag så forældede tal uden anden udvej end at genstarte appen; Hjem har nu en opdater-knap ved datoen. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Driftsloggen kan aflæses igen — fem gældsrækker lukket i én ombæring (`G6`, `G43`, `G44`, `G45`, `G46`), plus to fejl fra indbakken.** Fællesnævneren er ikke, at de var små, men at de alle sammen gjorde overvågningen *utroværdig*, og en overvågning, man ikke tror på, holder man op med at læse. Syv turneringer delte én række i `job_runs`, så den seneste kørsel vandt og en permanent fejlende turnering stod grøn bag en, der gik godt — præcis den fejlklasse, `B8` var, og den blev kun fundet, fordi nogen kiggede manuelt. Jobnavnet bærer nu turneringens UUID, og **både Admin → Drift og heartbeat'en udleder den forventede jobliste af `leagues`-tabellen** frem for af en nedskrevet liste, så en ny turnering forventes uden at nogen husker det. Kadencen stod fire steder med fire forskellige tal; registeret vandt (hver 12. time), og alarmgrænsen gik fra 14 til 26 timer — den var strammere end det skema, den skulle overvåge. `prune_job_runs()` havde ingen kaldere overhovedet, mens `CRON.md` påstod, at den ryddede: heartbeat'en kalder den nu, fordi den allerede har adgangen og allerede kører på et skema. Og heartbeat'en selv kiggede hver 6. time efter et job med en 30-minutters tavshedsgrænse — nu hver halve time. **To rettelser mere kom fra indbakken:** "Runden er slut" kunne stadig afgå for tidligt, fordi "alle kampe har resultat" er et svar om det, vi har *importeret*, ikke om runden — en kamp uden fastlagt dato findes slet ikke som række, og en kontrol af rækker kan ikke se en række, der ikke findes. Kun et lukket rundevindue garanterer, at sammensætningen ikke kan ændre sig mere; prisen er, at beskeden tidligst afgår tirsdag, hvilket sendevinduet i forvejen gjorde for de fleste runder. Og en push-kørsel med `failed=40, sent=1` stod grøn — nu vurderes fejlraten. **Den tredje indbakkelinje viste sig at være forkert:** `ensureCompetitionAwards` kan ikke afvise, den har sin egen catch. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**Forhåndsvisning af notifikationer fik en knap** — Admin → Drift → "Forhåndsvis notifikationer". Fundet som en omvej under `G51`: `?dryRun=true` viser hele outboxen uden at sende eller reservere noget, men der fandtes ingen vej til det, fordi `SYNC_SECRET` kun ligger i Vercel og admin-tokenen kun i appens hukommelse. Kortet er en læsning, ikke en handling. To valg i visningen kommer direkte af, hvad der gik galt: den grupperer pr. nøgle og viser **antal modtagere** — det tal, der var forkert i den falske besked — og den siger højt, at en tom liste betyder *intet nyt* og ikke *intet findes*, fordi `wouldSend` er filtreret mod `notification_log`. Ren frontend. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: 1. august 2026** (**`G51` lukket — en runde-notifikation meldte en runde slut, som ikke var slut.** Runde-opslaget i `api/send-notifications.js` havde kun en NEDRE grænse på `round_key` og bad dermed også om resten af sæsonen; med `A19`s fem officielle turneringer er det over de 1000 rækker, Supabase leverer i ét svar, og afkortningen er tavs. Faldt en rundes uspillede kampe uden for klippet, så runden færdigspillet ud, og feltet i "nr. X af N" talte kun de spillede kampes tippere. Ingen kodeændring udløste den — den kom af, hvor mange data der efterhånden var. Rettet i begge ender: `round_key=lte.<dagens danske dato>` fjerner de rækker, svaret aldrig skulle have båret, og `sbAll()` pagerer til en TOM side, så klassen af fejl er lukket for alle brede opslag i `api/`. Se [`DECISIONS.md`](./DECISIONS.md) og `DOCUMENTATION.md` §13.)

**Endnu tidligere: 1. august 2026** (**`B8` er lukket — Champions League fejlede ikke.** football-data.org har endnu ikke oprettet sæsonen 2026 (deres aktuelle er 2025), fordi ligafasen ikke er lodtrukket; `api_season_id` er rigtig, og turneringen henter af sig selv. Vejen dertil kostede tre merges og bar to lærepenge: diagnosen var bygget på en antaget tomhed (200 med tom liste), mens virkeligheden var 404 — og den rettelse blev afprøvet, mens dens eget deploy var **fejlet tavst** på Vercels 12-funktioners loft, så det samme svar blev aflæst tre gange som kodens resultat. Se [`DECISIONS.md`](./DECISIONS.md) og `DOCUMENTATION.md` §13.)

**Endnu tidligere: 1. august 2026** (**`B8` er halveret: en tom sæson forklarer nu sig selv.** Champions League hentede ingen kampe, og spørgsmålet stod åbent, fordi kørslens svar ikke kunne skelne de to mulige årsager — `/competitions/CL/matches?season=2026` svarer 200 med tom liste både når sæsonen findes uden kampe, og når `api_season_id` peger på et år, leverandøren ikke kender. Den ene retter sig selv, den anden gør ikke. `sync-matches` spørger nu selv datakilden, når nul kampe kommer hjem, og lægger svaret i `emptySeason` i Admin → Drift. Selve aflæsningen mangler stadig. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: august 2026** (**`G20`, `G23`, `G24` lukket: appen kan nu fejle synligt** — frontenden havde hverken fejltilstande på indlæsningerne eller en error boundary, så en fejl endte enten som en spinner, der blev stående, eller som en hvid side. Det dyreste af de tre var det mindste: en tom `catch` om gemningen af et tip. Den var sand om dataene og forkert om brugeren — ✓'et er den eneste kvittering, et gem har, så et fejlet gem så nøjagtig ud som et, man ikke havde set kvitteringen på endnu. Det er kernehandlingen, og den eneste med en deadline. Ingen af de tre rettelser opfandt et mønster: slette-stien, `ProfileScreen` og repoets egne fejlkort havde dem alle i forvejen. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: august 2026** (**`A24`: push sendes kun mellem 08 og 22 dansk tid** — som en *tavshed*, ikke som en kø. Rammer notifikations-jobbet natten, stopper det før beskederne overhovedet udregnes og reserverer intet i `notification_log`; kl. 08 udregnes de forfra og lander som én besked. Det billigste korrekte var derfor ingenting — køen fandtes allerede i form af, at outboxen genberegnes hver kørsel. Deadline-påmindelsen bortfalder til gengæld bevidst frem for at blive udskudt: dens indhold er en nedtælling, og en nedtælling, der er blevet usand undervejs, må ikke leveres. Se [`DECISIONS.md`](./DECISIONS.md).)

**Endnu tidligere: august 2026** (**`B5` leveret: ligamedlemmer får besked, når der oprettes en ny konkurrence i deres liga** — den tredje beskedtype i `api/send-notifications.js` og den første, der handler om fællesskabet frem for om kampe. Fire ting udelukker et medlem, og den mindst åbenlyse er den vigtigste: den, der meldte sig ind i ligaen *efter* oprettelsen, får ingen besked — konkurrencen stod der, da de kom ind. Beskeden dybdelinker med `?join=`, så trykket lander i den samme bekræftelse som et invitationslink i stedet for i en ny skærm. Ingen SQL, intet nyt cron-job.)

**Endnu tidligere: august 2026** (**`G9`–`G10` lukket: runde-resultat-notifikationen var holdt op med at afgå** — færdigmeldingen så alle turneringer, mens stillingen kun summerer de officielle, så én uspillet skotsk kamp kunne holde beskeden tilbage for alle; og stillingen blev læst uden `scope`-filter, så "nr. X af N" opgav et N, der var et multiplum af det rigtige felt. Begge sider følges nu ad. Forbeholdet i `B10`/`B11` er dermed faldet.)

(**`G14`–`G16`: de tre 🔴-sikkerhedshuller fra august-gennemgangen er lukket** — `matches` er admin-only at skrive i, `recompute_ratings()` er `service_role`-only bag wrapperen `admin_recompute_ratings()`, og `monthly_standings` har fået `security_invoker`. Ingen adfærdsændring for brugerne.) · Fra 1. august 2026: (**Opret-galleriet anbefaler Sæson** — kortrækkefølgen er vendt til langt → kort med Sæson øverst og mærket "Anbefalet", og hvert kort har fået ikon og varigheds-mærkat, så forskellen på typerne kan aflæses uden at læse. Se [`DECISIONS.md`](./DECISIONS.md)) · (**A22: opret-flowet er galleri-først, og lokale kåringer er leveret** — `I4`, `I13` og `I14` er lukket i én leverance: opret-skærmen viser konkurrence-typerne som seks kort (Sæson, Favorithold, Quick League, Quick Pick, Ugens kupon, Custom — rækkefølgen justeret samme dag, se ovenfor) og spørger derefter kun om dén types felter; en opt-in-konkurrence kårer "Ugens/Månedens bedste" i den nye tabel `competition_awards` (lazy SECURITY DEFINER-writer, ingen cron); Ligaer-fanens to permanente kort er foldet sammen til én knap-række, når man har ligaer. `I15` er skrumpet til automatikken alene — Ugens kupon-kortet leverer indholdet manuelt. **`sql/competition_awards.sql` skal køres i produktion FØR frontend-mergen.** Spec: [`features/opret-flow-v2.md`](./features/opret-flow-v2.md)) · Fra 31. juli 2026: (**A17 lukket: ratingen tæller kun officielle turneringer** — `is_official` betyder nu det samme overalt, så en turnering tæller enten alle officielle steder eller ingen. A2's argument kunne ikke genbruges: en titel er permanent, en rating slettes og genberegnes fra runde nul, så der var intet arkiv at beskytte. Det udslagsgivende blev sprogligt — status quo krævede en ledsætning om, at "officiel" betød to ting på to skærme. **Per-turnering-rating er afvist, ikke udskudt.** Prisen er kendt: tipper man kun uofficielle turneringer, får man ingen rating) · (**Migreringerne er kørt i produktion, og turnering #2 er i drift** — `tournament_scope.sql`, `story_engine.sql`, `career_profile.sql` og Skotlands datarække er kørt, og Scotland-cron'en er oprettet. `B3` er dermed lukket, og `A5`'s ur er startet: Story Engine v1.1's 14 regler genererer nu rigtige kort. **`schema.sql` er til gengæld forældet** — eksporten er fra 30. juli og kender hverken `is_official` eller `scope`) · (**Championship navngiver sit eget omfang** — `is_official` gjorde Skotland usynlig i stillingerne, men fanen sagde det ikke: både turneringsvælgeren og "To niveauer"-forklaringen er gated på mere end én *officiel* turnering, så med én af hver viste skærmen ingen af delene. Nu står der, hvad der afgør titler, hvad der ikke gør, og hvor pointene så bliver af. Gennemgangen rejste `A17`: ratingen har intet liga-filter overhovedet) · (**K2 lukket: karriereprofilen viser per-turnering-titler adskilt fra de samlede** — de tæller med, men i deres egen sektion, så et gammelt titeltal betyder det samme som et nyt) · (**A2 afløst: Championship kårer på to niveauer** — det samlede (alle officielle turneringer) bærer titlen "Leagly", mens hver turnering får sin egen stilling med kåringen "bedste i X". Ny `leagues.is_official` adskiller *tipbar* fra *tæller i Championship*. Spørgsmålet blev afgjort i det abstrakte i juli; da turnering #2 gjorde det virkeligt, viste det sig, at de eneste konkurrencer, ingen selv har valgt, blev afgjort af valg truffet andre steder) · (**`B2`s kode leveret: turneringsvælger på Championship-fanen** — navne-regexet `/superliga/i` er væk, sæsonchampionshippet har én stilling pr. turnering med den ældste som forvalg, og rundechampionshippet tæller kun kampe i synlige turneringer, så en skjult turnering ikke kan holde 🏆 tilbage. Turnering #2 tændes **synlig** med det samme, hvilket fraviger A10 — begrundelsen står i beslutningsloggen. Tilbage står drift: SQL-scriptet, første sync og cron-jobbet) · (**A10 lukket: turnering #2 bliver Scotland Premiership** — planen indeholdt allerede en anden rigtig liga, så `B2` kan bygges og QA'es uden at betale; Premier League koster €29/md og venter på reel efterspørgsel) · (**Det ubyggede har fået ét hjem: [`BACKLOG.md`](./BACKLOG.md)** — åbne beslutninger, ubyggede opgaver, teknisk gæld og rå ideer lå spredt over syv steder, og ideer havde slet intet sted at være. Spredningen drev allerede: `DOCUMENTATION.md` §12 beskrev stadig en fil-opdeling, der var leveret, og pegede på en beslutning, der var lukket. Backloggen er den spejlvendte udskillelse af CHANGELOG og DECISIONS — de peger kun bagud, den peger kun fremad. Med en **indbakke** øverst, hvor en idé kan skrives som én rå linje uden ID og uden begrundelse; oprydningen sker næste gang nogen er i filen) · **30. juli 2026** (**Tragt for nye brugere bygget — A13 lukket: Onboarding v1's centrale påstand er nu målt** — produktbogens *"en ny bruger skal hurtigst muligt oprette eller tilslutte sig en liga"* og iagttagelsen, at selvstarteren faldt igennem mens den inviterede klarede sig fint, var indtil nu **aldrig efterprøvet**. Fire trin (konto → liga → konkurrence → første tip), splittet på de to veje ind, udledt af rigtige tabeller og ikke af hændelsesloggen — en tragt, der undervurderer sit eget første trin, er værre end ingen tragt. To visninger, fordi trinnene ikke er strengt indlejrede: trin-tællingen viser hvor stort et spring der forsvandt, og "hvor står de nu" er en ægte partition, hvor hver bruger tælles præcis ét sted) · (**Push-effekt: virkede beskeden, eller blev den bare åbnet?** — deadline-påmindelsen er produktets eneste aktive fastholdelses-værktøj, og vi målte kun dens open rate. Nu sammenlignes deltagelse blandt dem, der åbnede den, med dem der ikke gjorde, pr. (bruger, runde) — plus varsel før rundelås, som er den eneste knap, der reelt kan drejes på. Tallet er mærket som **korrelation og et loft**, ikke et estimat: de, der åbner notifikationer, er de engagerede i forvejen) · (**Story Engine pr. regel — A5 kan nu besvares med data** — v1.1 gik fra 9 til 14 regler, og ingen kunne sige, om alle 14 nogensinde var vist til et menneske. Pr. regel: genereret, vist, delt, afvist, plus permanent dækningsmåling. Genereret/afvist er rigtige rækker og præcise; vist/delt er hændelseslog og et gulv — de to holdes adskilt, fordi forskellen afgør, hvad man må konkludere. Regler, der **aldrig** har udløst, markeres separat fra dem, der blot er stille i vinduet; katalogen bor i JS, fordi RPC'en per definition kun kan se regler, der har udløst, og en test læser `sql/story_engine.sql` og fejler ved drift) · (**A12 lukket: Liga Health Score fjernet — en sammenvejet score kunne ikke sige, hvad der var galt** — de fire første rigtige ligaer fik 75/77/77/88 og var alle grønne, så metrikken kunne hverken skelne dem fra hinanden eller pege på den, der havde brug for hjælp. Tre af fem vægte målte desuden det samme ("kommer medlemmerne her"), så deltagelse reelt vejede mindre end de 35 %, tallet lovede. Erstattet af **Liga-diagnose**: målte signaler hver for sig plus én navngivet tilstand med en handling, valgt af 12 regler oppefra og ned — *årsag før symptom*, så en liga uden aktiv konkurrence hører "opret en konkurrence" i stedet for "for få tipper". Nyt nøglesignal **bredde** (hvor mange af medlemmerne der faktisk tipper), som den gamle score manglede helt: den målte, om folk *åbnede appen*, ikke om de *spillede*. Reglerne bor i JS og ikke i SQL — de er produktjudgement, der skal tunes ofte, og kan unit-testes; samme valg som Onboarding v1. A12 spurgte "hvornår rekalibrerer vi vægtene?", og svaret blev "vi fjerner den størrelse, der kræver kalibrering") · (**Måle-ordbog: hvert nøgletal i Analytics forklarer nu sig selv** — 36 metrikker × hvad måles / hvordan / kilde / forbehold bag en ⓘ. Svarene stod før kun i SQL-kommentarerne, altså præcis dét sted den, der læser dashboardet, ikke kigger. Samme grænse som karriereprofilens: etiketten bærer omfanget, ⓘ'en uddyber) · (**Fem fejl fundet ved gennemgangen af Analytics** — fire *døde felter*, der blev hentet uden nogensinde at blive vist (completion pr. måned, sessionstid for flercase-sessioner, hændelser pr. session, missede runder), et vindue der talte 31 dage, mens nabotallet talte 30, en liga-kolonne der hårdkodede 30 dage uanset vinduesvælgeren, og completion-søjler der tegnede en uge uden låste runder som et falsk 0 % — nøjagtig den falskhed, retention-sektionen omhyggeligt gråtoner væk. North Star vises nu med retning mod forrige vindue: "62 %" kan ikke bære en beslutning, "62 %, ned fra 71 %" kan) · (**Karriereskærmens forklaringer er flyttet ind i info-feltet** — de synlige scope-linjer er fjernet igen efter brugerfeedback ("det skal ikke stå på forsiden"), og hver sektion med tal har i stedet fået sin egen `InfoDot`: Titler, Rekorder, Milepæle, Rivaler, H2H og basistallene. Regel fremover: ny sektion med tal ⇒ ny `InfoDot`, ikke ny brødtekst) · (**K3 lukket: rivaler måler jævnbyrdighed, ikke antal historier** — rangeringen sagde "Din tætteste rival", men målte hvor dramatisk forholdet havde været; på testdata gav den gamle metode én rival, som ejeren slår 4-0, mens den mest jævnbyrdige var usynlig. Møde-definitionen er lånt 1:1 fra `h2h`, så de to steder svarer det samme, og `rating_history.rnk` er afvist, fordi den kunne nævne folk man ikke deler konkurrence med. Rivalnavnet er nu en tryk-flade) · (**Fem forbedringer af karrierestatistikken** — sæsontitel (`titles.season`: Championship har tre kåringer, karrieren registrerede kun to), "din bedste runde nogensinde" (flest point i én runde, vises kun ved >0 point), korrekte udfald i basistallene, toppunkt ringet ind i ratingkurven og akser på kurven. To af de fem var **døde felter**, der blev hentet uden at blive vist. Verificeret mod en rigtig PostgreSQL 16.13, hvilket fangede en fejl en gennemgang ikke gjorde: "din bedste runde nogensinde: 0 point") · (**Karriereprofilen siger nu, hvilke konkurrencer dens tal gælder** — skærmen blander bevidst to omfang (Titler/Rekorder/basistal er globale: Championship + global rating; Milepæle er konkurrence-nære), og indtil nu sagde den ikke hvilket er hvilket. Hver linje navngiver nu sin kilde, hver sektion har en **synlig** scope-linje, og "4. plads" er blevet "4. plads af 31 spillere" via `records.best_round_rank_field` — udeladt ved `rank >= field`, så der aldrig står "8. plads af 8". Ny regel i beslutningsloggen: et tal skal navngive sit eget omfang i den sætning, det står i; en `InfoDot` må uddybe, men aldrig alene bære det, der skal til for at læse tallet rigtigt) · (**Analytics v1 leveret** — internt måle-lag: `analytics_events` (hændelseslog, kun INSERT/egne rækker) + `sql/analytics_dashboard.sql` (`analytics_completion_facts`/`analytics_round_locks`-views + fire `admin_analytics_*`-RPC'er) + admin-fanen "Analytics" med fire sektioner (Produktets sundhed, Engagement, Liga Health — siden erstattet af Liga-diagnose, Retention). North Star: Prediction Completion Rate, beregnet fra `predictions` — ALDRIG fra hændelsesloggen, som er fire-and-forget og lossy by design. Fire arkitekturvalg: kun Postgres/intet Google Analytics, ingen ny chart-dependency, udledte KPI'er frem for et nyt cron-job (`prediction_locked`/`story_generated`/`push_sent` findes allerede bedre i `matches`/`stories`/`notification_log`), samlet levering. Kolonnen hedder `group_id`, ikke `league_id` — samme "liga" ≠ "turnering"-ordbog som liga-laget. Health Score (0-100) var en v1-heuristik med navngivne, tunbare vægte — **fjernet igen 30. juli 2026**, se øverst. Spec: [`features/analytics-v1.md`](./features/analytics-v1.md)) · (**Onboarding v1 leveret** — de første to minutter: guidet flow for selvstartere, "Kom godt i gang"-checkliste på Hjem og kontekstuel hjælp overalt hvor nye brugere gik i stå. Tilstanden udledes af data, ingen SQL. To latente fejl lukket undervejs: `LigaerTab`s kode-indsættelse var ikke idempotent, og "Ny konkurrence"-knappen var usynlig for præcis de brugere, der havde brug for den) · (Story Engine nævnte folk, man ikke deler konkurrence med — `_se_rp` manglede join til `competition_participants`, så alle, der havde tippet den samme kamp i en anden konkurrence, indgik i stillingen · ny ufravigelig designregel: en historie må kun nævne personer, modtageren deler konkurrence med) · **Story Engine v1.1: 9 → 14 regler, kalibrerede tærskler med dynamisk prioritet og et dæmpet tier for runder uden andre historier — A3 og A4 lukket · dækningen i premiereugen gik fra 1 af 8 til 8 af 8 brugere, verificeret mod en rigtig PostgreSQL 16) · **27. juli 2026** (rundelåsen håndhæves nu også for skrivning — INSERT/UPDATE på `predictions` manglede låse-betingelsen, så et tip kunne skrives via REST efter låsen · dokumentation, app-tekst og kode bragt i overensstemmelse efter fuld gennemgang: 5 kodefejl, 9 tekstrettelser og 3 specs, der beskrev tilbagerullet eller udvidet adfærd · `sql/README.md` har nu filindeks med kørerækkefølge og advarsel om de to scripts, en gen-kørsel tavst ruller tilbage) · *Levende dokument — opdateres, hver gang en feature leveres eller en beslutning træffes. Filosofien bag prioriteringen: [`PRODUCT_BOOK.md`](./PRODUCT_BOOK.md).*

---

## MVP-status

Opgjort mod MVP-kravene i produktbogens kapitel 3, ud fra den faktiske kodebase.

| MVP-krav | Status | Bemærkning |
|---|---|---|
| Konto og login | ✅ Bygget | Supabase Auth med unikke brugernavne (case-insensitivt). |
| Afgivelse og redigering af tips | ✅ Bygget | En kamp kan tippes, indtil den låser 1 time før sit eget kickoff (`A21`). Det valgfrie rullende tippevindue er fjernet igen 1. august 2026 (`B1`). |
| Automatisk låsning | ✅ Bygget | Per-kamp-lås i databasen (RLS): en kamp låses 1 time før sit eget kickoff og kan ikke omgås fra brugerfladen. Var rundebaseret indtil `A21`, 1. august 2026. |
| 3–1–0-pointsystem | ✅ Bygget | +3 for præcist resultat, +1 for korrekt udfald, 0 ellers. Ingen minuspoint. |
| Automatisk import af kampe og resultater | ✅ Bygget | Serverless sync med cron (hver 12. time pr. turnering; kadencen var anført tre forskellige steder med tre forskellige tal indtil `G6`, august 2026 — registeret i `docs/CRON.md` er kilden), automatisk holdoprettelse og opdatering af flyttede kampe. Suppleret af `api/sync-live.js` (juli 2026): ét cron-job hvert minut, der viser live-stilling og færdigmelder kampe inden for et minut efter slutfløjt. |
| Konkurrence- og månedsstillinger | ✅ Bygget | Fulde stillinger med form, præcise hits og placeringsbevægelse. Fælles tiebreaker-stige i alle stillinger (præcise → udfald → rundesejre → tættest på), med delt placering og delt titel ved ægte lighed. Månedschampionship med Månedens Champion. |
| Global multiplayer-Elo | ✅ Bygget | Ét ratingskridt pr. spillerunde på tværs af alle konkurrencer. Skridtet genberegnes automatisk, hver gang et resultat gemmes, og er dermed foreløbigt, indtil rundens sidste kamp er spillet. |
| Enkel ratinghistorik | ✅ Bygget | Ratinghistorik med bevægelse, formkurve og provisorisk markering af nye spillere. |
| Mobilorienteret forside | ✅ Bygget | Hjem-fanen: deadline-kort med nedtælling, live-oversigt over indeværende runde, rating-snapshot og egne placeringer. |
| Invitationer | ✅ Bygget | Delbare invite-links, to slags og begge permanente (A7, juli 2026): liga-linket (`?liga=`) inviterer til fællesskabet, konkurrence-linket (`?join=`) direkte til én konkurrence — og melder samtidig ind i konkurrencens liga (A8). Stedet afgør: Invitér-knappen deler det, siden handler om. **Flowet er gennemgået som én oplevelse (`I7`, 11. august 2026):** link-preview med billede og — for crawlere — ligaens eget navn, invitationskontekst på login-skærmen før man opretter en konto, koden overlever en omvej over oprettelsen, afsendernavn i begge del-tekster, og `invite_landed` som tragtens midterste trin. Spec: [`features/invitationsflow-v1.md`](./features/invitationsflow-v1.md). |
| Permanente ligaer | ✅ Bygget | Liga-laget v1 live (juli 2026): `groups`/`group_members` + liga-side. `sql/groups.sql` kørt i produktion. Spec: [`features/liga-laget-v1.md`](./features/liga-laget-v1.md). |
| Konkurrencer inde i ligaer | ✅ Bygget | `competitions.group_id` + liga-dropdown ved oprettelse + liga-side viser ligaens konkurrencer. Liga-løse konkurrencer bevares under "Øvrige". |
| Frivillig tilmelding pr. konkurrence | ✅ Bygget | Deltag/Frameld dig pr. konkurrence på liga-siden. RLS tillader framelding, når enten alle konkurrencens kampe har resultat, eller man ingen tips har på låste kampe (`sql/group_membership_invariant.sql`). |
| Grundlæggende karriere og head-to-head | ✅ Bygget | Karriereprofil v1 (juli 2026): drill-in-skærm med hoved, titler (måned/runde), milepæle (story-arkiv, private), ratingkurve og rivaler + diskrete basistal. Ét DB-RPC (`career_profile`, `sql/career_profile.sql`); K1 er siden udvidet, så enhver indlogget bruger kan se enhver karriere (milepæle og rivaler forbliver private). Head-to-head bor fortsat primært i Story Engine (regel 40/60). **Udvidet (K4, 30. juli 2026):** ét narrativt cross-profile H2H-punkt, "bedste nogensinde"-rekorder og liga/konkurrence-fodaftryk. **Omfang tydeliggjort (30. juli 2026):** hver sektion siger nu, om dens tal er globale (Championship + global rating) eller konkurrence-nære. **Fem forbedringer (30. juli 2026):** sæsontitel, "din bedste runde nogensinde", korrekte udfald i basistallene, toppunkt og akser på ratingkurven. **K3 lukket (30. juli 2026):** rivaler rangeres på jævnbyrdighed fra faktiske møder. **Omfanget forklares i en `InfoDot` pr. sektion (30. juli 2026)**, ikke som brødtekst på siden — se beslutningsloggens sidste fire rækker. Spec: [`features/karriereprofil-v1.md`](./features/karriereprofil-v1.md). |
| Få, relevante notifikationer | ✅ Bygget | Web Push (juli 2026): deadline-påmindelse før kampene låser og runde-resultat med point og placering. `notification_log` sikrer, at samme besked aldrig sendes to gange — dedup er nu kapløbssikker (claim-før-send), så to samtidige cron-kald ikke længere giver dubletnotifikationer. **Tredje type (`B5`, august 2026):** ny konkurrence i din liga — den første besked om fællesskabet frem for om kampe. "Få" holdes af modtager-reglen: opretteren, den der allerede deltager, og den der meldte sig ind efter oprettelsen får ingen. **Sendevindue (`A24`, august 2026):** intet sendes mellem 22 og 08 dansk tid — "relevante" gælder også tidspunktet. **Fjerde og femte type (`B11`/`B9`, 3. august 2026):** lokal kåring ("Du er Ugens/Månedens bedste") og ny turnering. Den sidste er den eneste uden en modtager-regel, og betaler for bredden med to hårde betingelser: turneringen skal være synlig og have kampe. **Til/fra-kontakt på Indstillinger (`B40`, 18. august 2026):** push kan nu også slås FRA i appen — pr. enhed, med samme "nej tak"-flag som opt-in-kortets kryds. |
| Story Engine (enkel første version) | ✅ Live | v1 bygget: `stories`+`latest_story`, `generate_stories()` i matches-triggeren (exception-guarded), guld-historie-kort på Hjem. **Live for alle brugere** (skyggetilstand fjernet, juli 2026). **v1.1 (juli 2026):** 9 → **14 regler**, tærskler kalibreret (A4 lukket) og et **dæmpet tier** for runder uden andre historier (A3 lukket) — dækningen gik fra 1 af 8 til 8 af 8 brugere i premiereugen. **v1.2 (august 2026, `B10`):** 14 → **16 regler** — de lokale kåringer (`AWARD_WEEK` 65, `AWARD_MONTH` 15) læser `competition_awards`, og regel 70 tier, hvor en kåring dækker samme øjeblik. **v2 (august 2026):** dagsregler og karrusellen på Hjem ([`features/story-engine-v2.md`](./features/story-engine-v2.md)). **v3 (7. august 2026):** ét dagskort pr. bruger pr. dag, valgt på nyhedsværdi (tærskel 45) — det, der kører i produktion i dag ([`features/story-engine-v3.md`](./features/story-engine-v3.md), `DOCUMENTATION.md` §17). Spec-historik: [`features/story-engine-v1.md`](./features/story-engine-v1.md). |

**Ud over MVP-listen er der allerede bygget:** flere konkurrenceformater (hel sæson, enkelt hold, datointerval, håndplukkede kampe, tilfældig kupon), arkivering pr. bruger, indsigt i andres tips fra rundens lås, PWA-installation, en fuld admin-flade til kampe og resultater, DB-views til runde- og sæsonstillinger (`round_standings`/`season_standings`), en vitest-testsuite, staging-konfiguration, optimeret rating-trigger, rundebaseret åbningsvindue for tips og live-resultater under kampen (`api/sync-live.js`) og en sæson-simulator til staging (`sql/dev/simulate_season.sql`, 6. august 2026), der spiller en hel syntetisk sæson igennem for testbrugerne — i sin egen turnering, som ingen synkronisering kan overskrive.

---

## Kendte afvigelser mellem bog og app

- ~~**Spillerunder:** Bogen beskriver en global tirsdag–mandag-runde på tværs af turneringer. Appen beregner i dag pr. turneringsrunde.~~ **Lukket (1. august 2026):** afvigelsen fandtes ikke. `round_key()` har givet rundens tirsdag siden begyndelsen, og appen har aldrig regnet pr. turneringsrunde — leverandørens rundenummer importeres ikke. Se [`DECISIONS.md`](./DECISIONS.md).
- ~~**Navngivning:** bogen anvendte arbejdstitlen Prediction Hub, appen et andet navn.~~ **Lukket (juli 2026, A1), og navnet er siden skiftet igen (4. august 2026):** produktet hedder **Leagly**, og både bog og app bruger det hele vejen igennem. Ingen afvigelse tilbage.
- ~~**Administratorens rolle:** Bogen beskriver flere steder administrator som den, der opretter konkurrencer ("Administrator kan oprette så mange konkurrencer, som ligaen ønsker", kapitel 4 og 5).~~ **Lukket:** beslutning A6 gjorde det til **alle medlemmer**, koden har ingen admin-gate på oprettelse, og bogen er rettet — kapitel 4 og 5 beskriver nu værtsrollen frem for en rettighed.
- ~~**Global tirsdag–mandag-runde:** bogens kapitel 4 og 5 beskriver den som gældende, hvilket den ikke er endnu.~~ **Lukket (1. august 2026):** bogen havde ret, og markeringerne som "fremtidige" er fjernet igen. Den ene rest — gætte-vinduets scoping pr. turnering — er lukket samme dag ved at fjerne vinduet helt (`B1`, se [`DECISIONS.md`](./DECISIONS.md)).
- ~~**Månedschampionship-beskrivelser:** "Sådan virker det"-siden sagde "point pr. kamp i gennemsnit".~~ **Lukket (juli 2026):** `HowItWorksScreen.jsx` retter nu teksten til **samlede point** (tiebreak: flest præcise), så den matcher `monthly_standings`-viewet og Championship-visningen. Gennemsnit bruges kun i ratingen.

---

## Prioriteret rækkefølge

Ét princip: størst mulig fastholdelseseffekt pr. indsats, i den rækkefølge der lader hvert trin bygge på det forrige.

**Alle syv trin er leveret** (juli–august 2026), så listen er nu historik og ikke en plan. **Rækkefølgen på det, der er tilbage, står i [`BACKLOG.md`](./BACKLOG.md) → "Prioriteret rækkefølge"** — det aktuelle antal og fordelingen på tiers aflæses i backloggens eget hoved ("Alle N åbne punkter"), ikke her, hvor tallet rustede (opdateringsloggen øverst bærer bevægelserne). Status 10. august 2026, da afsnittet sidst blev skrevet: **Tier 1 til og med 5 var alle tomme.** Tier 4 blev kørt tomt med `G93` (dagskort-kontrollen fik sin test og sit CI-trin) og `G91` (de sidste to tests flyttet fra deres miniskemaer til produktionsskemaet — hvor den ene viste sig at have bevist en påstand, der var forkert). `G91` fandt undervejs `G94`, som blev skrevet og lukket samme dag: `competition_matches`' læsepolicy var en tautologi, der kostede en netop inviteret bruger hele ligasidens indhold. **`B25` er leveret samme dag:** appen har sin egen mailafsender (`noreply@leagly.app` via Resend), pladsholderne i privatlivspolitikken er udfyldt, og opsætningen uden for repoet står i [`MAIL.md`](./MAIL.md) — runbogen er kørt, og rækken blev lukket 9. august 2026. `B26` er dermed ikke længere gated, og `I10` er skrumpet til hjemmesidens domæne. **`B26`s klientside er bygget 10. august 2026**, og **rækken er kørt helt igennem 12. august 2026:** alle otte trin i [`OPRETTELSE.md`](./OPRETTELSE.md) bestået, Bot Protection **til**, `Confirm email` **til**. Rækken er slettet af backloggen. `B29` (brugernavnet kunne ikke ændres efter oprettelsen) er siden leveret, 10. august 2026. Det, der er tilbage, venter enten på en udløser eller er udadvendt. `G85` er leveret: markøren blev `matches.kickoff_uncertain`, display-only, udledt af de tider der flytter sig mellem to synkroniseringer. Tier 6 er det største (alt, der venter på en udløser), og Tier 7 bærer det udadvendte — de præcise antal står i backloggen. **Backloggens historik står nu kun i ét eksemplar** — filens Log-afsnit bærer den seneste kørsel, og ældre slettes (8. august 2026, se [`DECISIONS.md`](./DECISIONS.md)).

| Nr. | Indsats | Hvorfor nu | Omfang |
|---|---|---|---|
| 1 | **Story Engine v1 (regelbaseret)** | ✅ Bygget og live for alle (skyggetilstand fjernet, juli 2026) — og **fungerer nu faktisk**: A9 (juli 2026) afslørede, at motoren aldrig havde genereret en eneste historie i produktion. Produktets motor ifølge kapitel 6. Ingen AI. **v1.1 (juli 2026):** dækningen rettet efter første rigtige runde — 14 regler, kalibrerede tærskler (A4) og dæmpet tier (A3). **v2 (august 2026): [`features/story-engine-v2.md`](./features/story-engine-v2.md)** — motoren taler nu også, når dagens sidste kamp er spillet. Syv dagsregler (bånd 110–189), højst 2 kort pr. bruger pr. dag, som akkumulerer gennem runden i en vandret karrusel på Hjem med rundens konklusion øverst. Samtidig blev **milepæle skilt ud som selvstændigt begreb** — se trin 4. ~~Næste: læs tonen på de kort, der nu faktisk vises (A5).~~ `A5` er lukket 10. august 2026 (emojis bliver i kortene), og **v3** er leveret 7. august 2026 — se MVP-tabellens Story Engine-række. | Mellem |
| 2 | **Head-to-head som historieregel** | Indgår allerede i Story Engine v1-kataloget (regel 40 og 60) i stedet for en selvstændig statistikside — billigere og mere i bogens ånd. | Lille |
| 3 | **Liga-laget (permanente fællesskaber)** | Bogens vigtigste strukturelle princip og den største ændring. En gruppe-tabel oven på de eksisterende konkurrencer (konkurrencer får et gruppetilhør, medlemskab flyttes til gruppen, til-/framelding pr. konkurrence). **✅ Leveret og live (juli 2026): [`features/liga-laget-v1.md`](./features/liga-laget-v1.md)** — alle 3 faser + Hjem-gruppering pr. liga. `sql/groups.sql` kørt i produktion. | Stor |
| 4 | **Karriereprofil** | ✅ **Leveret (juli 2026):** ratingkurve over tid, titler (måned/runde), milepæle (genbrug af story-arkivet, private) og rivaler + diskrete basistal, samlet i ét DB-RPC (`career_profile`) gated på K1-relationen. **Udvidet (K4, 30. juli 2026):** cross-profile H2H-narrativ, rekorder og fodaftryk — se beslutningsloggens sidste række. **Omskrevet (august 2026):** milepæls-sektionen var et filtreret udsnit af `stories` (`priority < 90`) og blev derfor en rundelog, fordi Story Engine gemmer *alle* udløste kandidater hver runde. Milepæle er nu **engangs-bedrifter** i tabellen `milestones` — ~30 nøgler i fire familier, hver opnået én gang og aldrig trukket tilbage: [`features/milepaele-v1.md`](./features/milepaele-v1.md). **Spec: [`features/karriereprofil-v1.md`](./features/karriereprofil-v1.md)** — forudsætninger F1/F2 og K1 var lukket før implementering. Engangsopsætning: kør `sql/career_profile.sql` i Supabase ("Run without RLS"). | Mellem |
| 5 | **Global tirsdag–mandag-runde** | ✅ **Leveret — og var det længe før nogen skrev det ned (bekræftet 1. august 2026).** Rækken stod som ubygget, fordi den forventede en ombygning "fra turneringsrunder til kalenderuger". Den ombygning skulle aldrig laves: `round_key()` har returneret rundens tirsdag siden begyndelsen, `matches.round_key` er en genereret kolonne oven på den, og leverandørens rundenummer importeres slet ikke — der har aldrig eksisteret et turneringsrunde-begreb at bygge om fra. Det, der reelt manglede, var **tvær-turnerings-aggregeringen**, og den kom stykkevis: `scope = 'ALL'` i rundechampionship og månedschampionship (`sql/tournament_scope.sql`, 31. juli 2026), rating og Story Engine har altid kørt på `round_key` alene, og Hjem-fanen samler på tværs af alle brugerens konkurrencer med dedup på match-id. Med syv turneringer i drift kunne påstanden endelig efterprøves frem for antages. **Én bevidst undtagelse:** det rullende gætte-vindue er stadig scopet `(season_id, round_key)` — fulgt som `B1` i [`BACKLOG.md`](./BACKLOG.md). Fuld begrundelse i [`DECISIONS.md`](./DECISIONS.md). | Mellem |

| 6 | **Onboarding v1 (de første to minutter)** | ✅ **Leveret (juli 2026):** [`features/onboarding-v1.md`](./features/onboarding-v1.md). Produktbogens stærkeste påstand om nye brugere — *"en ny bruger skal hurtigst muligt oprette eller tilslutte sig en liga"* — var ikke sporet noget sted, og selvstarteren faldt igennem: syv skærme og ti valg før første tip. Den inviterede bruger blev derimod allerede onboardet fint. Tre lag: guidet flow, checkliste på Hjem, kontekstuel hjælp. Ingen SQL. **Rettet 3. august 2026:** de tre localStorage-flag lå på enheden og ikke på brugeren, så en ny konto i en brugt browser arvede den forriges "færdig" og fik ingen onboarding overhovedet — flagene bærer nu et bruger-id, og Hjem har fået et fallback-kort, så skærmen ikke kan stå tom. Se spec §14. | Mellem |
| 7 | **Analytics v1 (internt måle-lag)** | ✅ **Leveret (juli 2026):** [`features/analytics-v1.md`](./features/analytics-v1.md). Produktbogens "fastholdelse før vækst" (kapitel 3) var indtil nu kun målt via DAU/WAU/MAU (`admin_user_stats()`) — uden en samlet North Star-metrik og uden liga- eller funktions-niveau-indsigt. North Star: Prediction Completion Rate, beregnet direkte fra `predictions` (aldrig fra den fire-and-forget hændelseslog). Seks dashboard-sektioner (Tragt, Produktets sundhed, Engagement, Story Engine-regler, Liga-diagnose, Retention) bag en admin-gatet RPC pr. sektion. **Gennemgået 30. juli 2026:** Liga Health Score fjernet (A12 lukket), måle-ordbog tilføjet, fire døde felter taget i brug og to vinduesfejl rettet. **Udvidet samme dag:** tragt for nye brugere (A13 lukket), Story Engine pr. regel (lukker A5's datamangel) og push-effekt. | Mellem |

**Eksplicit næste handling:** ~~byg karriereprofilen~~ **Leveret (juli 2026** — `sql/career_profile.sql` + `src/screens/ProfileScreen.jsx`**)**. **Næste:** ~~gen-kør `sql/story_engine.sql` i produktion (`B3`)~~ **kørt 31. juli 2026** sammen med `career_profile.sql`, `tournament_scope.sql` og Skotlands datarække — `B3` er dermed lukket og slettet fra backloggen. Nu skal der bare **gå tid**: lad et par runder løbe, og læs derefter tonen på de kort, der nu faktisk vises (`A5`), med regelstatistikken i Analytics i hånden. Parallelt: ~~afklar Sportmonks-abonnementet (`A10`)~~ ~~**`A10` er afgjort (31. juli 2026)** — **turnering #2** kan startes nu og koster ingenting~~ ~~**`B2` er i drift 31. juli 2026** ([`features/turnering-2.md`](./features/turnering-2.md)): koden, datarækkerne og cron-jobbet er på plads — tilbage står **verifikation**, ikke opsætning: dubletter i holdene (den fuzzy holdmatch er ikke afprøvet på skotske navne), fasenavnene mod `STAGE_LABELS`, og testcases 2–6.~~ **`B2` er lukket 2. august 2026:** to af de fire verifikationspunkter blev lavet om til permanente kontroller (`ambiguousTeams` ved hver sync, `STAGE_LABELS`-testen), kampantallet og dublet-meldingen er aflæst i Admin → Drift, og ejeren har kørt testcases 2–6. Rækken er slettet af backloggen. ~~Derefter den globale tirsdag–mandag-runde (`B1`, trin 5), som netop kræver to turneringer i samme kalenderuge.~~ **Den viste sig at være leveret (1. august 2026)** — trin 5 er ✅, og `B1` er skrumpet til gætte-vinduets scoping. Der er ikke længere nogen ekstern afhængighed i vejen. Alle ID'erne står i [`BACKLOG.md`](./BACKLOG.md). **Opdateret 3. august 2026:** den eksplicitte næste handling er nu **backloggens Tier 1** — de fem aflæsninger (`A11`, `B12`, `G8`, `I16`, `A5`), som ikke kræver, at der bygges noget, og som afgør, hvad de næste tiers overhovedet skal indeholde. `A5` er den ene af de fem, der stadig kun venter på, at der går tid. **Opdateret 6. august 2026: `B18` er leveret — staging findes.** Preview-miljøet peger nu på et selvstændigt Supabase-projekt med syntetiske data ([`STAGING.md`](./STAGING.md)), så en ændring kan prøves af, uden at brugernes rigtige data er indsatsen. Det gør `G76` mulig (kontolukningen har aldrig kørt og er uigenkaldelig) og fjerner den håndholdte advarsel i `DOCUMENTATION.md` §11 som eneste værn. **Opdateret 4. august 2026:** aflæsningerne har fået `B19` foran sig — `sql/season_end.sql` (#41) og `sql/liga_admin.sql` (#42) skal køres i Supabase i den rækkefølge, før dagens leverance virker i produktion. Det er ikke en sjette aflæsning, men en kørsel, og indtil den er sket, regner klienten stadig med det svage "afsluttet" og får en 404 på `competition_status`. ~~**Opdateret 10. august 2026:** den eksplicitte næste handling er **at køre `sql/username_change.sql` (#51) i Supabase**~~ **Kørt i staging og produktion samme dag.** ~~Den eksplicitte næste handling er derfor **`A40`**~~ **`A40` er bygget 10. august 2026.** ~~Den eksplicitte næste handling er **[`UDRULNING-A40.md`](./UDRULNING-A40.md)** — ni trin, hvoraf de fire første er en prøvekørsel i staging.~~ **`A40` er udrullet 11. august 2026**, og runbogen står som skabelon. `A5` er samtidig ude af sætningen ovenfor: den ventede på, at der gik tid, og er nu lukket som en beslutning frem for som en måling. **Opdateret 14. august 2026:** alt siden A40 — `B26`, `A43`, `I10`, `I8`/`A50`, `A52` (Vercel Web Analytics fravalgt i appen, sat op og rullet ud igen samme dag — se `DECISIONS.md` og `CHANGELOG.md`), `A53` og `G101`–`G108` — er ført i opdateringsloggen øverst i denne fil. **Den eksplicitte næste handling aflæses nu i [`BACKLOG.md`](./BACKLOG.md) → "Prioriteret rækkefølge"**: laveste ikke-tomme tier først; denne sætning udpeger ikke længere selv en enkelt handling, for det var den, der rustede.

**Tommelfingerregel:** Trin 1–2 kan leveres inden for den igangværende sæson og bygger direkte oven på de nye push-notifikationer (historierne kan på sigt genbruges som indhold i runde-resultat-notifikationen). Trin 3 modnes i Del 2 af produktbogen, før den bygges. ~~Trin 4–5 venter, til der er data og turneringer nok til, at de giver mening.~~ Trin 4 er leveret (juli 2026), og trin 5 viste sig at være det hele tiden — ventetiden gav alligevel noget, den ikke var tiltænkt: det var først med syv turneringer i drift, at påstanden kunne efterprøves i stedet for antages.

---

## Åbne beslutninger

Flyttet til [`BACKLOG.md`](./BACKLOG.md) den 31. juli 2026, sammen med det
ubyggede, den tekniske gæld og ideerne.

De åbne spørgsmål hørte til hos det, de gater. ~~`A10` gater turnering #2, som
gater trin 5 ovenfor~~ (`A10` er lukket 31. juli 2026 — turnering #2 er ikke
længere gated af abonnementet, og `A18` samme dag fjernede abonnementet som gate
for Premier League og Champions League helt); `A5` ventede på, at et par runder kørte (lukket 10. august 2026). Så længe de stod
her og opgaverne stod tre andre steder, var der intet sted at læse "hvad mangler
vi" i én omgang.

ROADMAP'en er dermed status og prioritering: hvad er leveret, hvad er næste, og
hvordan hænger det sammen med produktbogen. Det uafklarede står i backloggen,
det afgjorte i [`DECISIONS.md`](./DECISIONS.md).

Når en åben beslutning afgøres, flyttes den fra backloggen til `DECISIONS.md`
med dato og begrundelse.

## Trufne beslutninger

Arkivet er flyttet til [`DECISIONS.md`](./DECISIONS.md).

Flytningen skete i to tempi, og det andet er det, der gjorde den færdig. **30.
juli 2026** røg "Trufne beslutninger" derover, fordi den var vokset til
hovedparten af denne fils indhold — ROADMAP'en skal kunne læses for at *træffe*
en beslutning, ikke for at læse alle de gamle igennem. Tilbage stod en sektion
ved navn "Nyeste beslutninger", som samlede sig op igen og **5. august 2026 bar
seksogfyrre rækker**: en anden kopi af den log, den henviste til, hvor nogle
rækker stod begge steder og andre kun her.

**Det andet tempo (`G70`, 5. august 2026) fjernede den sektion helt.** En
beslutning, der kun stod her, så ud som om den aldrig var truffet — og det er
netop `DECISIONS.md`, `CLAUDE.md` sender én til for at vide, *hvorfor* noget
blev, som det blev. Rækkerne er fordelt frem for kopieret: hver beslutning ét
sted i `DECISIONS.md`, hver rene leverance ét sted i
[`CHANGELOG.md`](./CHANGELOG.md). Regnskabet for fordelingen — hvor mange stod
allerede der, hvor mange blev ført over, og hvilke seks blev efterladt som rene
leverancer — står øverst i `DECISIONS.md`s tabel.

**Der kommer altså ingen beslutningsrækker tilbage i denne fil.** Når en åben
beslutning afgøres, flyttes den fra backloggen til `DECISIONS.md` med dato og
begrundelse.
